# EYE OS - Emma0923 · 0.8 验证记录

最新日期：2026-09-24。验收范围是网页控制台、本机桥接与 ShellOS 只读状态联调，不是眼镜完整固件。下文 v0.1 / v0.2 保留为历史验证记录。

## 2026-09-24 交接发布

已加入 [现场数据与复现说明](../docs/hardware-test-Emma0924/README.md)，并保留以下同事 v0.7/v0.8 验证历史。当前标定仍未通过；最终发布复核见交接目录。

## 2026-09-24 Windows 现场通信与遥测

- 下载远程 `emma0924/upper-limb-improvements` 的 `8f5a817`，相对本机 v0.4 增加 2 个提交、27 个文件。下载时完整源码树与远程 `0407253254d21a9a8a2c03bd9e72cb39467180a0` 一致；本机修复另行记录，不声称已经合入远程分支。
- pyserial 实际识别 CP210x `COM6`（10C4:EA60）；PING 收到 PONG，VERSION 收到 `2.9.99.1`。本机 CIM 读取权限失败，网页原生 USB 查询仍显示不可用；串口直接核验与网页枚举结果分开记录。
- 固件的 ENABLE 同时使能电机和开启遥测。用户确认离身、杆件支撑和采集准备后，以有限时长采集；不运行腿部 main、不调用输出 Guard、不发送任何 T 力矩命令。每次均收到 OK,DISABLE，随后关闭串口。
- 尚未按电源键开机时，10 秒采集 1820 帧、181.9 Hz，IMU 有更新但关节角度/速度全零。用户开机后，静止 10 秒采集 1804 帧、180.3 Hz：左角度约 3.936°，右角度约 -8.352°；波动分别约 0.024° / 0.036°。
- 手动轻移杆件时，10 秒采集 1801 帧、180.0 Hz，左角度 -4.644° 至 6.540°，右角度 -86.412° 至 -78.024°。三次采集均无坏帧或重复板端时刻，主机最大帧间隔约 16 ms。右侧角度使用已有 ShellOS 归一化约定。
- 两侧在部分时间段同时变化；本次只证明两路读数随现场活动产生变化，尚未通过单侧隔离动作确认左右对应、正方向、中位或行程。未做三点校准、自动抬杆、阻尼和脉冲施力。
- 真实 Windows 记录中，1801 帧只有 641 个不同主机接收时刻。修复 PoseRecorder 对相同主机时刻的误拒：旧实现离线重放在第 2 帧失败；修复后全部 1801 帧和原始时间保留，活动记录仍判为 stable:false。继续拒绝真实时钟倒退、过滤重复板端时间，不伪造接收时间。新增 5 个回归用例，手部测试 40/40 通过；测试与重放均屏蔽真实串口访问。
- 网页 93 项测试通过，并刷新确认新版双杆输入与左右枪计数显示。当前手部实时服务尚未配置；已关闭串口的数据文件不能冒充网页实时遥测。

## 合并准备复核：main 67e6783 + 开发代码 5726a8d

- 获取远端最新 main 后，用 `git merge-tree --write-tree` 生成无文本冲突的合并树 `885b11111feb8e49bfbeaada8f10118beae8edbd`；没有合并任何正式分支。主分支的新 `shellos/ui/server.py` 内容与本分支角色字段同时保留。
- 在合并树的隔离副本运行本目录 `npm test`：**118/118 通过**。Python `test_device_ownership.py test_hands_haptics.py test_guard.py test_serial_ports.py test_fault.py test_app.py -q -k "not stress"`：**92 passed、2 subtests passed、2 deselected**。使用假串口、假连接和回放，不启动真实设备。
- 首次导出的精简测试副本漏了 `shellos/worlds/*.json`，11 项 App 测试因找不到默认场景失败；补齐来自同一合并树的场景配置后整套相关 Python 回归通过。没有修改项目实现或测试断言来消除失败。
- 本轮新增提交只更新合并指引、README 与本记录；未改变运行代码。GitHub PR #3 无自动 CI 检查结果，以上是本地合并树测试证据。远程真机校准数据、实际力反馈及双设备端到端体验仍未验收。

## v0.8 穿戴自查与游戏操作教学

- Node 九套 **118/118** 通过，无失败或跳过。新增按角色分组、未完成自查阻止校准与画面输入、切换穿戴方式和换人清空、独立角色连接、双套端口冲突、错误时间和过期快照、迟到返回、断连不自动恢复、每款游戏独立教学、主动暂停和清空练习结果等覆盖。
- 应用内浏览器走通单套双手流程演练：六项确认后解锁准备，靶场左右枪各一次及 Esc 完成教学；进入试玩计数从 1/1 清为 0/0。切换挖掘机重新教学，左杆控制动臂、左杆回中后右杆控制铲斗、点击暂停完成三步。只在明确的流程演练模式填写示例自查，未代替现场穿戴者认证。
- 浏览器自动化 Playwright 操作间曾触发画面暂停，改用同一浏览器的无障碍操作走通；未放宽 250ms 页面停顿保护。页面模式变更和自查结果不持久化。
- 窄屏验证发现并修复网格最小宽度与长按钮溢出，实测 250 CSS px 宽度下单套、双套均无横向溢出；已恢复默认视口。发现切换穿戴数量可能使游戏和教学场景不同步，已修复并增加回归断言、在浏览器复验。最终浏览器 error/warn 日志为空。
- 服务仅在 127.0.0.1:18907 提供画面预览，上游配置为未运行的本机端口，没有连接外骨骼、读取用户姿态或发送电机指令。设备链路及过期路径用模拟 HTTP 服务和时钟测试。
- 本轮只改 EYE OS 网页模块。绑带扣合、固定、握持与真实急停没有传感器证据；`wearVerified`、`hardwareOutput` 始终 false。手动勾选、操作教学和软件测试不能作为真机穿戴安全验收。原腿部游戏入口与硬件联动留待伙伴集成。

## v0.7 单套 / 双套与控制隔离

- Node 七套 **100/100** 通过。新增三种配置、停用角色不请求上游、单套同服务端口、双套重复实体端口拒绝、角色缺失/错配、切换后迟到响应、旧输入清空及设备页正常显示停用腿部。
- Python 核心 **66 passed，2 subtests passed**：`test_device_ownership.py test_hands_haptics.py test_guard.py test_serial_ports.py`。其中所有串口句柄为 mock；新增跨进程文件锁、进程被终止后锁释放、并发抢占、COM 别名、闭会话后回调不干扰新 Guard、两连接独立、固定端口及 USB 身份变化拒绝重连。只读 capture 也不能争用已占用端口。
- 原腿部故障/恢复与 App 回归：`pytest tests/test_fault.py tests/test_app.py -q -k 'not stress'`，**26 passed，2 deselected**，使用 ReplayLink / 假连接，无实际串口。
- 应用内浏览器实际验证：单套腿部禁用手部输入；单套双手完成模拟解绑、校准、握持、就位，左枪计数为 1/0；切换双套清空状态，读取时正确报告与单套服务不匹配。桌面配置区截图检查正常，浏览器 error/warn 为空。
- 未驱动硬件。网页选择是试玩配置，服务配置在启动时固定；没有新增网页电机启停接口。双套游戏完整联动、真机换用途流程、握持检测与真实力反馈仍待现场验收。

## v0.6 手部力反馈计算与校准准备

- 新增 `shellos/hands/` 独立控制内核与显式 Guard 适配、只读 capture、三点观测合并和离线验证报告；未改写原 SerialLink / Guard / main，没有运行真实硬件。
- Python：`tests/test_hands_haptics.py tests/test_guard.py tests/test_serial_ports.py` 共 **54 passed，2 subtests passed**，其中新增手部测试 35 项。覆盖参数拒绝、目标限速、保持/重力/阻尼分量、每轴幅值/时间变化率、脉冲包络与重放、数据/握持过期、设备/时钟变化、锁止恢复、专用 Guard 假设备接线、通信异常、无写入采集、观测整理。
- 14 秒、1400 周期的虚拟惯量模型完成抬起稳定、左/右/双侧脉冲、切换并重新就位、手部扰动；11.5 秒注入过期数据后保持故障和零提案。模拟峰值提案约 0.174 N·m，示例限值为 0.8 N·m；这些是虚拟模型参数，不能用于真机。报告包含 HTML 曲线、CSV 和所有工程数值为空的硬件配置模板。
- 原 Node 网页自动测试 **93/93 通过**。网页控制逻辑本轮不改，v0.5 浏览器验证仍作为既有行为证据。离线报告完成生成与内容检查；浏览器工具禁止打开本地 file URL，未绕过，也未将其记为视觉验收通过。
- 尚需现场设备身份/固件、扭矩符号、工程限位、负载/增益、物理握持/固定/急停及失力支撑验证。采三个角度点不等于物理输出可验收。

## v0.5 独立手部角度与双杆游戏

- 基于 Emma 开发分支 `codex/eye-os-Emma0924`，提交 `86974dc`。本轮仅修改本目录，未修改或运行真实串口、Guard、控制循环或腿部游戏。
- `npm test`：七套测试 **93/93 通过**。新增覆盖三点稳定校准、左右反向/非对称行程、死区、过期与源变更失效、读取中断/迟到响应、独立手部源配置、双枪冷却与回拉重新准备、页面停顿/切场景暂停、键盘不覆盖另一侧滑杆。
- 本机模拟上游提供明确的 simulation 数据，禁用实体串口与电机输出。浏览器实际记录中位 10°/-10°、后拉 -10°/15°、前推 40°/-45°；偏离中位时无法就位，回中后成功开始。
- 模拟左杆前推得到枪计数 1/0，右杆前推得到 1/1；挖掘机左杆使动臂达到 100%、铲斗保持 50%。切场景暂停；输入 400ms 过期帧后读取停止、两轴归零、挖掘机冻结，未自动恢复。
- 首选 agent-browser 完成加载、截图和部分校准，后因本机 daemon EOF 中断；使用应用内浏览器完成剩余完整流程。浏览器 error/warn 日志为空。
- 手机 390px 视口分别检查开发输入与角度校准展开状态，文档宽度 375px，无横向溢出；已恢复临时视口设置。桌面初始页及双杆游戏均正常显示。
- 软件测试通过不代表真实设备、双设备同时佩戴、自动抬杆、阻尼、脉冲或硬件安全验收。这些仍待后续专用适配与现场验证；原 v0.3/v0.4 结果保留为历史记录。

## v0.4 双手模式预览（历史）

- 新增纯浏览器虚拟抬杆、双杆输入、靶场脉冲及挖掘机交互；39 个模块文件。无新增硬件输出接口或 ShellOS 控制修改。
- Node 五套自动测试 **75/75 通过**，包含新增 13 项双手模式状态、反馈、异常时间、输入和无 I/O 边界测试。
- 独立审查确认内核无网络/串口/输出回调，硬件输出标志恒 false；无 I/O VM 完整流程通过。
- 应用内浏览器验证模拟解绑、校准、握持、一次抬起、脉冲计数递增、场景切换、左右杆独立控制及松手暂停归零。
- 桌面 1280px（文档宽 1265）与手机 390px（文档宽 375）检查均无横向溢出，浏览器 error/warn 日志为空；预览明确标为仅模拟。真实手持行程、位置闭环、阻尼或主动反馈均未接硬件。
- 首选 agent-browser 因本机 socket 目录写权限受限，改用应用内浏览器完成操作验证；测试未读取用户已有浏览器资料。

## v0.3 外骨骼连接与状态验证

- 读取 aima 主分支基线 `9881b6384d2a0939a081859e1242a271b7817d05`（珠峰风雪线第 1–3 轮合并）。交付包括本目录 33 个文件，另有 `shellos/device/serial_link.py` 的 Windows 端口发现窄修改和 `tests/test_serial_ports.py`；原有 `SerialLink` 类、Guard、握手和控制循环未改。
- 提交前再次读取并快进到 `c4f465b8e3d7afd400ebfaafccfebb251c9f63b2`；最新变化为导游语音与跑酷内容，`/state`、串口与 Guard 无差异。上述模拟联调基于 `9881b638`，未重启最新版本新增的语音缓存后台补全任务。
- Node v24.14.0：四套测试 **62/62 通过**，后端 13、显示 20、外骨骼后端 13、外骨骼前端 16。覆盖模拟/回放/真机来源、过期帧、无效上游、来源和请求边界、超时、慢 USB、后台挂起与迟到响应。独立复核确认延迟 8 秒的旧快照不会变为实时，新 USB 查询等待 8 秒后产生的新采样仍可显示。
- Python 3.12.14、pyserial 3.5、pytest 9.1.1：新增 8 项串口发现及 Guard、恢复、断流、急停回归合计 **33 passed，2 deselected，2 subtests passed**。未运行两项无关 stress 测试。运行器全局拦截真实串口打开与枚举；本机 pytest 私有临时目录权限异常，验证运行器改用工作区普通继承权限目录，不修改项目断言。
- 使用仓库自身模拟器 `python -m shellos.main --sim --ctl transparent --no-input --no-record --http 18865`，将 `SHELLOS_BRAIN` 指向 `http://127.0.0.1:9`；EYE OS 设置 `SHELLOS_PORT=18865`、`PORT=4188`。观察到上游有效、来源 simulation、帧龄新鲜、角度持续更新、deadman=0、已发力矩为 0。未打开真实串口，未访问云端 AI。
- 独立无登录态 Edge 浏览器验证：设备页开始读取、模拟标签、实时数值、暂停后数值清空、停止模拟器后服务未响应且数值清空、返回概览导航。桌面页面与 390×844 手机视口检查通过；手机文档宽度与视口均为 390，无横向溢出。应用控制台和页面脚本错误列表为空。
- Windows 本机原生枚举成功，CP210x 设备列表为空。因此 **真实外骨骼连接、设备握手、插拔恢复和佩戴体验尚未验收**；模拟数据不能替代真机验证。
- 本轮没有重新验证音频、录音或实际带屏眼镜视频链路；旧版结果仅作历史记录。未上传用户媒体、驱动、安装日志、开发虚拟环境或 GitHub 登录资料。

复验：在本目录执行 `npm test`。受限环境可使用 `node --test --test-isolation=none backend.test.mjs display.test.mjs exoskeleton.test.mjs exoskeleton-ui.test.mjs`。运行与契约见 [外骨骼接入说明](EXOSKELETON-Emma0923.md)。

## v0.2 aima 审阅提交前复核（历史）

- 已读取 `aima` 主分支基线 `a86564f` 的 README、ShellOS HTTP 服务、Three.js 游戏入口和 Luma 适配器，并补充合并说明；本次只新增 `EYE-OS-Emma0923/`，没有运行或修改现有外骨骼控制系统。
- Node v24.14.0 复验现有测试：**33/33 通过，0 失败或跳过**；后端 13 项、显示 20 项。模拟测试没有调用真实音频、录音、屏幕采集或外骨骼。
- 交付范围为 27 个受版本控制文件；不包括本机录音、安装日志、驱动、GitHub 登录资料或令牌。测试通过不代表已完成 ShellOS 游戏与带屏眼镜的端到端联调。

## v0.2 自动适配新增验证

- 自动测试共 **33 项通过**：后端 13 项、显示策略与控制器生命周期 20 项。覆盖外接主屏、多屏歧义、重复名称、负坐标、尺寸变化、弹窗失败、授权撤销、共享取消/替换/结束，以及选择器晚返回和旧播放失败的竞态。
- 实机只读显示接口成功：当前一块主屏，物理桌面 2560×1600；没有额外桌面显示区域，不代表本次验证了带屏眼镜。
- 浏览器显示授权成功，识别到内置主屏（1707×1067 CSS 像素），刷新后自动恢复已有授权；没有将缩放后的尺寸误当成原始物理分辨率。
- 从控制台打开独立输出窗口，收到就绪及 1108×691 窗口尺寸反馈，关闭后控制台状态恢复。
- 独立输出页在 1280×720 和 390×844 下可见，竖屏布局正常；新版主控制台在桌面与 804px 宽度下无横向溢出。进一步切换主控制台 390px 视口时自动化工具超时，未将该项计为通过。
- 上述浏览器检查时主页面和输出页错误/警告日志为空。
- 修复异步停止消息清掉新画面、迟到授权恢复已撤销权限、旧播放失败误停新流；均有回归测试。
- 待验收：实际浏览器游戏捕获画面到达输出页、真实眼镜呈像、真实插拔/全屏/镜像、多屏定位、延迟和 iOS/Android 视频连接。控制器模拟测试不能代替这些硬件和媒体流验收。

## v0.1 已完成的音频与资料功能验证

- Node 后端 9 项自动化测试全部通过：同源请求、Host、JSON 与大小限制、静态路径边界、状态缓存、并发播放锁、断连/静音/桥接错误、跨平台回退和设备名匹配。
- `app.js`、`server.mjs`、`sw.js` 均通过 Node 语法检查；应用清单返回 200 和正确 MIME。
- 真实 Windows 状态接口读到 E06-003B 播放与输入端点 Active、未静音，快照音量分别为 90% 和 100%。这些数值不是眼镜电量。
- 从页面点击试听，真实调用指定 E06 的 Windows 播放接口；完成后出现本人确认弹窗。测试人员没有代替用户点击“听到了”。本次物理听感仍待用户确认。
- 浏览器验证四个导航页、朗读配置、预设路线选择、设备能力说明弹窗与计时开始状态。
- 将应用自己的示例图标保存到 IndexedDB，预览成功，刷新后仍存在。测试留下的文件明确标为“本地示例”。
- 检查默认桌面视口、390px 和 320px 手机宽度；检查页面无横向溢出，手机使用底部导航。
- 最终浏览器错误/警告日志为空。
- 代码审查修复了录音保存期间重复开始的竞态、未保存时关页的提示、系统朗读暂停状态未清除的问题。

## 尚需真机或人工验收

- 实际麦克风录音、蓝牙音源是否为眼镜、录音音质、同时录放、手机锁屏及后台行为。本次没有采集用户声音，也没有代用户授予浏览器麦克风权限。
- 系统 TTS 的实际耳端听感；界面已读取到三个中文系统声音，但不据此宣称眼镜听见了朗读。
- 外部文件选择、下载、删除和存储空间耗尽路径。内嵌预览的自动化文件选择器未成功唤起；示例文件验证了本地写入与预览，但不能代替普通浏览器的文件选择器测试。
- iPhone / Android 实际设备测试、添加主屏幕和离线启动。响应式视口测试不等于手机蓝牙或浏览器兼容认证。
- HTTPS 手机部署、厂家拍摄/相册/电量/按键/固件 SDK、AI/翻译服务及 aima 工程合并。

自动化复验：`node --test --test-isolation=none backend.test.mjs display.test.mjs`。测试用模拟适配器和浏览器对象验证错误与生命周期，不会播放声音、获取真实屏幕共享权限或采集桌面。
