# 珠峰北坡 · 专项架构（主打地图）

> 9/23 定：展位默认地图 = 珠峰。评委 7 分钟只够爬一座山，这座山要让人记住。
> 负责线：E（珠峰线）。数据 `shellos/worlds/everest_north.json`，画面 `game/themes/snow_summit.js` + `themes/snow_summit/*`。

## 1. 设计目标

1. **腿上的力 = 屏幕上的山**：每段地形的力都有画面理由（冰壁 → 台阶阻力，中国梯 → 爬梯阻力，北坳 → 吸氧停顿）。
2. **路边有人间**：大本营是个村子，不是一片雪地。帐篷、经幡、牦牛、直升机，走过去会动、会响、峰哥会点评。
3. **越高越难**：天色、风雪、缺氧、人声随海拔变化，玩家不看 HUD 也知道自己在多高。
4. **只读状态，不碰控制**：场景互动只读 `route.at(s)` / `state`，不调任何控制接口。力只由 ShellOS 地形控制器出（安全员把关）。

## 2. 路线 × 力 × 画面（56 步）

| 段 | kind / 步数 | 腿上的力 | 画面地标 | 互动时刻 |
|---|---|---|---|---|
| 珠峰大本营 5200 m | flat 3 | 无 | **帐篷村**（黄/橙/红穹顶帐 + 大餐厅帐）、玛尼堆、「珠穆朗玛峰大本营 海拔5200米」石碑、经幡、**直升机停机坪** | 开场：直升机从镜头上方掠过、降落在停机坪（旋翼转 + 扬雪）；峰哥导游开场 |
| 东绒布冰川 | up 19 | 上坡阻力 | 冰塔林（serac）、**牦牛驮队**迎面下山、路边零星帐篷 | 牦牛铃铛声由远及近；走到牦牛旁边它们让路 |
| 前进营地 6500 m | flat 2 | 无 | 帐篷群 + 氧气瓶堆 + 晾晒的睡袋 | 雪线：开始下雪 |
| 北坳冰壁 | stairs_up 5 | 台阶阻力 + 落阶冲击 | 冰壁、路绳、铝梯 | 每一阶雪沫（stepFx） |
| 北坳营地·吸氧 7028 m | wait 1 | 停顿（红灯） | 北坳帐篷（被风压低）、氧气瓶 | 屏幕边缘缺氧暗角 + 喘气声；峰哥催吸氧 |
| 北山脊·大风口 | up 19 | 上坡阻力 | 风雪、路绳、远处**直升机救援**悬停 | 风雪最大；直升机第二次出现（远景悬停、探照灯） |
| 第一台阶 | stairs_up 2 | 台阶阻力 | 岩阶 | — |
| 第二台阶·排队上梯 | wait 1 | 停顿 | 排队的登山者（climber 实例） | 排队人群前挪一格 |
| 第二台阶·中国梯 | stairs_up 3 | 爬梯阻力 | 黄色中国梯 | — |
| 顶峰雪坡 8848.86 m | up 1 | 上坡阻力 | 测量觇标（beacon）、经幡、云海 | 登顶：云海 + 峰哥登顶台词 + 直升机远景绕顶一圈 |

## 3. 模块分层

```
everest_north.json            路线（kind/steps/label/alt）── 力的唯一来源
        │
snow_summit.js  build()/update()/rigFor()
  ├─ sky.js        天空、太阳、云海（按进度插 PAL.low/high/storm/summit）
  ├─ snow.js       雪粒（storm 强度随海拔 + 大风口）
  ├─ hypoxia.js    缺氧暗角 / 模糊（5300 m → 8849 m）
  ├─ props.js      静态道具几何：帐篷、氧气瓶、经幡、路绳、梯子、觇标、登山者、冰塔
  ├─ camp.js   ★新  大本营村落布局（大帐篷、石碑、玛尼堆、停机坪、晾衣绳）
  ├─ heli.js   ★新  直升机：低模机身 + 旋翼 + 尾桨；三段脚本（开场降落 / 大风口悬停 / 登顶绕飞）
  └─ yaks.js   ★新  牦牛驮队：6–8 头 instanced，沿路外侧下山，玩家靠近时侧让
```

- **触发只看进度**：`const seg = route.at(s)`；脚本按段 `label` 和段内比例触发，**不写死步号**（路线被 AI 改过也能对上，沿用现有 `zonesOf()`）。
- **音效**：直升机旋翼 / 牦牛铃 / 风声 / 喘气，按进度混音。用现有音频通道，`?sfx=0` 关。离开对应段自动淡出。
- **峰哥**：已有的 `seg` 事件在进新段时自动点评地标。大本营、北坳、第二台阶三段在 `brain` 的 FENGGE 提示词里给真实事实提示（例如「中国梯是 1975 年架的」这类事实，只写拿得准的）。
- **HUD（U 线）**：顶部显示海拔（按 `alt` 线性插值）+ 当前段名；登顶结算页写「8848.86 m」。

## 4. 性能预算（展位 MacBook M2 大屏 60 fps；`?fx=low` 给弱机）

| 项 | 正常 | fx=low |
|---|---|---|
| 新增 draw call | ≤ 12（帐篷/牦牛全部 instanced，直升机 1 个 group） | ≤ 6 |
| 新增三角形 | ≤ 40k | ≤ 15k |
| 直升机扬雪粒子 | 300 | 0 |
| 贴图 | canvasTexture 程序生成，不加外部文件 | 同 |

不引入新依赖、新模型文件（直升机、牦牛都用基本几何拼低模，风格跟 style.js 的哑光低多边形一致）。

## 5. 验收

1. `/game?world=everest_north` sim 模式走完全程无报错，登顶 < 4 分钟。
2. 五张截图：大本营直升机降落、冰川牦牛、北坳帐篷、大风口直升机悬停、登顶。
3. `?fx=low` 下帧率不低于改前。
4. `pytest -q` 全绿（控制层零改动）。
5. 改动只在 `themes/snow_summit*` 和本文档；要动 hud / fengge 的在指挥板留言给 U / 我。
