> 历史现场记录；当前交付结论以 [README](README.md) 为准。JSON 在 recordings/ 中压缩保存，先按 README 解包。早期 passed 仅表示通信；不是标定或力反馈验收。

# 双杆引导采集 · 2026-09-24

目标：单侧通道确认 → 中位/上抬/下放三点观测 → 回中重复性。
采集仅使用有限时长 PING / VERSION / ENABLE / DISABLE，不下发 T 力矩命令。
每轮结束须收到 OK,DISABLE 并关闭串口。观测角度不代表工程限位。

## 第 1 轮：左杆单独活动

- 用户确认设备已开机、离身且支撑可靠，准备轻动左杆、保持右杆静止。
- 记录：`telemetry-10s-20260924-091558.json`。
- 收到 1833 帧；查询、使能、停用均有应答；串口已关闭。
- 左右角度与角速度全为 0，未验证左侧动作响应，本轮不能用作通道校准。
- 用户随后确认“左杆动了，但设备已关机或待机”。恢复开机后重新采集，没有把本轮全零数据用于校准。

## 第 2 轮：开机后左杆活动

- 记录：`telemetry-10s-20260924-091734.json`，1804 帧、180.3 Hz，零坏帧；已确认停用并关闭串口。
- 指示仅活动左杆。左角度跨度 50.376°，右角度跨度 4.716°，主导变化位于左通道。
- 右侧仍有支撑晃动，不能当作严格机械隔离测试，也不能由来回动作直接判断前推正负方向。

## 第 3 轮：右杆活动

- 记录：`telemetry-10s-20260924-091836.json`，1807 帧、180.6 Hz，零坏帧；已确认停用并关闭串口。
- 指示仅活动右杆。右角度跨度 48.096°，左角度跨度 6.168°，主导变化位于右通道。
- 两轮数据支持按佩戴者左右映射通道，但非活动侧都有晃动，尚未做严格串扰或轴独立性验收。
- 后续姿态采集固定设备主体位置，以舒适握持中位为参考，明确使用每轮最后 3 秒稳定窗口。

## 第 4 轮：舒适握持中位

- 原始记录：`telemetry-10s-20260924-091949.json`，1805 帧、180.4 Hz，零坏帧；停用应答已收到、串口已关闭。
- 按采集前声明的规则使用末尾 3 秒：540 帧，左中位 −89.244°，右中位 −88.188°。
- 左角度跨度 0.024°，右角度跨度 0.060°，两侧最大角速度均 0.6°/s，通过稳定窗口校验。
- 已保存 `guided-center.json`，保留原始时间、已归一化角度、USB 身份和来源；这只是输入中位观测，不是电机限位。
- 用户随后说明杆件实际无法向后移动，之前的“后拉/前推”描述不适用。后续按实际可活动方向命名“上抬/下放”，保留中位；不把上抬伪装成 back 姿态。

## 第 5 轮：上抬姿态（未通过稳定性）

- 记录：`telemetry-10s-20260924-092355.json`，1802 帧、180.1 Hz，零坏帧；已确认停用并关闭串口。
- 最后 3 秒共 542 帧。左中值 −99.840°、跨度 6.288°、最大角速度 15.8°/s；右中值 −98.868°、跨度 4.668°、最大角速度 13.2°/s。
- 相比已保存中位，两侧上抬读数均减小，但稳定性不满足跨度不超过 2°且最大角速度不超过 5°/s 的观测条件。本轮保留原始记录，不导出合格标定点。
- 用户随后明确本轮实际“直接上抬到极限”。这是操作者描述，不是经过验证的机械限位角度；本轮不作为游戏工作端点，也不写入电机限位。
- 已说明游戏工作端点应在机械极限之前留出余量，等待用户离开机械顶点、选择能舒适稳住的位置后重采。

## 第 6 轮：离开机械极限后重采上抬（关节全零）

- 用户确认已从顶点回到舒适、能稳住的位置后采集。
- 记录：`telemetry-10s-20260924-092837.json`，1826 帧、182.5 Hz，零坏帧；PING、VERSION、ENABLE、DISABLE 均应答，串口已关闭。
- 左右角度、左右角速度在整个窗口均为 0。通信成功不等于关节测量有效；本轮不作为上抬标定点，不导出观察文件。
- 已请求用户核实开机工作态。当前不能仅凭全零断言设备已关机，也不能把这一轮当成真实上抬角度。

## 第 7 轮：再次确认工作态后重采（仍关节全零）

- 用户回复“已进入工作态并摆稳，立即重采”后即开始。
- 记录：`telemetry-10s-20260924-092933.json`，1809 帧、180.8 Hz，零坏帧；所有查询和停用应答完整，串口已关闭。
- 仍为四项关节数据全零，未导出上抬观察；停止重复使能采集，改为核实实际按键方式、指示灯及开机提示。
- 离线对比：091734 至 092355 四轮板时连续，期间每次停用并未阻止下一轮关节数据有效；092837 板时出现重置但 IMU 仍更新。不能归因于 DISABLE，也不能据此判断重置原因。
- 本地协议未给出独立的电源状态或关节在线字段，ACK 不能替代工作态确认。
- 用户反馈进入工作态的操作是“开机键长按，没有提示音”。已暂停重复按键及采集，等用户说明指示灯颜色/闪烁和具体型号。
- 查阅 Hypershell 官网链接的 X 系列用户手册 v5.1（2026-05），第 13、27 页描述开机为短按一次后迅速长按 2 秒，而已开机时单独长按 2 秒为模式切换。此资料仅用于排查线索；具体设备型号及开发固件行为尚未核对，未据此自动操作或断言原因。
- 参考：https://cdn.shopify.com/s/files/1/0746/6326/4492/files/EN_Hypershell_X_Series_UserManual_v5.1_Media.pdf?v=1780657810

## 第 8 轮：恢复后诊断采集（不作姿态标定）

- 用户回复“现在好了”后，仅诊断关节数据是否恢复，未要求摆标定姿态。
- 记录：`telemetry-10s-20260924-093129.json`，1807 帧、180.6 Hz，零坏帧；已确认停用并关闭串口。
- 左角度中值 −9.912°、右角度中值 −24.072°，四项关节不再持续全零。仅确认测量恢复，不赋予 center/upper/lower 姿态标签。
- 与旧 center 的板时推算起点不同；从当前状态重新记录三姿态，不混用重置前后的数据。
- 092837/092933 全零与本轮非零的板时起点连续，故时钟连续本身不能证明关节工作态正常。

## 第 9 轮：重置后重新采中位

- 用户重新摆到舒适游戏握持中位并确认后采集。
- 记录：`telemetry-10s-20260924-093219.json`，1810 帧、180.9 Hz，零坏帧；停用应答已收到、串口已关闭。
- 末 3 秒共 543 帧，左中值 −7.248°、跨度 0.912°、最大角速度 3.2°/s；右中值 −18.576°、跨度 1.032°、最大角速度 4.4°/s，通过稳定性校验。
- 已保存 `guided-center-session2.json`。本轮为新的中位基准，旧 `guided-center.json` 只保留历史，不混入当前三姿态。

## 第 10 轮：重置后上抬观察（稳定，但左侧行程不足）

- 用户确认舒适上抬工作终点、设备开机和主体位置不变后采集。
- 记录：`telemetry-10s-20260924-093317.json`，1802 帧、180.1 Hz，零坏帧；停用应答已收到、串口已关闭。
- 末 3 秒共 542 帧：左中值 −9.816°、跨度 0.192°、最大角速度 2.2°/s；右中值 −24.084°、跨度 0.012°、最大角速度 0.4°/s，稳定性通过。
- 保存 `guided-upper-session2.json` 作为如实观察，尚未应用为游戏标定。相对当前 center，左侧差 −2.568°、右侧差 −5.508°；左侧未达到现有输入标定至少 3°的分辨条件。
- 不要求为了通过门槛越过舒适范围。已询问左杆实际是否有明显相对转动、是否只是小幅活动或主体跟随、是否受阻，以确定下一步。
- 用户选择“只抬了一点，或设备主体也跟着动了”。因此小幅读数尚不足以推断传感器异常；已说明腰部主体保持原位，只绕各自关节上抬杆件，在舒适且留有余量的位置重采。

## 第 11 轮：按主体固定要求重采上抬（方向仍未确认）

- 用户确认主体保持原位、杆件分别舒适上抬且设备工作后采集。
- 记录：`telemetry-10s-20260924-093449.json`，1807 帧、180.6 Hz，零坏帧；停用应答已收到、串口已关闭。
- 末 3 秒共 542 帧，左中值 −2.460°、右中值 −17.484°；两侧跨度均 0.012°，最大角速度均 0.5°/s，稳定性通过。
- 保存 `guided-upper-retry-session2.json`，仅作实际位置观察。相对 center 左差 +4.788°、右差 +1.092°，与上一轮“上抬”标签的负向变化不一致，且右侧行程未达到 3°输入分辨条件。
- 当前不能由两轮相反的操作标签推定上抬方向，未合并、未应用游戏行程标定。暂停重复采集，需核对当前实际摆放和关节转动动作。

## 当前摆放照片与测试前提澄清

- 用户提供 `8b0ff142c51b908d68d0f23f2f4373fc.jpg`，画面中腰部主体仍佩戴在身上，杆件伸向前方由手托住，绑带垂下。
- 仅凭该照片不能判定此前每轮姿态，也不能测量机械角度或确认工作模式。照片说明当前应重新核实“离身”前提。
- 已明确“离身”包含取下腰部主体，不只是解绑腿部。保持采集停止及上次已确认的停用状态，等待用户确认整机取下并可靠支撑；若没有合适支撑，则转软件模拟。
- 不会在照片所示穿戴状态下沿用此前离身测试授权发送 ENABLE。该命令同时使能电机，并非纯读数开关。
- 下一轮如恢复测试，优先同一次采集内比较单杆上抬前后的读数，避免只用分开的静止窗口推定方向。

## 第 12 轮：确认整机离身后首次左杆对照（全零）

- 用户明确回复“整机已取下并可靠支撑，可以做左杆观察”。指导前 3 秒静止、随后缓慢上抬左杆、末尾稳住，右杆和主体保持不动。
- 记录：`telemetry-10s-20260924-094048.json`，1837 帧、183.6 Hz，零坏帧；停用确认和串口关闭完整。
- 四项关节持续全零，无有效方向结论。本轮不能据此断言“离身检测”导致数据为零。
- 已停止重复动作，着手增加持续全零提前终止与通信/关节有效性分开的报告。用户随后主动反馈“开机了”。
- 重新查阅仓库 `docs/已领设备-资料对照.md`，确认设备资料为 Hypershell X MaxS、黑客松固件 2.9.99.1，而非普通消费版固件。协议要求先开机进入工作态再发 ENABLE，未给出佩戴状态字段或纯遥测启动命令；普通版手册不能据此替代开发固件状态验证。

## 第 13 轮：开机后读数检查

- 用户明确开机后，仅检查读数，不要求活动杆件。
- 记录：`telemetry-10s-20260924-094225.json`，1815 帧、181.4 Hz；左中值 −49.656°、右中值 −47.232°，非零且稳定，停用和关闭均确认。
- 数据恢复说明本次状态可读取角度，不将本轮额外命名为中位或游戏端点。

## 第 14 轮：同一窗口左杆对照

- 用户确认开机、离身、主体支撑可靠且可只活动左杆后，按前 3 秒静止、缓慢抬左杆、最后稳住的指示采集。
- 记录：`telemetry-10s-20260924-094302.json`，1800 帧、179.9 Hz，零坏帧；已确认停用并关闭串口。
- 全窗左角度跨度 15.120°，右角度跨度 6.252°。左杆明显变化，但非目标右侧也变化，需离线比较首末稳定段后评估，不能仅用全窗跨度宣布方向确认。
- 离线结果见 `single-left-094302.json`：前 3 秒左中值 −39.744°（跨度 0.984°、最大速度 6.2°/s）、右中值 −35.880°（跨度 1.140°、最大速度 7.0°/s）；末 3 秒左中值 −41.364°（跨度 15.120°、最大速度 41.9°/s）、右中值 −32.772°（跨度 3.396°、最大速度 16.1°/s）。
- 首末窗口均未通过稳定性条件，左首末差仅 −1.620°，另一侧全窗跨度超过 2°观察门槛。结果为 `observationSufficient=false`、`direction=null`，没有输出确定上抬方向。
- IMU 首末中位变化 pitch −0.535°、roll +0.1825°，只供参考；板时连续、最大帧间隔 16 ms。
- 已询问用户是上抬未及稳住、往返/提前放回，还是已稳住，以改进动作与采集时段的配合，暂不重复采集。
- 用户确认“上抬一次，但还没稳住就结束了”，不能把本轮不足归因于反复动作或硬件故障。调整下一轮为显式确认的 20 秒窗口，开始留静止余量，中途发送稳住提示，末尾再比较固定 3 秒窗口；准备好新采集器后再询问就绪，不自动延长已批准的轮次。

## 第 15 轮：20 秒左杆对照（隔离改善，终点仍未稳定）

- 用户明确确认 20 秒、开机、整机离身且支撑可靠后，采用 `capture-ten-seconds-guarded-Emma0924.py --seconds 20`。开始提示先静止 5 秒再缓慢抬左杆；约中途明确提示稳住，结束通知可以放松。
- 记录：`telemetry-20s-20260924-094820.json`，3602 帧、180.05 Hz，零坏帧；20 秒完整采集，停用应答和串口关闭均确认，无力矩命令。
- 离线分析：`single-left-094820.json`。首 3 秒稳定，左中值 −81.912°（跨度 0.048°），右中值 −37.740°。末 3 秒左中值 −84.780°、跨度 3.108°、最大速度 13.5°/s，仍不稳定；右杆全窗跨度仅 0.012°。
- 左首末变化 −2.868°、右首末变化 0°。本轮支持左侧独立变化，上抬读数减小为初步趋势；未满足预设终点稳定性及 3°变化门槛，正式结果仍为 `direction=null`，未确认标定。
- 为避免继续受聊天提示时序影响，下一步改为用户先摆稳左杆上抬姿态，再进行 10 秒静态终点观察，不要求边读提示边在窗口内完成动作。

## 第 16 轮：先摆稳后采静态上抬点

- 用户明确“已经摆稳，开始静态采集”，使用 guarded collector 默认 10 秒，仅要求静止。
- 记录：`telemetry-10s-20260924-095042.json`，1810 帧、180.9 Hz，零坏帧，通信和关节有效性标记通过；停用确认、串口关闭完整，无力矩命令。
- 全窗左中值 −93.696°、跨度 0.024°，右中值 −90.012°、跨度 0.012°，两侧最大速度 0.4°/s。
- 保存 `guided-upper-static-095042.json` 作为稳定位置观察。此前明确要求的是左杆上抬，右侧相对上一轮也大幅换位，因此不把右侧静态读数直接当作其上抬终点；已向用户核实两侧是否均在舒适、未触及机械限位的工作终点。
- 单杆对照和游戏端点标定的意义区分：此静态记录不补写成之前动态试验的成功结果，也不跨不同摆放直接认证力矩或方向。
- 用户明确回复“只有左杆，右杆只是放在那里”。记录已标注 `intendedSides=[left]` 和操作说明，只能作为左侧舒适上抬工作终点；右侧不能由该记录赋予任何游戏端点含义。
- 后续逐侧记录两点：该侧舒适游戏起点（center，输入 0%）和舒适上抬工作终点（upper，输入 100%），另一侧位置不参与该侧映射；只输出软件输入观察，不作为控制器物理参数。

## 第 17 轮：左杆游戏起点（全零提前终止）

- 用户确认左杆放低到舒适游戏起点并摆稳后采集。
- 记录：`telemetry-10s-20260924-095244.json`。guarded collector 在四项持续全零约 2 秒、366 个递增有效帧后提前结束；DISABLE 已应答、串口已关闭。
- `communicationPassed=true`，`jointDataUsable=false`，`terminationReason=ambiguous_joint_data`，`passed=false`。没有保存起点观察，也未把全零解释为确定的硬件故障。
- 请求用户正常开机并保持左杆起点，随后连续完成静态记录。若工作状态重置，需要重新核对同轮起点和终点，不将历史观察自动当成持续有效的设备标定。

## 第 18 轮：重新开机后左杆舒适游戏起点

- 操作者确认刚已开机，并按“舒适游戏起点，不用机械最低位置”的指令将左杆摆稳。
- 记录：`telemetry-10s-20260924-095432.json`；离线末 3 秒共 544 帧，左中值 −80.460°、跨度 0.012°、最大角速度 0.4°/s，通过稳定性校验。
- 保存 `lift-left-center-095432.json`，标注 `intendedSides=[left]`、`physicalStopReached=false` 及操作说明。未触及机械极限的标记来自操作者按舒适起点指令确认，不是传感器验证；样本和时间未修改。
- center 仅代表左侧游戏输入 0%，不表示力学中位。右杆只被动观察，不赋予右侧起点或终点含义。
- 本轮刚经历工作状态切换，未与旧 upper 比较或合并；将重新记录同轮左侧舒适上抬终点。

## 第 19 轮：左杆上抬终点重采（稳定，但与起点跨度不足）

- 用户确认左杆舒适上抬终点摆稳、主体原位、设备开机及离身后，静止采集 10 秒。
- 记录：`telemetry-10s-20260924-095528.json`，1815 帧、181.4 Hz，零坏帧；通信与关节有效性通过，DISABLE 应答且串口已关闭。
- 全窗左中值 −78.252°，右中值 −0.300°，稳定。与本次左起点 −80.460°相比约 +2.208°，未达到软件输入观察至少 3°跨度条件，且与此前上抬减小趋势不一致。
- 原样保留本轮稳定观察，不合并成已通过的左侧标定。停止重复举杆采集，需要核对实际连续转动过程与协议输出轴定义；文档未定义角度零点或 ENABLE 是否保持参考坐标，不能靠猜测改符号。

## 产品映射意图（待验证）

- 用户期望中位至上抬工作终点的游戏输入逐渐增大，端点对应满量程；靠近工作终点手感逐渐加重。
- 按照片所示手部构想，优先提供“舒适握持零位→上抬工作终点”的单方向 0–100% 输入；下放回中位使输入减小。无需为了沿用旧 back/forward 三点流程而要求低于中位的动作。既有三点上下合并工具只能用于确实需要双向输入且有对应观测的情况，当前尚未应用。
- 位置输入映射与执行器力反馈须分开。若停住时也需拉力，应另行设计位置相关弹性力；纯阻尼与运动速度有关。
- 当前只采集角度观察，不测力、不下发力矩；尚无可用于实机渐强阻力的工程参数。

## 尚未采集

- 含明确实际动作方向、有效行程的上抬及下放观测（已有稳定上抬标签记录，但行程或方向未满足要求）。
- 多次回到中位的重复性。
