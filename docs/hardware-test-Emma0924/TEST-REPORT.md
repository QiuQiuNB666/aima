> 历史现场记录；当前交付结论以 [README](README.md) 为准。JSON 在 recordings/ 中压缩保存，先按 README 解包。早期 passed 仅表示通信；不是标定或力反馈验收。

# 外骨骼连接测试 · Emma0924

日期：2026-09-24。当前结果：USB 串口、设备查询、开机后静止遥测及手动活动时的两侧读数变化已确认；单侧映射与校准待验证，未做施力测试。

## 下载版本

- GitHub 分支：`emma0924/upper-limb-improvements`
- 远程提交：`8f5a817dab9c576a5aa7046d0fa09888168f6812`
- 相对原本机版本更新 2 个提交、27 个文件，模块版本从 0.4.0 到 0.6.0。
- GitHub 原生 Git 拉取因本机 Schannel / Git 凭据辅助进程限制失败，改用已认证 gh 客户端下载官方 API 差异。
- 完整源码树已校验为 `0407253254d21a9a8a2c03bd9e72cb39467180a0`，与远程提交一致。
- 本地独立测试分支 `codex/hardware-test-Emma0924`，本地同步提交 `397368c`；该提交仅为本地快照，不冒充远程原始提交。原开发分支保留。
- 此次采用手部功能远程分支，没有把 main 上其他团队的后续改动混入现场测试版本。

## 软件验证

- Node 93 项测试通过，含双手输入、页面生命周期和状态桥接。
- Python 35 项手部测试通过，使用假设备并阻止真实串口调用。
- 新版 Node 页面服务已启动：`http://127.0.0.1:4188/hands.html`。
- 已刷新浏览器旧缓存，确认显示左右枪 `0 / 0` 和输入来源选择，新页面未记录 JS error。

## 实际设备结果

- pyserial 枚举确认 `COM6` 为 Silicon Labs CP210x，VID `10C4`、PID `EA60`。
- Windows CIM 枚举在当前环境拒绝访问；控制台 USB 状态接口因此显示 `USB_STATE_UNAVAILABLE`，这不否定串口查询已成功。
- COM6 使用 3000000 baud、8N1；打开前设置 DTR=False / RTS=False。
- 新版只读姿态采集尝试 3 秒：数据不足，没有生成姿态校准结果。
- 独立查询仅发送 `PING`、`VERSION`，实际收到 `PONG` 和 `OK,VERSION,2.9.99.1`。
- 查询期间有效传感器帧 0、坏帧 0；结束后已关闭串口。
- 第一轮查询没有发送 `ENABLE`、`DISABLE` 或力矩 `T` 命令，没有启动腿部控制循环。
- 原始查询报告：`connectivity-20260924-085919.json`。

## 已批准的 10 秒遥测采集

用户明确确认“已离身并支撑好，可以开始采集”后执行。

- 实际命令依次为 `PING`、`VERSION`、`ENABLE`、`DISABLE`；均收到预期应答。
- 采集时长 10.0 秒，有效帧 1820，实测 181.9 Hz。
- 坏帧 0，重复板端时间帧 0，主机最大帧间隔 16 ms，板端时间跨度 9996 ms。
- 左右关节角度、角速度全部为 0。此结果不能证明关节传感器已有效工作，也不能用于三点校准。
- 主板 IMU 和气压数据有更新，例如 pitch 在 18.913° 到 19.124° 之间，ax 在 -0.352g 到 -0.343g 之间。
- 没有发送力矩 `T` 命令，没有启动自动恢复或腿部控制循环。
- 结束收到 `OK,DISABLE`，串口已关闭。该应答表示设备确认停用；没有实际力矩传感器回读。
- 完整数据：`telemetry-10s-20260924-090201.json`。其 `passed` 仅表示本次传输及命令确认通过，不表示关节响应、运动控制或力矩已验收。

## 待继续

用户说明第一轮只有 USB 连接、尚未开机，随后明确回复“已开机”。继续采集 10 秒静止数据，结果如下：

- 完整记录：`telemetry-10s-20260924-090350.json`。
- 有效帧 1804，实测 180.3 Hz；坏帧、重复帧均为 0，最大主机帧间隔 16 ms。
- 左角度中位数 3.936°，范围 3.924° 至 3.948°；右角度中位数 -8.352°，范围 -8.364° 至 -8.328°。
- 左右最大绝对角速度均为 0.6°/s。右侧数据已按 ShellOS 的约定归一化。
- 查询、使能及停用应答均收到；结束已收到 `OK,DISABLE` 并关闭串口，未发送力矩指令。
- 这证明开机后关节读数已出现且静止时波动较小；尚不能据此证明动作方向、左右映射或工程限位。

用户回复“准备好了，开始手动验证”后，提示先轻动左杆、再轻动右杆，完成第三次 10 秒采集：

- 完整记录：`telemetry-10s-20260924-090449.json`。
- 有效帧 1801、180.0 Hz；坏帧 0、重复板端时间 0，最大主机间隔 16 ms。
- 左角度 -4.644° 至 6.540°，跨度 11.184°；右角度 -86.412° 至 -78.024°，跨度 8.388°。
- 结束收到 `OK,DISABLE` 并关闭串口；只发 PING / VERSION / ENABLE / DISABLE，没有任何力矩命令。
- 左右两路均有变化，但多个时间段都有两路同时变化，未使用物理动作标记，不能把这次记录当成左右单侧隔离、方向或独立通道验证。
- 右侧采集起点已约 -85.7°，与上一轮静止姿态不同；这不是标定后的中位，也不能推导机械行程。

同时发现 1801 帧只有 641 个不同的主机接收时间，而板端时间持续递增。Windows 时间分辨率会导致连续多个帧具有相同的主机时刻；原 PoseRecorder 的严格递增检查会误拒这些正常样本。现已修复为仅拒绝真正的主机时间倒退，保留板端倒退拒绝和重复帧过滤。

- 旧版离线回放第 2 帧就失败，修复后全部 1801 帧成功导入，641 个原始主机时刻及板端时间未改变。
- 手动活动记录正确标记 stable:false，没有被当作有效静止校准。
- 新增 5 个回归用例，手部 Python 测试共 40/40 通过；回放与测试禁用真实串口访问。
- 代码修复仅涉及 `shellos/hands/capture.py` 和 `tests/test_hands_haptics.py`，另在模块 VALIDATION.md 保存现场结果。

本轮结束状态：设备已确认 DISABLE，串口关闭。网页服务继续运行，但没有启动常驻硬件采集，也没有把已保存的文件当作实时数据。三点校准、单侧方向映射、网页实时绑定与真实力反馈仍待后续工作。

ShellOS 8765 尚未启动；手部遥测 8766 尚未配置。当前浏览器页面不会接收实时 COM6 角度，也不会让电机自动抬杆或产生阻尼。连接成功不能代替角度校准或实际力反馈验收。
