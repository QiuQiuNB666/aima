# 指挥板 · 9/23 夜 6 条并行线

> 开工先读 `docs/架构-v3-完整版.md`（第 2 节规格、第 3 节分工与验收）。只改自己名下的文件；要动别人的写到「给别的线的留言」。不 push，总指挥统一提交。结果不理想照实写。
> 今晚没有硬件：用 `--sim`（模拟，空格走）或 `--replay data/recordings/*.csv`（9/22 真机录制）。

## 各线状态（自己更新自己这一节）

### G 登山游戏 ★（`static/game.html`、`static/game/`、`static/vendor/` 新增、`server.py` 的 `/game` 路由）
**做了**（引擎；各世界精细地图留给下一轮）
- `game.html`（HUD DOM + importmap）+ `game/`：`engine.js`（轮询 /state 10 Hz、60 fps 插值、预览模式）· `path.js`（路线→几何：坡=斜面带，台阶=InstancedMesh 方块成阶，每步刻度 + 路沿，红灯段=停止线 + 斑马线对面的信号灯，起点营地、山顶旗；高度规则与 terrain.py 的 RISE 一致）· `avatar.js`（CesiumMan，body3d 同一套骨骼映射：`leg_joint_*_1` 髋 / `_2` 膝，flex = −frame.l；另加手臂放下 + 与腿反向摆）· `ghost.js`（整数步→连续位置的平滑器，化身和影子共用；影子合成步态）· `camera.js`（侧后跟随，上坡抬、台阶拉近，登顶环绕一圈）· `hud.js`（海拔、第 n/N 步、路段 + 类型色块、「n 步后：…」预告、左右腿力矩条（上升沿过 0.25 Nm 闪）、用时/最佳/登顶次数/穿戴者、安全灯、红灯站定倒计时、登顶卡片「已写入山的记忆」、经验卡新卡/命中滑入）· `fx.js`（脉冲 → 脚下波纹 + 当前那一步路面发亮；登顶彩带、旗子飘）· `input.js`（模拟：按住空格走、1/2/3 = 80/105/130 步/分；R = /demo/reset）
- 主题插件 `game/themes/<style>.js`：`build(scene, ctx)` / `update(dt, st)`，5 个基础版（cyber_night 夜空+紫雾+楼群剪影+霓虹条；dawn_mountain / subtropical 山体高度场 + 远山剪影；night_to_dawn 随进度从星空过渡到日出；grid 干净网格）。公用工具在 `themes/kit.js`（天空穹顶、雾、沿路高度场、远山、网格贴图、噪声；ctx / st 字段写在文件头）。**下一轮加道具/灯光/粒子只改 `themes/<style>.js`**
- 离线预览：`/game?preview=<world_id>&pos=<n>&ghost=<n>[&who=名字][&summit=1]`，不轮询，用 /worlds.json 摆静态画面；`window.__ready` 渲染完为真；调试 `window.__game`、`window.__camHold=true` 可手动摆镜头
- 降画质开关 `/game?aa=0`（关抗锯齿）；非 terrain 控制律时显示横幅 + 一键切回；/terrain 换世界时游戏页自动刷新
- `server.py` 只改一行：静态前缀加 `/game/`
- 化身前进节奏学的是「pos 实际多久涨一次」而不是步频（按 A 线留言：真机只接住约一半的步），停顿时站在原地、腿照真实髋角动

**验收**
- `--sim`（8801）+ 驱动脚本自动走（遇红灯站 2.5 s）：连续 5 圈登顶；第二圈起影子出现且逐步前进（ghost_pos 1→8→14→20→26→32，与 pos 并行），头顶「上一位：anon 并排/领先 2 步」；红灯站定 2 s 放行；登顶卡片、彩带、环绕镜头；R 回 pos 0；空格走/松停、3 → 130 步/分；切 /terrain 泰山页面自动换世界；切 transparent 出横幅
- 截图 `/tmp/shellos-shots/G/`：4 个世界各 `-pos0.png`、`-mid.png`（tokyo 18 / taishan 20 / fuji 20 / wutong 17），`taishan_18pan-summit.png`（登顶），`sim-live.png`（模拟运行中、天桥台阶、影子领先 2 步）
- **FPS**（这台开发机 i5-7400 + 核显，Claude 浏览器面板模拟 1920×1080，canvas 1920×1080、pixelRatio 1、无阴影）：抗锯齿开 50–58 fps（10 次采样 54/58/54/53/54/52/50/53/53/54）；`?aa=0` 54–58。MacBook M2 Pro 没测
- **loop_ms**（/state 里读，同样 60 s 自动走、每 0.2 s 采一次）：不开游戏 中位 14 / p95 16 ms（两次一样）；开游戏 1080p 中位 15 / p95 21–22 ms；小窗口 中位 15 / p95 18–19。**中位 +1 ms 达标；p95 +5–6 ms 正好在线上**，这台机器同时还跑着别的线的进程，结果偏悲观
- pytest 23 过（我没加 Python 代码）

**没做完 / 不理想**
- ⚠ **无头截图（scripts/shot.sh，swiftshader 软件渲染）吃约 200% CPU，连拍 8 张期间主循环卡了 1.5 s → 看门狗 DISARMED**（安全层工作正常）。展位机/穿戴时千万别跑 shot.sh；真 GPU 浏览器里没复现（最大 69–99 ms）
- 各世界只有基础版场景（天空/雾/地面/远景），道具、雨、灯光、云海都没做——下一轮精加工
- 化身真实髋角只在模拟数据上看过；9/22 录制没跑回放验证；明天上真机第 9 条要看「化身跟腿」
- 富士 Z 字坡拐角路面有折痕；东京台阶在暗色主题下偏暗（路面色取自 world.theme.path）
- 登顶时化身在山顶停 6 s，这段时间如果继续走，pos 已经从 0 往前，6 s 后化身直接跳到当前位置

### A 步态鲁棒（`shellos/gait/`、`scripts/gait_audit.py`、`tests/test_gait.py`）
**做了**（报告 `docs/报告/步态审计.md`）
- `scripts/gait_audit.py`：12 段录制 × 1080 档门限网格（周期区间 / 整周期最低 conf / 活动度 / 两腿同相），每段接受/拒绝、坐站假周期、覆盖率、步频中位 P10/P90；走动中保持时长；原地踏步模拟；`--hs` 估 HS_PHASE；`--rec` 直接跑明天的新录制
- 发现 9/23 前门限**穿戴坐站假周期不是 0，是 13 个**（174946 第 85–100 s，两腿同相）；走动段只接住 25% 的步
- `estimator.py` 改默认（证据见报告 §2）：半周期锁存（去一步切两段）+ 两腿同相检查（sync_tol 0.2）+ 去掉整周期最低 conf + 周期 0.8–2.2 s。接口不变，全部门限可用关键字覆盖；`latch=False` 回旧规则
- 踏步模式 `GaitEstimator(stepping=True)`（rom≥8、r_ref=4），默认关；`recent_s` 选项（走动中要求最近接受过周期），默认关
- `tests/test_gait.py` +4：锁存、同相/单腿拒、踏步模式、审计脚本自检（参考标签 + HS 检测找回已知相位）

**验收**：穿戴坐站假周期 13 → **0**（网格相邻档也 0）；181327 走动段覆盖率 0.25 → **0.44**；步频中位 108（P10–P90 77–125）；模拟 60/90/110/140 步/分估准；`--sim` 全栈 8802 走/停/变速正常；全套 pytest 23 过；系统 Python 3.9.6 跑通

**没做完 / 不理想**
- HS_PHASE **证据不够，保持 0.5**：唯一够长的一段像原地踏步（支撑平台占比 0.44），az 冲击左右差 0.14。明天要 3 人向前走各 ≥30 s 再跑 `--hs`
- 只有球球 1 个人、约 170 s 像样的走动；覆盖率 0.44 = 地形前进约是实际步数的一半弱
- 真人踏步时「走动中」只有 44% 的时间成立（平台期 conf<0.5，也就是 Guard 的门），脉冲约一半时间给不出来——安全语义，没动
- 桌上（没穿、手摆）假周期 15 个，原理上分不出来，靠死人开关 + 穿好再开始

### B 地形手感（`shellos/control/terrain.py`、`scripts/afc.py`、`scripts/pulse_plot.py`）
**做了**（报告 `docs/报告/地形手感.md`，图 `docs/报告/脉冲/*.svg`）
- `terrain.py`：抽出纯函数 `pulse(kind, phase)`；参数范围收紧（`t_push/t_brake/t_step` 0–20、`width` 10–20，原来 `t_push` 能被推进摆动期）；输出自裁 ±3 Nm（上台阶 1.2×3=3.6 以前全靠 Guard）；红灯一拍多步也停在路口前、站定计时要求"没在出步"。接口 / `status()` 字段不变
- `scripts/pulse_plot.py`（纯标准库 SVG）：5 路段左右腿一周期力矩图 + 核对（梯形 / 底宽 10–20% / 30–60% 与 60–90% 无伸展 / 无方向突变 / ≤软限）+ 27 组参数边界扫描，不过退出码 1
- `scripts/strength_ladder.py`：0.5→1.5→3 Nm 峰值三档（按路段倍率换算 strength、超软限自动压），每档强制路段 N 步，选完 `/set` + `/mark`；`--dry` 对模拟器自动跑
- `scripts/afc.py`：`--api`、`--dry`（理想观察者读实发力矩作答）、`--seed`、Ctrl-C 还原、每轮和总结果写 `/mark`、补 `from __future__`
- `tests/test_terrain.py` 8 条

**验收**：5 张图全部核对通过、边界扫描 0 组不过；`--sim` 8803 红灯：走着 30+ 步不前进且被 −0.75 Nm 轻拉 → 停 → 2.0 s 放行（pos 4→7）→ 继续计步；强度阶梯干跑实测峰值 0.50/1.50/3.00；AFC 干跑 slope 6/6、strength 4/4；Guard 斜率限下强度 3 快走脉冲重心滞后 ≤2.2%（JND 2.8%）；全套 pytest 31 过；系统 Python 3.9.6 跑通脚本和 8 条测试

**没做完 / 不理想**
- 全部是模拟，手感一条没验证；HS_PHASE 仍 0.5、正=伸展仍是推断
- 强度阶梯结果没进经验卡库（`/feedback` 只能 ±0.5 一句话），只是 `/set` + `/mark`
- 回放 181327 起不来（录制里有粘行），见给 F 的留言
- 「摆动中段 30–60%」按文献约定其实是支撑后期；两种口径（30–60、60–90）都查了，都干净

### D 仪表盘打磨（`static/index.html`、`body3d.js`）
**做了**
- 暗场：底色 #06080b，正文 17 px、状态 28 px、数值 24–30 px；次要文字 #a9b6c5（在面板底上约 8:1）
- 地形面板挪到左列最上面：世界名 + 副标题、当前路段 label + 类型色块、前方（label · 类型 · N 步后，到头显示山顶名）、海拔（world.unit）、进度 pos/total · 第几圈 · 本圈用时 / 最佳 / 上圈、影子（并排 / 领先 N 步 / 落后 N 步 · 谁 · 第几步）；红灯段提示「站定 2 s 放行 · 已站 x s」；有 force 时显示「强制 xx」
- 剖面：阶梯轮廓 + 路段色带（已走过的变暗）+ 路段名；**力矩 |sent|>0.05 Nm 时当前步整列亮黄、化身外圈脉动、面板描黄边、标签「脉冲 x.x」**，亮 0.45 s（脉冲太短，10 Hz 轮询只采到一两帧）；本圈吃到脉冲的步在路面上方留 ▼；影子是紫点
- 经验卡：只在内容变化时重建（原来每 100 ms 重建，动画没法做）；新卡滑入 + 黄光，命中数上涨或新被套用也滑入发光，并弹顶部提示「新经验卡 #n」；套用中的卡绿框 +「已套用」
- 顶栏「演示态 / 累积态」：点演示态 = POST /demo/reset + 提示（每次点都复位一次）；累积态 = 不复位只提示；选择记在本机浏览器。原来记忆面板的「演示复位」按钮并进这里
- 模拟模式（/state 里 `sim.on`）才出现「按住走路」按钮（按下 /sim walk:true，松开/移出/失焦 walk:false）+ 步频滑条 60–140 + 「一直走」锁定
- 「打开游戏屏 ↗」按钮：新窗口 /game
- 手机竖屏（≤900 px）：单列、16 px 边距，**安全面板排第一**（急停不用往下翻），地形第二；390/375 宽无横向滚动
- 去掉了眼镜面板和手柄表里的 □ 眼镜行（眼镜已砍）；手柄表补了 Options 演示复位、左右摇杆 puppet
- 卡片引语、事件、参数名都转义后再进 innerHTML（评委说的话是用户输入）
- `body3d.js` 只改一行：pixelRatio 上限 1.5，少吃点 GPU（控制循环和游戏屏同机）

**验收**（--sim 8804 + 驱动脚本自动走、红灯站 2.6 s）
- 截图 `/tmp/shellos-shots/D/dashboard-1440x900.png`、`dashboard-390x844.png`（390 宽是 iframe 拍再裁的：无头 Chrome 窗口最窄约 500 px）
- 浏览器面板里用脚本点过：按住走路 → sim.walk true、松开 → false；一直走 / 停下正常；/feedback「太陡了」→ 新卡 class=new + 提示；走 6 步检索套用后卡片 class=hit；累积态不发请求；演示态 → 事件流出现「演示复位」
- 爬台阶时 15 s 内 83/150 次采样处于「脉冲」高亮；375 宽 scrollWidth = 375；控制台 0 报错
- pytest 31 过。测试时往 `data/experiences.jsonl` 加的 #4–#6 已 `git checkout` 还原

**没做完 / 不理想**
- 两张截图里都没拍到脉冲高亮：无头 Chrome 启动要好几秒，拍到时往往已经走出台阶。高亮本身在浏览器面板里看过（黄框 + 列高亮 + 外圈）
- 截图里的曲线只有左边一小段：无头模式的虚拟时间下只轮询了十几次，真浏览器里是满的
- 「红灯站定 2 s」里的 2 是写死的（terrain.py 的 WAIT_STILL_S），那边改了这里要跟着改
- 演示态 / 累积态只是前端的操作习惯，后端没有「模式」：累积态下换人照样会复位参数（`set_wearer` 本来就这样）

### E 提交物（`README.md`、`docs/提交/`）
**做了**
- `docs/提交/项目介绍.md`（官网提交用）：定位句 · 做了什么 · 为什么（痛点 / 机会 / 与出题方方向） · 三层新在哪 + Holotron 对比表 · 架构图 · 地形脉冲表 · 步态 · 记忆与 Agent（对评分表） · 安全表 · 验证计划与已有数据（每个数标真机 / 回放 / 模拟 / 待真机验证） · 演示截图 4 张 · 团队（只写球球） · 复现 · 参考文献
- `海报文案.md`：主海报（标题「山的记忆」+ 一句话 + 三行卖点 + 二维码位）、A5 三步自助桌卡、备用短句、不上海报的字
- `60秒演示脚本.md`：模拟版（A，带水印）/ 真机版（B）分镜 + 录制准备命令 + 口径检查
- `4分钟展位脚本.md`：对齐架构 §5 的 6 段分镜（操作 / 旁白 / 拿哪一分）、开场前 30 秒复位动作、砍的顺序、节奏估算、现场出问题预案、禁用说法、9/24 上午回填表
- `评委题库.md`：Seeed 硬件创业者 / Hypershell 工程师 / 跨赛道终审各 15 问 + 标准答，每条标出处【】，待真机验证的照标；底线口径表
- `README.md` 重写：一句话 · 截图 · 快速开始（模拟 / 回放 / 真机）· 架构 · 安全 · 数据 · 目录 · 提交物链接；去掉眼镜 / EvoMap / 手机

**验收**：5 份提交物 + README 成稿；文中每个数字对过来源（架构 v3、A/B/G 报告、调研原文、代码）。README 里的模拟 / 回放命令在 8805 上实跑过（回放用 174946，起得来）；`/feedback`「太陡了」→ 1.5→1.0、删卡回退 → 在 8805 上实测过；跑完已还原 `data/experiences.jsonl`、kill 进程。pytest 31 过（我没改代码）

**没做完 / 不理想**
- 截图用的是 G / B / D 线 9/23 夜的模拟截图，最终版要等 G 精加工后重拍；60 秒视频没录（要在不跑 shot.sh 的机器上录）
- 所有「待真机验证」的数字（力矩方向、缺省强度、2AFC x/10、穿戴用时、一圈用时）要 9/24 上午回填，位置列在 4 分钟脚本末尾
- 实测发现 3 个会影响演示叙事的代码问题（见下方 E 的留言），脚本里先写了绕法

### F 稳定性压测（`replay.py` 故障注入、`tests/test_fault.py`、`guard.py` 仅修 bug）
**做了**（报告 `docs/报告/稳定性.md`）
- `replay.py`：故障注入 `ReplayLink(faults=...)` 或环境变量 `SHELLOS_FAULTS="gap@10:3,noten@20,reboot@30:0.5,bad@40:0.5,stall@50:0.3"`（断流 / ERR,NOT_ENABLED / 开机日志=设备复位 / 帧格式错误 / 主机卡顿）；补上和 SerialLink 同名的 `enabled / reboots / needs_recovery / recover`，加了设备侧模型（未使能时拒绝 T、固件 100 ms 清零）；**CSV 坏行跳过并计数，181327 能回放了**
- `guard.py` 修了 4 个 bug（都是收紧）：① Lock → RLock，**Ctrl-C / SIGTERM 约 1/12 概率死锁、DISABLE 发不出去**（改后 30 次 0 次卡死）② `arm()` 在 DISARMED / 急停时拒绝，只能走 `rearm()` ③ 断流立刻 T,0,0，不再走斜率限慢慢降 ④ 看门狗清零也写进录制
- `tests/test_fault.py` 13 条

**验收**
- 故障矩阵（11 种 × 3 次）：**10 种故障结束后 ≤0.09 s 就恢复**；断流 / 坏帧时 Guard 在 0.20 s 归零（固件模型 0.10 s）；恢复后力矩从 0 按斜率限爬；卡顿 1.2 s → 看门狗 DISARMED，设备复位 + 恢复逻辑都拉不回来，只能 rearm；急停同样
- ❌ **设备复位且静默超过 1 s：恢复最坏 2.02 s**（main.py 的 2 s 重试节拍），见给整合的留言，改一行后 ≤0.04 s
- 12 段录制全栈各 60 s（terrain、--force-deadman）：**0 异常退出、0 DISARM、0 Traceback**；loop_ms 中位 14，p95 15–17，最大 238（复现不出来，空循环探针最大 20 ms，判断是别的线抢 CPU）
- 游戏页开着（回放 8806）：中位 +0–2 ms ✅；**p95 +6–9 ms，超过 +5 ms 的线**；无头 swiftshader 1080p 30 s 最大 33 ms，真 GPU 浏览器面板 60 fps 最大 27–131 ms；shot.sh 连拍 3 张最大 78 ms；全程 0 DISARM
- pytest 43 过；系统 Python 3.9.6 跑 test_fault + test_guard 22 条过

**没做完 / 不理想**
- 全是模拟故障，真机没测过；每段录制只跑了开头 60 s（大多是坐着 / 站着）
- `--replay` 模式下 main.py 跳过恢复逻辑，所以全栈回放里掉使能以后一直不恢复（真机不受影响）。用 exec 打补丁验证过，去掉这个条件就能恢复
- A 的踏步模式没接进 main，没测
- 压测脚本在 scratchpad `F/`，没进仓库

### 整合（9/23 合并，端口 8810）
**做了**（改的 core 文件：`main.py`、`server.py`、`serial_link.py`、`recorder.py`、`agent/interpret.py`；跨线顺手修：`terrain.py`、`hud.js`、`tests/test_fault.py`、`tests/test_terrain.py`、`strength_ladder.py`、README / 展位脚本的「已知问题」）
- 🐞 **新发现并修了**：`Terrain.reset()` 把步数基准清成 0，而步态估计器还在累计（几百步）→ **走过以后从 /worlds 换世界、或从 puppet 切回 terrain，下一拍就把几百步算成好几圈**：0.0 s 登顶、best=0、一条假影子。改成复位后以步态当前计数为基准（`_strides=None`）。代价：冷启动第 1 步不计（`test_terrain` 的 force 测试起点 1→0）
- A 的留言：`--stepping` 接进入口（`App(stepping=)`，`__init__` 和 `demo_reset` 两处 GaitEstimator 都带上）；README 真机命令加一行
- E 的留言：① `demo_reset` 不再沿用 `old.params`，换人 / 演示复位强度回缺省，影子 / 最佳照旧搬运 ② `set_ctl`：切离 terrain 时暂存实例，切回沿用（影子、最佳、世界都在，回山脚）；`set_terrain` 同一个世界不再 `set_preset`（影子不清），换到别的世界照旧清影子 ③ `interpret.py`：同一参数只取第一条命中的规则、否定说法排前面、差值为 0 不存卡（`App.feedback` 也挡）；另加「轻一点 / 重一点」按要求理解（原来「轻一点」会 +0.5，「重一点」会 −0.5，展位上评委最可能说的就是这两句）
- F 的留言：① 恢复块抽成 `main.recover_tick()`，去掉 `not a.replay`，检测到复位立即重试；`guard.arm()` 返回 False 时日志写失败；`test_fault.drive()` 改成直接调它 ② `serial_link`：`_wlock` → RLock，`reboots` 只在 `enabled` 时 +1 ③ `recorder`：加锁，torque 行 `",,"*14` → `","*15`（现在 frame / torque 行都是 19 列）④ F→G：HUD 里 ACTIVE 但 `reason≠ok`（低置信 / 断流）显示黄灯「归零」，不再写「有力」
- B 的留言：`POST /memory/add {"delta":{...},"quote":...,"source":"ladder"}` → `App.add_exp()`（写卡 + 记为已套用，差值 0 不写）；`strength_ladder.py` 末尾改为写一张相对缺省 strength 的卡（换人后参数回缺省，走 6 步检索命中这张卡再套用）
- `tests/test_app.py` 5 条：换人参数回缺省且影子保留 / 切走再切回 terrain / 差值 0 不存卡 + 轻一点 / recover_tick 复位立即重试且 DISARMED 不动 / 走过后换世界不出假圈

**验收**
- pytest **48 passed**；改过的 9 个 .py 用系统 Python 3.9.6 `ast.parse(feature_version=(3,9))` 全过
- `--sim` 8810，130 步/分自动走、红灯站 2.5 s：**tokyo_night 3 圈**（每圈 21.2 s；3 次红灯都是走着 3 s pos 停在 4 不动 → 站定约 2.5 s 放行 4→7；第 2 圈起影子 ghost_pos 与 pos 同步 0→4→13→24→36）；**taishan_18pan 3 圈**（17.5 s/圈，影子 0→9→19→29→38）。loop_ms 中位 14 / p95 15–17 / 最大 21–22；全程 ACTIVE
- 修 bug 前第一次跑泰山：换世界那一拍直接 laps=3、用时 0.0——就是上面那个 bug，修后复跑正常
- 实跑 API：puppet → terrain 后 best/影子/泰山都在；「太陡了」1.5→1.0 → 换人 judgeB → 1.5；「不明显」+0.5；`/memory/add` 写卡 #6
- `/`、`/game`、`/game/engine.js`、`/worlds` 都 200；截图 `/tmp/shellos-shots/INT/game-live.png`（红灯段、走着不前进）、`game-ghost.png`（第 2 圈，影子「上一位：judgeB 并排」+ 命中经验卡 #5 滑入）、`dashboard-live.png`
- 13 个录制文件（12 段真机 + synthetic-walk）各回放 30 s（terrain、--force-deadman、4 个并行）：**全部活满 30 s、SIGTERM 退出码 0、0 Traceback、0 DISARMED**；181327 `bad=1` 正常跳过
- 故障注入回放（181327，`reboot@5:1.5,noten@12,gap@16:2`）：检测到复位立刻「重新 ENABLE 成功」，回放模式现在也会恢复
- 新录制格式核对：`--sim` 录 8 s，2206 frame + 1094 torque 行全部 19 列、0 粘行，ReplayLink 读回 bad=0（测试录制已删）
- `data/experiences.jsonl` 已还原（测试卡 #4–#6 没进提交）；8810 进程已 kill

**没做 / 不理想**
- 录制器 `close()` 后设备线程再写会抛异常（退出时一瞬间），没改：进程本来就在退出
- 截图只各拍了一张（shot.sh 吃 CPU，sim 下 loop_ms 在截图期间到 42 ms，没 DISARM）
- `interpret.py` 的规则表仍然很粗：「明显」单独出现算「太强」-0.5；大模型没配，全走规则
- D 的可选项（status 带 `wait_still_s`）没做：前端写死的 2 s 与 `WAIT_STILL_S` 一致，YAGNI

## 给别的线的留言

> 9/23 合并：A / B / D / E / F 写给整合的留言、F→G 的 HUD 那条都已处理，见上面「整合」节。


- **A → 整合/总指挥（main.py）**：踏步模式还没接到入口。建议 `main.py` 加 `--stepping` 参数，`App.__init__` 和 `demo_reset` 里 `GaitEstimator(stepping=args.stepping)`（两处都 new 了 GaitEstimator）。不接的话，展位原地踏步 ±6° 左右会出现「腿上一直有脉冲、化身不前进」（报告 §5.1）
- **A → B（terrain.py）**：① HS_PHASE 证据不够，别改 0.5；明天用 `gait_audit.py --hs --rec <新录制>`。② 地形前进用的是被接受的周期，默认档下真人约 44% 的步被接受，路线步数 / 游戏节奏按「实际步数的一半」估。③ `gait.moving` 在站着晃时也会成立（非走路段 250 s）；如果想只在真走时给脉冲，可以 `GaitEstimator(recent_s=1.5)`，代价是起步多等 2 s、走路时脉冲时间减半——我不建议今晚开
- **A → F / Guard**：Guard 没动。但踏步模式把置信度参考半径 8 → 4，效果上 conf<0.5 归零门更容易过（非走路误判走动中 162 → 295 s）。压测时如果开踏步模式请一起测
- **A → G（游戏）**：模拟模式下每一步都会被接受；真机上真人的步只接住约 44%，`pos` 会一顿一顿地前进。化身插值别假设固定节奏（比如「每 0.5 s 必来一步」），停顿时原地踏步动画就好

- **G → F（压测）**：游戏开着时 loop_ms 我的数字见 G 节；另外 `scripts/shot.sh` 无头 swiftshader 截图会把主循环卡到 1.5 s（看门狗 DISARMED），压测时别把截图和 loop_ms 测量混在一起跑；展位机上别跑截图
- **G → B（terrain.py）**：游戏读 `status()` 的这些字段：`preset, world, pos, total, laps, segment, label, next{label,kind,in}, wait_still, altitude, force, segments, profile[].label, elapsed, best, last_lap, ghost_pos, ghost_who`，改名请留言
- **G → 总指挥**：`server.py` 我只改了一行（静态前缀加 `/game/`）。测经验卡滑入时我往 `data/experiences.jsonl` 加过一张测试卡 #4，已用 `git checkout` 还原——各线跑 /feedback 都会写这个文件，合并前看一眼
- **G → 下一轮地图精加工**：只改 `static/game/themes/<style>.js`；接口和可用工具见 `themes/kit.js` 文件头；看效果用 `/game?preview=<world>&pos=<n>&ghost=<n>`（`&summit=1` 看山顶）

- **B → F（replay.py）**：`data/recordings/0922-181327-qiuqiu.csv` 有 3 行两帧粘连（如 `-0.30frame,...`），`ReplayLink.__init__` 里 `float()` 抛 ValueError，`--replay` 这一段整个进程起不来——而它是唯一像样的走动段。建议解析时坏行跳过并计数（`n_bad`）
- **B → 整合（server.py / memory）**：强度阶梯想把"评委选的档"写成真正的经验卡，需要一个直接写卡的接口，例如 `POST /memory/add {"delta":{"strength":x},"quote":"强度阶梯选 1.5 Nm"}` → `store.add(wearer, ctl_key, {"cadence":…}, delta, quote, 1.0, "ladder")`。有了我把 `strength_ladder.py` 末尾换成它
- **B → G（游戏）**：`status()` 字段没变。红灯时 `wait_still` 是站定秒数（满 2.0 放行，放行时 pos 一次跳过整段红灯 3 步）；`force` 非空时是强度阶梯 / 2AFC 在强制路段，画面可以提示"训练中"。腿上能分出来的只有"台阶为主"和"坡为主"两类，泰山和梧桐手感几乎一样
- **B → A**：红灯放行现在要求 `not moving` 且这一拍没接受新周期；站着晃 `moving` 为真会一直不放行（回放 175524 里 40 s 0 步但 moving 多数为真）

- **D → 整合**：`server.py` 不用改，仪表盘要的字段 /state 里都有（sim、terrain.world/label/next/altitude/ghost_pos/ghost_who、safety.sent）。注意 `server.py` 启动时就把 index.html 读进内存，改完页面要重启进程才看得到。可选：terrain.status() 带上 `wait_still_s`（现在前端写死 2 s）
- **D → B（terrain.py）**：仪表盘也读 G 列的那些 status 字段，外加 `profile[].h / kind / label`、`force`、`wait_still`，改名请留言

- **E → 整合（main.py）⚠ 影响展位闭环叙事**：`demo_reset()` 里 `self.ctl.params = old.params` 让 terrain 换人 / 演示复位后**强度不回缺省**，而仪表盘 toast 和事件流都说「参数回缺省」。8805 实测：评委 A 说「太陡了」1.5→1.0 → 换人 → 走 6 步命中这张卡 → **0.5**（叠了两次）；删卡后回到 1.0 而不是 1.5。建议：换人时参数回缺省（B 的强度阶梯要"保留选档"，可以改成把选的档写进缺省值或写成经验卡，而不是整体沿用 params）
- **E → 整合（main.py set_ctl / set_terrain）⚠**：从 puppet（或任何非 terrain）切回 terrain 时 `CTLS[name]()` / `Terrain(preset)` 新建实例，**影子、最佳用时、世界都丢**。展位脚本 0:00 共驾开场 → 0:40 切回 terrain 正好踩到。建议每种控制律的实例缓存起来复用，或切回 terrain 时沿用 demo_reset 里那段影子搬运
- **E → 整合（agent/interpret.py 规则表）**：「不明显」同时命中 `明显`（−0.5）和 `不明显`（+0.5），差值 0，还存了一张空卡（8805 实测卡 #5 `{'strength': 0.0}`）。「再来重一点」同理。建议否定词规则先匹配、命中后跳过同参数的其它规则，并且差值为 0 不存卡
- **E → 整合**：puppet「单段 ≤10 s」代码里没有，只是操作规程；文档里已照实写成「操作规程」。要强制的话在 `Puppet.step` 里加计时

- **F → 整合（main.py 恢复块）⚠**：① 去掉 `not a.replay and`（回放现在有 `needs_recovery/recover`，不去掉的话故障注入测不到恢复）② 设备复位后立刻重新 ENABLE：条件改成 `(now - last_recover > 2.0 or link.reboots != seen_reboots) and ...`，复位且静默 >1 s 时最坏恢复时间 2.02 s → 0.04 s ③ 最好把恢复块抽成函数，`tests/test_fault.py` 的 `drive()` 现在是照抄的，抽出来以后直接调。④ `guard.arm()` 现在有返回值，DISARMED 时返回 False，不会再把 DISARMED 改回来；「重新 ENABLE 成功」的日志可以看一下这个返回值
- **F → 整合（serial_link.py）**：① `reboots` 每行开机日志都 +1（`[initImpl]` 和 `Total PSRAM` 各算一次），一次复位会数成 ≥2 次；建议只在 `self.enabled` 为真时 +1（replay 就是这么做的）② `_wlock` 改成 `threading.RLock()`：信号处理函数里的 `disable()` 可能打断正在 `send()` 的主线程，和 Guard 那个死锁是同一类
- **F → 整合（recorder.py）⚠ 录制会坏**：`frame()`（设备线程）和 `torque()`（控制线程 + 看门狗线程）写同一个文件句柄却没有加锁 → 181327 的粘行就是这么来的，明天的真机录制也会有。加一把 `threading.Lock` 包住两个 write。另外 torque 行 `",," * 14` 多了 13 列，应该是 `"," * 15`，tl/tr 现在落在错误的列（目前没有人读）
- **F → G**：低置信归零时 HUD 显示「ACTIVE · 有力 low confidence」，其实腿上是 0 Nm。可以在 `reason != ok` 的时候不写「有力」

## 总指挥
- 已就绪：模拟器 `--sim` + `/sim`；地形每圈计时与影子（`terrain.status()` 的 `segments / elapsed / best / ghost_pos / ghost_who`）；`/game` 路由已占位（页面由 G 线写）
- 真机已确认 / 待验证事项见架构 v3 第 2.1、2.3、2.8 节
