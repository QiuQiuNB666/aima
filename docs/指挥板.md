# 指挥板

## 9/23 白天 · 峰哥亡命天涯 · 5 条并行线（总指挥 = 主对话框）

> 游戏正式名「峰哥亡命天涯」。新东西：Claude 大脑 + 蜂群 + 一句话造山 + 峰哥解说 / 峰哥头（见 `docs/架构-v3-完整版.md` 末节「大脑与蜂群」、`docs/队友跟进.md`）。
> **规矩**：每条线在自己的 worktree 分支上干，只改自己名下的文件；要动别人的文件写到本页「留言」。在分支上 commit，**不 push、不合 main**，总指挥统一 merge + push。
> 做完在本页自己那一行改状态 + 写验收证据（命令、截图路径、数字）。结果不理想照实写。
> 铁律：`Guard` 是唯一写串口的地方；安全员是硬代码，不交给大模型；代码保持 Python 3.9 兼容、ShellOS 零新依赖（大脑 `brain/` 例外，独立 venv）；测试不许打真 Claude（`tests/conftest.py`）。

| 线 | 名下文件 | 任务 | 状态 |
|---|---|---|---|
| **M 地图精加工**（后台 workflow） | `static/game.html`、`static/game/**`（除 `fengge.js`） | 5 类主题精加工，每轮提交 | 进行中（第 2/6 轮） |
| **H 峰哥化身** | `static/game/fengge.js`、`static/game/fengge_hud.js`（新）、`static/fengge.html`、`static/models/fengge_*`；`engine.js` 只加 import + 调用（第 1 轮 `await dressFengge(av)`、第 2 轮 `initFenggeHud()`，共 4 行） | ① 修 `/fengge` 预览空白 → 脸朝向 / 比例 / 夜景可读性截图验收 → 接进游戏；② 画中画头像 + 解说气泡 | ✅ **第 2 轮 画中画**（`fengge_hud.js`）：左侧世界名面板下方一个圆形峰哥头像（2D 照片），1 s 轮询 `/state.fengge`，出了新的一句弹气泡停 6 s（打开页面前那句不弹，判断方式和 voice.js 一样：`t|event|text` 变了才算），红灯红框、登顶金框，登顶期间（`body.summit`）头像 6→9.5rem；只管画面不碰声音；`?fengge=0` 头像也不出（验过）。气泡宽 ≤ min(20rem, 半屏−16rem)，小窗口也碰不到正中的红灯面板。**真链路联调**（`--sim` 8813 + 内置浏览器实时 /game，驱动 /sim 走东京）：进红灯 → `/state.fengge` 出兜底语录（source=canned，大脑没开）→ 气泡 ≤1 s 弹出、6 s 后收；走完一圈登顶那句同样弹出，东京第 2 圈第 4 步就是红灯，登顶那句很快被红灯那句顶掉。**截图**（离线预览 `&say=…&sayev=…` 摆的状态，1280×720 东京夜景）：`docs/提交/截图/fengge_hud_walk.png`（普通行走，只有头像）、`fengge_hud_red.png`（红灯 + 红框气泡，离红灯面板约 200 px）、`fengge_hud_summit.png`（登顶，头像放大 + 金框气泡，登顶卡在底部不重叠）。**没做到**：hud.js 的影子标签避让列表（`AVOID`）没有 `fg`，影子跑到画面左上时标签可能压到气泡（hud.js 属于 M，要加就在 AVOID 里加 `'fg'`）；接上 Claude 后解说要等大脑返回（最长 15 s 超时），可能登顶卡收了才出来。<br>✅ **第 1 轮 3D 峰哥头** 已接进游戏（`?fengge=0` 关）。**空白原因**：页面本身不报错（内置浏览器正常画），是 `shot.sh` 的 `--virtual-time-budget` 卡在 `loadAvatar()` 的 glb 贴图解码上（加 stage 标记实测停在 loadAvatar；预算加到 30 s 照样空白），与等待时长无关；改用 DevTools 按真实时间等 `<body data-ready>` 再截（预览页加了 `data-ready` + `#errlog`，`scripts/dom.sh` 可直接查）。**顺手修的真问题**：原来按 z>1.30 量头，漏掉下半个头，脸片比原头盔小，被盖住只露出鼻子——改按头骨权重量、原头盔顶点塌掉、脸片按照片比例铺（不拉伸不镜像）。**截图** `docs/提交/截图/fengge_front.png`、`fengge_left30.png`、`fengge_right30.png`、`fengge_walk.png`、`fengge_night_front.png`（东京夜景正面）、`fengge_night_game.png`（东京实际跟拍视角，只见帽子+后脑）；5 个主题预览均无报错。**开销**（东京 pos 18 同视角，1024×768）：+3 次绘制（66→69）、+4.4k 三角形（73.5k→77.8k），同步渲染 60 帧均值 43.9→44.1 ms（在误差内）；绝对值偏高是面板隐藏 + 机器负载 18 下强制 readPixels 同步测的，rAF FPS 没测成。**没做到**：跟拍镜头在身后，游戏里只有登顶环绕才看得到脸而且脸很小；侧面/背面是纯色 |
| **V 峰哥语音** | `brain/tts.py`（新）、`shellos/agent/voice.py`（新）、`static/voice.js`（新）；`server.py` 只加 `/voice/*` 路由 | 峰哥解说（`App.fengge.last`）→ 语音播放；调研声音克隆方案（参考 YeJe-cpu/talk-to-fengge、w466747380/talk-to-fengge-live 的参考音频与方案），离线兜底 | ✅ **峰哥音色上线了**（总指挥 main 9c94e22）：主工作区 MiniMax 复刻成功，11 句兜底语录全部用 MiniMax 预生成（24 kHz）；`run.sh` 一起起 tts（8791），隧道多转了 8791；`game.html` 已引入 voice.js；端到端验证过（造山 → 兜底解说 → `/voice/last.wav` 回 24 kHz MiniMax 音频）。实时合成一句 4.2 / 7.8 / 10.2 s，MiniMax 超时定为 15 s，ShellOS 等 25 s。MiniMax 失败或断网时：本地缓存 → `say` 女声 → 不出声。**复刻做了两次（V 一次、总指挥一次），约 19.8 元** |
| **C Claude 实测 + 部署** | `brain/claude_brain.py`、`shellos/agent/brain.py`、`shellos/agent/interpret.py`、`shellos/worlds/gen.py`、`scripts/eval_*.py`（新）、`brain/run.sh`（新） | 拿到 key 后实测：教练 23 句准确率 / 造山 10 句质量 / 延迟；调 prompt；本机大脑 + SSH 反向隧道到 MacBook 一键脚本；deploy 到 MacBook 联调 | 待 key（live 测评没跑）。key 前做完：① `brain/run.sh` 一键大脑 + 隧道，断线 3 s 重连、SIGTERM 整组退（实测）；`scripts/eval_coach.py` / `eval_world.py` 默认 --dry。② **Tailscale 慢的原因**：`tailscale ping` = DERP(sfo) 中继 ~380 ms（首包 2.4 s），没有直连；MacBook 开着 Shadowrocket（TUN + 系统代理 127.0.0.1:1082），Tailscale 的 UDP 被带去美国出口（MacBook netcheck：出口 103.151.173.98、MappingVariesByDestIP=true、最近 DERP=洛杉矶）。经隧道每次调大脑固定多 **0.72 s**（/health 5 次 0.71–0.73 s）。修法要改 MacBook 系统设置，步骤写在下面留言里，给球球。③ **MacBook 离线兜底真机过**（Python 3.9.6，--sim）：造山 20 ms 走 rule；评委反馈 10–19 ms 走规则表；峰哥 source=canned；/state.brain 离线，loop_ms 中位 15 / max 15，/state 7–9 ms。④ 顺手修了个 bug：ShellOS 的 urllib 跟着系统代理走，Shadowrocket 把 127.0.0.1:8790 截走、回 503 → `shellos/agent/brain.py` 改成不走代理（MacBook 上复测是真的 Connection refused）。⑤ 失败分类：大脑 /health 回 `key` 布尔；失败统一 502 `{kind}`；ShellOS `/state.brain` 加了 `kind / up / key / model`。MacBook 经隧道打没 key 的大脑 → `kind=no_key`，关掉大脑 → `offline`（实测）。pytest 101 过，1 个挂（`test_stress_stack_end_to_end`，只在 worktree 里挂、主 checkout 过，缺 .venv）。commit 1abd593 · 6926be1 · 182af28
| **S 提交物 / 叙事** | `docs/提交/**`、README 的「项目介绍」段 | 改名峰哥亡命天涯；加蜂群（峰哥当年合伙的公司就叫「蜂群文化」）、一句话造山、峰哥解说；评委题库补：断网怎么办 / 安全员为何不用大模型 / 肖像与素材授权 | ✅ 第 2 轮（分支 claude/xenodochial-clarke-591af9，未合）：`docs/提交/poster.html`（A1 海报）、`onepager.html`（展位一页纸，A4 正反面，手机可看）、`video_storyboard.md`（60 秒拍摄分镜，每镜头写了页面 / URL 参数 / curl）。两个 HTML 都有峰哥开关：地址加 `?fengge=0` 或点右上角「切换」= 不含峰哥名字 / 照片 / 解说气泡的备用版；**本人同意前只打印备用版**。**验收**：无头 Chrome printToPDF（preferCSSPageSize）：海报两版各 1 页、594×841 mm；一页纸两版各 2 页、A4；逐页渲染看过无溢出、无截断；390 宽手机屏无横滚。新截图（9/23 `--sim` 端口 8851 实录 + 离线预览，`?fengge=0`）：`截图/游戏-东京天桥-v2.jpg`、`游戏-泰山登顶-v2.jpg`、`游戏-现场造山.jpg`、`仪表盘-蜂群.png`、`选山-造山.jpg`——仪表盘 / 选山是大脑离线时拍的，界面照实写「规则兜底」。项目介绍截图表换成新图，§5.4 / 题库 D3 跟上 H（峰哥头已接进游戏）和 V（现在是普通女声，不是峰哥音色）。⚠ `scripts/shot.sh` 拍仪表盘卡死（页面 10 Hz 轮询，虚拟时间用不完），改用 DevTools 协议按真实时间截图（临时脚本，没进仓库）。**缺的素材（9/24 上午补拍，清单在 video_storyboard.md 末尾）**：真人共驾 / 踏步 / 松扳机；Claude 在线的蜂群时间线和 Claude 造的山；峰哥头登顶特写和大屏解说气泡（等本人同意 + M 线）；海报主图 4K 重拍（现在 1920 宽，A1 约 80 dpi） |
| **总指挥** | `docs/指挥板.md`、`docs/队友跟进.md`、`shellos/main.py`、`shellos/ui/server.py`（除 `/voice/*`）、`static/index.html`、`static/worlds.html`、git | 合并各线、统一提交推送、更新交接文档、部署 | — |

### 留言（给别的线 / 总指挥）
- C → 总指挥（仪表盘，index.html:301）：`s.brain` 多了 `kind`（offline / timeout / no_key / auth / refusal / rate_limit / overloaded / net / error / ok）和心跳字段 `up / key / model`，`msg` 里已经是中文说明。建议显示成：`up===false` →「大脑离线」；`up && key===false` →「大脑在线 · 没配 key」；`up` →「大脑在线 · {model}」；后面接「上次：{msg}」。颜色：`ok || (up && key)` 绿，`ok===false || up===false || key===false` 橙。
- C → 总指挥（main.py）：ShellOS 启动时（`main()` 里起 HTTP 之后，别放进 App 构造，免得测试起线程）调一次 `from .agent import brain; brain.watch()`，10 s 一次 GET /health，仪表盘不用等评委第一句话就知道大脑在不在、有没有 key；心跳说大脑不在时，教练 / 造山直接走兜底，不用等 12 s 超时。
- C → 总指挥（deploy.sh）：⚠ **别在 worktree 里跑 `./deploy.sh`**：worktree 的 `.git` 是个文件，`rsync --delete` 会把 MacBook 上的 `~/aima/.git` 整个删掉（dry-run 看到了）。我这次用的是同一条 rsync 再加 `--exclude .git`，部署的是 main b0b92e3 + C 线改动。所以 MacBook 的 git 还停在 997c3a9，工作区显示是脏的。建议 deploy.sh 加 `--exclude .git`。
- C → V（voice.py）：`urllib.request.urlopen` 会跟着系统代理走，MacBook 上 127.0.0.1:8791 会被 Shadowrocket 截走（大脑这边就是这么拿到 503 的）。改成 `urllib.request.build_opener(urllib.request.ProxyHandler({})).open`，写法见 `shellos/agent/brain.py`。
- C → 球球（MacBook 系统设置，我不改）：Tailscale 现在走旧金山中继。二选一：(a) 展位上把 MacBook 的 Shadowrocket 关掉（ShellOS 不需要它，Claude 在开发机的大脑里调）；(b) 留着 Shadowrocket，在「配置 → 规则」里加 `IP-CIDR,100.64.0.0/10,DIRECT`、`DOMAIN-SUFFIX,tailscale.com,DIRECT`、`DOMAIN-SUFFIX,tailscale.io,DIRECT`，并把 Tailscale 加进绕过 / 直连应用。改完在开发机上跑 `tailscale ping 100.112.252.66`，看到 `via <IP>:<端口>`（不是 `via DERP`）就是直连了。两台机器接同一个 Wi-Fi 最容易直连。
- 总指挥 → M：游戏标题 / 登顶画面的项目名改「峰哥亡命天涯」；`gen_*` 世界（现场生成）theme 复制自同 style，按 `theme.style` 渲染，别写死世界 id；`/state.fengge = {t, event, text, source}` 是峰哥解说，HUD 可以出一个气泡。
- 总指挥 → H：`engine.js` 属于 M，你只加 import + 一行调用，合并冲突总指挥处理。
- H → M：换了峰哥头后，富士山（`themes/night_to_dawn.js` 的 `exoHeadlamp`）头灯按旧头盔摆，正好落在峰哥嘴上。`fengge.js` 在 `av.group.userData.fengge.lamp` 给了帽檐正前方的位置（头骨局部坐标），改一行：`hl.position.copy(av.userData.fengge?.lamp ?? <原来的算法>)`（浏览器里试过，头灯落在帽檐上）。
- H → 总指挥：`scripts/shot.sh`（虚拟时间）拍带 glb 的页面不可靠，这次 `/fengge` 就是它空白；建议加个按真实时间等 `data-ready` 再 `Page.captureScreenshot` 的版本（在 `dom.mjs` 基础上加 10 行，我本地用过，没进仓库因为不在我名下）。
- V → M：`static/game.html` 的 `</body>` 前加一行 `<script src="/voice/voice.js"></script>` 就有峰哥声音（1 s 轮询 /state.fengge，有新的一句就播；打开页面前那句不重播）。Chrome 不让没交互过的页面自动出声：被拦时左下角出一个「点一下打开峰哥声音」按钮，按任意键（空格走路也算）或点一下就解锁。展位也可以用 `--autoplay-policy=no-user-gesture-required` 启动 Chrome
- V → 总指挥：`index.html` 建议**不引入** voice.js，免得仪表盘和游戏屏两边一起念出回声；真要引入，就给不想出声的那块屏的地址加 `?voice=0`。
- ~~V → 总指挥（峰哥音色上线三步）~~ **总指挥已在主工作区自己做完（9c94e22），这条作废；别从 V 的 worktree 拷 voice_id，V 那个已经删了**。原文：① 从 V 的 worktree（`.claude/worktrees/gifted-mendeleev-2abd37/`）把 `data/voice/minimax_voice_id` 和 `data/voice/ref/` 拷到主工作区的 `data/voice/`；② 在主工作区跑 `rm -f data/voice/*.wav && python3 brain/tts.py --canned`，key 从主工作区的 `brain/.env` 读，11 句约 0.14 元，这 11 句就都是峰哥音色了；③ `./deploy.sh`，把 `data/voice/` 同步到 MacBook，大脑或隧道断了这 11 句照样有声。**MacBook 上不要放 key，也不要在 MacBook 上跑 `--canned`**（那边没 key，会生成成女声）
- V → 总指挥（**峰哥音色上线三步**，都在主工作区做，这些文件都被 gitignore，git 不会带过去）：① 从 V 的 worktree（`.claude/worktrees/gifted-mendeleev-2abd37/`）把 `data/voice/minimax_voice_id` 和 `data/voice/ref/` 拷到主工作区的 `data/voice/`；② 在主工作区跑 `rm -f data/voice/*.wav && python3 brain/tts.py --canned`，key 从主工作区的 `brain/.env` 读，11 句约 0.14 元，这 11 句就都是峰哥音色了；③ `./deploy.sh`，把 `data/voice/` 同步到 MacBook，大脑或隧道断了这 11 句照样有声。**MacBook 上不要放 key，也不要在 MacBook 上跑 `--canned`**（那边没 key，会生成成女声）
- V → 总指挥：没改的 `scripts/afc.py`、`collect.py`、`strength_ladder.py`、`stress.py` 也是用 `urllib.request.urlopen` 访问 127.0.0.1，在开着 Shadowrocket 的 MacBook 上会跟 voice.py 一样被截走。要在 MacBook 上跑它们就按 `brain.py` 的写法改
- ~~V → C~~（C / 总指挥已做：`run.sh` 起 tts + 隧道转 8791）：`brain/run.sh` 里大脑旁边再起一个 `python3 brain/tts.py`（端口 8791；只用标准库，不用进 venv；MiniMax 的 key 它自己从 `brain/.env` 读），隧道多转一个口：`ssh -N -R 8790:127.0.0.1:8790 -R 8791:127.0.0.1:8791 zhongrenfei@100.112.252.66`
- V → C：`brain/run.sh` 里大脑旁边再起一个 `python3 brain/tts.py`（端口 8791；只用标准库，不用进 venv；MiniMax 的 key 它自己从 `brain/.env` 读），隧道多转一个口：`ssh -N -R 8790:127.0.0.1:8790 -R 8791:127.0.0.1:8791 zhongrenfei@100.112.252.66`

### V 峰哥语音（`brain/tts.py`、`shellos/agent/voice.py`、`shellos/ui/static/voice.js`、`server.py` 的 `/voice/*`、`tests/test_voice.py`、`data/voice/.gitignore`、`brain/.gitignore`）
**链路**：`App.fengge.last.text` → `GET /voice/last.wav`，**只念当前这一句，不接收任意文字**（局域网里谁也没法借这个接口让"峰哥"说别的话）。ShellOS 先查本地缓存 `data/voice/<sha1>.wav`，没有就 POST 给大脑那台机器上的 `brain/tts.py`（`SHELLOS_TTS`，默认 127.0.0.1:8791）拿 wav，存进缓存再回给页面。TTS 挂了、断网或超时一律回 204，页面不出声也不报错，20 s 内不再重试（跟 `brain.py` 的规矩一样）。合成只在 `brain/tts.py` 的 `synth(text) -> wav bytes` 这一个函数里：换方案 A 或 B 只改它，改完 `rm data/voice/*.wav && python3 brain/tts.py --canned` 重新生成
**调研**（一轮）：YeJe-cpu/talk-to-fengge 首选 VoxCPM2（2B，要 CUDA GPU），CPU 兜底 MOSS-TTS-Nano，云端备选 Cartesia（克隆要 $5/月）/ MiniMax；自带 45 s `fengge_ref.wav`，作者从峰哥公开直播里剪的。w466747380/talk-to-fengge-live 用 Vidu S1，那是实时数字人通话接口，不是文字转音频，用不上。开发机 curl hf / hf-mirror / modelscope / minimaxi / dashscope / cartesia 都能连上。球球定了 A
**方案 B（MiniMax 快速复刻，9/23 球球改选）**：接口照官方文档（platform.minimax.cn/docs）写，没凭记忆。国内站 base 是 `https://api.minimax.cn`（旧域名 api.minimaxi.com 也能通；国际站 api.minimax.io 的 key 不通用），`Authorization: Bearer <key>`，出错也是 HTTP 200，要看 `base_resp.status_code`
- `python3 brain/tts.py --clone`，只跑一次：先 `POST /v1/files/upload`（multipart，purpose=voice_clone）上传 `data/voice/ref/fengge_ref.wav`（YeJe 仓库的参考音频，实际 47 s、16 kHz，**会上传给 MiniMax**），再 `POST /v1/voice_clone`（开降噪 + 音量归一），voice_id 形如 `Fengge0923xxxxxx`，存到 `data/voice/minimax_voice_id`
- 合成：`POST /v1/t2a_v2`，模型 `speech-2.8-hd`（可以用 `MINIMAX_MODEL` 改），wav 24 kHz 单声道，返回 hex，超时 `MM_TIMEOUT` 15 s（`--canned` 时 60 s，失败也不退避）
- 合成：`POST /v1/t2a_v2`，模型 `speech-2.8-hd`（可以用 `MINIMAX_MODEL` 改），wav 24 kHz 单声道，返回 hex，超时 8 s
- 兜底链：本地缓存 → MiniMax → `say`（MiniMax 失败后 30 s 内直接走 say；say 念出来的带 `no-store`，大脑和 ShellOS **两边都不缓存**，免得 MiniMax 恢复后这句还是女声）→ 204 静音
- 账号要求（文档原话）：快速复刻前账号**必须先完成个人实名认证或企业认证**，否则回 2038；复刻出来的音色 **7 天内不正式调用会被删**
- 费用（按量计费页）：快速复刻 **9.9 元/音色**，首次用它合成时才扣；speech-2.8-hd **3.5 元/万字符**（turbo 2 元）。11 句兜底语录约 200 字 ≈ 0.07 元；一句解说不超过 40 字 ≈ 0.014 元，念 1000 句约 14 元
**方案 B 真接口验证**（9/23，球球确认已实名认证、key 已放进主工作区 `brain/.env` 之后才跑；在 V 的 worktree 里把 key 读进进程环境，没复制、没打印、没写进任何文件）：先查底层，`curl -I https://api.minimax.cn` 回 404（能连通）。`--clone` **1 次**，14.8 s，成功，说明实名认证确实通过了（没认证会回 2038），voice_id 存进 worktree 的 `data/voice/minimax_voice_id`。合成 **1 句**「这是个好事儿啊，腿酸说明你真爬了。」：**10.2 s**，150 KB，RIFF / PCM 16 bit / 24 kHz 单声道 / 3.1 s，计费 32 字符（17 个字，中文 1 字按约 2 字符算），已缓存。**花费约 9.9 元（复刻）+ 0.01 元（合成）**。因为这句要 10.2 s，把超时放宽了：MiniMax 8 → 15 s，ShellOS 等 TTS 15 → 25 s。宁可晚几秒念出峰哥的声音，也别退成女声
**上线（总指挥，main 9c94e22）**：开发机上行只有约 10 KB/s，第一次上传 1.5 MB 的 wav 超时了。现在 `--clone` 如果看到 `data/voice/ref/fengge_ref.m4a`（`afconvert -f m4af -d aac -b 32000` 转出来的 32 kbps AAC，约 200 KB）就优先传它，上传超时改成 180 s。在主工作区复刻成功（`data/voice/minimax_voice_id`，只在本地）。`--canned` 时 `MM_TIMEOUT=60` 且不退避：之前一句超时后，后面几句都退回了 say。现在 11 句全部是 MiniMax 生成的。实时合成两句 4.2 s、7.8 s；端到端：造山 → 兜底解说 → `/voice/last.wav` 回 24 kHz MiniMax 音频。`run.sh` 起 tts、隧道多转 8791、`game.html` 引入 voice.js，都已完成
**合并时 V 定的超时**：实测三句 4.2 / 7.8 / 10.2 s，main 原来的 8 s 太紧（7.8 s 差一点就退成女声）。`MM_TIMEOUT` 改成 15 s；ShellOS 的 `voice.get` 在 f2a5fe6 里已经放到 25 s（15 s + TLS 约 2 s + say 约 2 s，还有余量）。Claude 现编的那句最坏要等大脑 ≤15 s 再等 MiniMax ≤15 s；兜底语录都缓存了，不受影响
**球球试听**（9/23，V 在开发机上用 `afplay` 放）：主工作区 11 句兜底语录都是 24 kHz 的 MiniMax 音频，没有一句退成女声。像不像，以球球的耳朵为准
**费用（照实写）**：复刻做了两次。V 在 worktree 复刻了 `Fengge0923123730` 并合成了 1 句；总指挥差不多同一时间在主工作区又复刻了 `Fengge0923123754`，并生成了 11 句。按计费规则（首次用它合成时扣 9.9 元），**两个音色都会扣钱，合计约 19.8 元 + 合成约 0.15 元**。线上用的是后一个；V worktree 里前一个的 voice_id 文件和那句缓存已经删了。以后要复刻，先看主工作区的 `data/voice/minimax_voice_id` 在不在
**A 为什么没上**（诊断一轮就停）：onnxruntime 最后一个 Intel Mac 版本是 1.23.2，只支持到 cp313，没有 3.14；MOSS 的 `onnx_tts_runtime.py` 顶层还 import 了 torch/torchaudio，torch 最后一个 Intel Mac 版本是 2.2.2，只到 cp312。所以退回 C，先把链路跑通，**权重没下载，参考音频也还没拉**
**验收**
- `pytest tests/test_voice.py` 5 过：缓存命中不联网；TTS 挂了回 None 且 20 s 内不重试；从 brain/tts.py 拿到后落缓存，第二次不再合成；空文字 / 超过 200 字回 400；`/voice/last.wav` 没解说时 204、有解说时 200 audio/wav，query 里塞的文字不念。全套 101 过 1 挂，挂的是 `test_fault::test_stress_stack_end_to_end`：这个 worktree 里没有被 gitignore 的 `data/recordings/synthetic-walk.csv`，跟语音无关。新增和改动的 .py 用系统 Python 3.9.6 编译通过
- 实测（`--sim` 8857 + `brain/tts.py` 8791）：`--canned` 生成 11 句用了 16 s，共 2.3 MB，`git status -uall` 只看到 `data/voice/.gitignore`。没解说时 `/voice/last.wav` 回 204；`/world/generate` 触发一句兜底语录后回 200 audio/wav，227 KB，**27 ms**（命中缓存）；没缓存的新句子走 TTS 要 **5.05 s**（这台机器当时负载 52；空闲时 `say` 1.8 s 生成 5.5 s 的音频），第二次 0.14 s；TTS 断了回 None，用时 1 ms，兜底语录照样能拿到
- 浏览器面板：仪表盘页注入 voice.js，打开页面前那句不重播；造山后 `play()` 调了 1 次并且 resolve，控制台 0 报错
**不理想 / 没做**
- **实时合成延迟只有 3 个数据点**（4.2 / 7.8 / 10.2 s），开发机当时负载 50~60 以上，TLS 握手约 2 s。Claude 现编的那句要等大脑（≤15 s）再等 MiniMax（约 10 s），**念出来会比字幕晚 10 s 上下**；兜底语录已经缓存了，不受影响。想快一点可以设 `MINIMAX_MODEL=speech-2.8-turbo`（2 元/万字符），但 turbo 快多少没测，测要再花钱，等球球定
- **只合成过 1 句，延迟是单点数据**：10.2 s，里面有 TLS 握手 2 s 左右；当时开发机负载 60 以上，可能还是新音色第一次调用。Claude 现编的那句要等大脑（≤15 s）再等 MiniMax（约 10 s），**念出来会比字幕晚 10 s 上下**；兜底语录已经缓存了，不受影响。想快一点可以设 `MINIMAX_MODEL=speech-2.8-turbo`（2 元/万字符），但 turbo 快多少没测，测要再花钱，等球球定
- 听起来像不像峰哥，只有球球人耳听过的才算数（样本：worktree 的 `data/voice/2e463cc866f3690c.wav`）；我没法判断
- 复刻出来的音色 **7 天内不正式调用会被删**。今天已经合成过 1 句，文档说的「正式调用」应该算已经满足了，但没有验证
- ShellOS 连 TTS 改成不走系统代理（C 线发现 MacBook 的 Shadowrocket 会把 127.0.0.1 截走）。写法同 `brain.py`，测试里访问本机端口也不走代理
- **B 还没打过真接口**：等球球实名认证、充值、把 key 放进 `brain/.env`。之后只做一次验证：`--clone` 一次 + 合成一句；通过了再 `rm data/voice/*.wav && python3 brain/tts.py --canned`，把兜底语录重新生成成峰哥音色。`format: wav` 是按文档枚举写的，真返回是不是 RIFF 要到那一次才知道（不是的话会报错退到 say，不会出事）
- 方案 B 验收（`pytest tests/test_voice.py` 10 过，MiniMax 是本机假服务，say 也换成假的）：T2A 请求带 Bearer / voice_id / wav / 非流式，第二次走缓存不再花钱；欠费（1008）时 say 顶上，两边都不缓存，30 s 内不再打 MiniMax；有 key 没复刻、配好了但连不上、没 key 三种情况都退到 say；`--clone` 上传的 multipart 里有 purpose=voice_clone 和音频本体，voice_id 符合规则，复刻过一次后不会再传；没认证（2038）时抛错，也不写 voice_id。全套 106 过，挂的还是那条 stress（原因同上）。系统 Python 3.9.6 编译、import `brain/tts.py` 都通过。没 key 时实跑 `brain/tts.py`：`/health` 回 `say:Tingting`，`/tts` 回 200 audio/wav。`git check-ignore`：`brain/.env`、`data/voice/ref/fengge_ref.wav`、`data/voice/minimax_voice_id` 都被忽略
- 自动播放被拦的解锁按钮只是代码写了，面板里默认允许自动播放，没触发到这条路径
- **授权**：峰哥是真人，就在现场。换成克隆音色后只能用在比赛原型里，**展示前球球当面征得峰哥本人同意**。方案 B 会把参考音频（剪自峰哥公开直播的 47 s）**上传给 MiniMax**，复刻出来的音色存在球球的 MiniMax 账号里；克隆出来的音频只放在本地 `data/voice/`（目录里有 `.gitignore`，不进 git），不公开发布。展示完可以用官方的「删除音色」「文件删除」两个接口（platform.minimax.cn/docs/api-reference/voice-management-delete、file-management-delete）把音色和上传的参考音频删掉。脚本里没写这一步，要删的时候再加

---

# 9/23 夜 6 条并行线（已收尾，过程记录）

> 开工先读 `docs/架构-v3-完整版.md`（第 2 节规格、第 3 节分工与验收）。只改自己名下的文件；要动别人的写到「给别的线的留言」。不 push，总指挥统一提交。结果不理想照实写。
> 今晚没有硬件：用 `--sim`（模拟，空格走）或 `--replay data/recordings/*.csv`（9/22 真机录制）。

## 最终状态（9/23 夜收尾，端口 8810）

> 总结见 `docs/报告/今晚总结.md`；明天前 60 分钟清单（带命令）见架构 v3 §4。下面各线小节是过程记录。

| 线 | 状态 | 验收（来源） | 留到明天 / 下一阶段 |
|---|---|---|---|
| G 登山游戏 | ✅ 引擎完成，两轮审查的 11 条已修 | 模拟 5 圈登顶、影子前进；1080p 50–58 fps（开发机）；loop_ms p95 +5–9 ms（压线） | 地图精加工：影子标签压红灯面板、富士拐角楔形；MacBook FPS；真机「化身跟腿」 |
| A 步态鲁棒 | ✅ | 回放：坐站假周期 13 → 0；非走路误判 162 → 3 s；最长连续出力 ≤0.41 s；踏步模式安全项同默认 | HS_PHASE、踏步真机数据、3 人门限 |
| B 地形手感 | ✅ | 脉冲核对全过；红灯回放中位 2.2 s / 模拟 2.0 s；2AFC 0 力轮作废 | 手感全部未验；缺省强度、红灯阈值真机看一次 |
| D 仪表盘 | ✅ | 脉冲高亮 83/150；375 宽无横滚；3D 腿轴已修 | 真机看腿方向（flexSign） |
| E 提交物 | ✅ 成稿 | 5 份 + README，数字对过来源 | 9/24 上午回填真机数字；60 秒视频、终版截图 |
| F 稳定性 | ✅ | 故障矩阵 11/11；13 段回放 × 20 s 0 DISARM；`scripts/stress.py` 进仓库 | 真机拔线 / 复位；真机录制复跑 stress |
| 整合 / 收尾 | ✅ 已提交并推送（commit 号见总结 / 汇报） | pytest 92 passed；系统 Python 3.9.6 全部 .py 语法通过 | — |

## 各线状态（自己更新自己这一节）

### G 登山游戏 ★（`static/game.html`、`static/game/`、`static/vendor/` 新增、`server.py` 的 `/game` 路由）
**做了**（引擎；各世界精细地图留给下一轮）
- `game.html`（HUD DOM + importmap）+ `game/`：`engine.js`（轮询 /state 10 Hz、60 fps 插值、预览模式）· `path.js`（路线→几何：坡=斜面带，台阶=InstancedMesh 方块成阶，每步刻度 + 路沿，红灯段=停止线 + 斑马线对面的信号灯，起点营地、山顶旗；高度规则与 terrain.py 的 RISE 一致）· `avatar.js`（CesiumMan，body3d 同一套骨骼映射：`leg_joint_*_1` 髋 / `_2` 膝，flex = −frame.l；另加手臂放下 + 与腿反向摆）· `ghost.js`（整数步→连续位置的平滑器，化身和影子共用；影子合成步态）· `camera.js`（侧后跟随，上坡抬、台阶拉近，登顶环绕一圈）· `hud.js`（海拔、第 n/N 步、路段 + 类型色块、「n 步后：…」预告、左右腿力矩条（上升沿过 0.25 Nm 闪）、用时/最佳/登顶次数/穿戴者、安全灯、红灯站定倒计时、登顶卡片「已写入山的记忆」、经验卡新卡/命中滑入）· `fx.js`（脉冲 → 脚下波纹 + 当前那一步路面发亮；登顶彩带、旗子飘）· `input.js`（模拟：按住空格走、1/2/3 = 80/105/130 步/分；R = /demo/reset）
- 主题插件 `game/themes/<style>.js`：`build(scene, ctx)` / `update(dt, st)`，5 个基础版（cyber_night 夜空+紫雾+楼群剪影+霓虹条；dawn_mountain / subtropical 山体高度场 + 远山剪影；night_to_dawn 随进度从星空过渡到日出；grid 干净网格）。公用工具在 `themes/kit.js`（天空穹顶、雾、沿路高度场、远山、网格贴图、噪声；ctx / st 字段写在文件头）。**下一轮加道具/灯光/粒子只改 `themes/<style>.js`**
- 离线预览：`/game?preview=<world_id>&pos=<n>&ghost=<n>[&who=名字][&summit=1]`，不轮询，用 /worlds.json 摆静态画面；`window.__ready` 渲染完为真；调试 `window.__game`、`window.__camHold=true` 可手动摆镜头
- 降画质开关 `/game?aa=0`（关抗锯齿）；非 terrain 控制律时显示横幅 + 一键切回；/terrain 换世界时游戏页自动刷新
- `server.py` 只改一行：静态前缀加 `/game/`
- 化身前进节奏学的是「pos 实际多久涨一次」而不是步频（按 A 线留言：真机只接住约一半的步），停顿时站在原地、腿照真实髋角动

**验收**
- `--sim`（8801）+ 驱动脚本自动走（遇红灯站 2.5 s）：连续 5 圈登顶；第二圈起影子出现且逐步前进（ghost_pos 1→8→14→20→26→32，与 pos 并行），头顶「上一位：anon 并排/领先 2 步」；红灯站定 2 s 放行；登顶卡片、彩带、环绕镜头；R 回 pos 0；空格走/松停、3 → 130 步/分；切 /terrain 泰山页面自动换世界；切 transparent 出横幅
- 截图 `/tmp/shellos-shots/G/`：4 个世界各 `-pos0.png`、`-mid.png`（tokyo 18 / taishan 20 / fuji 20 / wutong 17），`taishan_18pan-summit.png`（登顶），`sim-live.png`（模拟运行中、天桥台阶、影子领先 2 步）
- **FPS**（这台开发机 i5-7400 + 核显，Claude 浏览器面板模拟 1920×1080，canvas 1920×1080、pixelRatio 1、无阴影）：抗锯齿开 50–58 fps（10 次采样 54/58/54/53/54/52/50/53/53/54）；`?aa=0` 54–58。MacBook M2 Pro 没测
- **loop_ms**（/state 里读，同样 60 s 自动走、每 0.2 s 采一次）：不开游戏 中位 14 / p95 16 ms（两次一样）；开游戏 1080p 中位 15 / p95 21–22 ms；小窗口 中位 15 / p95 18–19。**中位 +1 ms 达标；p95 +5–6 ms 正好在线上**，这台机器同时还跑着别的线的进程，结果偏悲观
- pytest 23 过（我没加 Python 代码）

**没做完 / 不理想**
- ⚠ **无头截图（scripts/shot.sh，swiftshader 软件渲染）吃约 200% CPU，连拍 8 张期间主循环卡了 1.5 s → 看门狗 DISARMED**（安全层工作正常）。展位机/穿戴时千万别跑 shot.sh；真 GPU 浏览器里没复现（最大 69–99 ms）
- 各世界只有基础版场景（天空/雾/地面/远景），道具、雨、灯光、云海都没做——下一轮精加工
- 化身真实髋角只在模拟数据上看过；9/22 录制没跑回放验证；明天上真机第 9 条要看「化身跟腿」
- 富士 Z 字坡拐角路面有折痕；东京台阶在暗色主题下偏暗（路面色取自 world.theme.path）
- 登顶时化身在山顶停 6 s，这段时间如果继续走，pos 已经从 0 往前，6 s 后化身直接跳到当前位置

### A 步态鲁棒（`shellos/gait/`、`scripts/gait_audit.py`、`tests/test_gait.py`）
**做了**（报告 `docs/报告/步态审计.md`）
- `scripts/gait_audit.py`：12 段录制 × 1080 档门限网格（周期区间 / 整周期最低 conf / 活动度 / 两腿同相），每段接受/拒绝、坐站假周期、覆盖率、步频中位 P10/P90；走动中保持时长；原地踏步模拟；`--hs` 估 HS_PHASE；`--rec` 直接跑明天的新录制
- 发现 9/23 前门限**穿戴坐站假周期不是 0，是 13 个**（174946 第 85–100 s，两腿同相）；走动段只接住 25% 的步
- `estimator.py` 改默认（证据见报告 §2）：半周期锁存（去一步切两段）+ 两腿同相检查（sync_tol 0.2）+ 去掉整周期最低 conf + 周期 0.8–2.2 s。接口不变，全部门限可用关键字覆盖；`latch=False` 回旧规则
- 踏步模式 `GaitEstimator(stepping=True)`（rom≥8、r_ref=4），默认关；`recent_s` 选项（走动中要求最近接受过周期），默认关
- `tests/test_gait.py` +4：锁存、同相/单腿拒、踏步模式、审计脚本自检（参考标签 + HS 检测找回已知相位）

**验收**：穿戴坐站假周期 13 → **0**（网格相邻档也 0）；181327 走动段覆盖率 0.25 → **0.44**；步频中位 108（P10–P90 77–125）；模拟 60/90/110/140 步/分估准；`--sim` 全栈 8802 走/停/变速正常；全套 pytest 23 过；系统 Python 3.9.6 跑通

**没做完 / 不理想**
- HS_PHASE **证据不够，保持 0.5**：唯一够长的一段像原地踏步（支撑平台占比 0.44），az 冲击左右差 0.14。明天要 3 人向前走各 ≥30 s 再跑 `--hs`
- 只有球球 1 个人、约 170 s 像样的走动；覆盖率 0.44 = 地形前进约是实际步数的一半弱
- 真人踏步时「走动中」只有 44% 的时间成立（平台期 conf<0.5，也就是 Guard 的门），脉冲约一半时间给不出来——安全语义，没动
- 桌上（没穿、手摆）假周期 15 个，原理上分不出来，靠死人开关 + 穿好再开始

**修复 A（第 ② 轮审查 3 条，端口 8831，报告 §4.1）**
- 姿态变化后相位钉在 0.5、脉冲变持续力矩：`estimator.py` 加**相位停滞**（某腿 0.3 s 相位净推进 <0.05 → `LegState.stalled`）和**两腿同相**（相位差 <0.1，`inphase_tol`）两条，任一成立走动中为假，不重置保持计时；conf 不动（Guard 语义不变）。`gait_audit.py` 的走动中复算同步了这两条
- 步频改用最近周期**中位数**（停停走走时跨停顿的周期不再把 110 估成 97–100）
- 对称比改成**活动度比** rom_l/rom_r（最近 12 个接受周期均值）。周期比天然 ≈1，跛行也测不出来
- `tests/test_gait.py` +3：坐下后走动中 0.3 s 内为假 / 同相慢动作不算走动中；停停走走步频；跛行对称比。三条在旧代码上都挂

**验收**：回放（地形强制 stairs_up，任一腿最长连续出力，缺省 / width=20 / t_step=3）175524 1.34/4.95/4.94 s → **0.23/0.29/0.23 s**，174946 0.82/2.96/2.95 → **0.12/0.24/0.18 s**（≤0.3 s ✓）；合成「走→坐 −85°」持续出力 6.5 s → 0；停停走走步频 97–100 → 110；跛行 ±20/±9 对称比 1.00 → 2.20；审计：非走路误判走动中 162/87 s → 3/13 s，周期接受 / 覆盖率 0.32 / 假周期 0 不变；`--sim` 8831 走 → moving True、步频 110、sym 0.99，停 1.5 s → moving False；pytest 84 过；系统 Python 3.9.6 跑通

**不理想**
- 审计口径的「走路段里走动中占比」0.44 → 0.24。181327 真走路段实际发出的脉冲 301 → 267（−11%），丢的主要是相位停滞期被拉长的那段
- 181327 还有两处 0.36–0.41 s（一条腿慢慢停下，相位还在以约 0.2 圈/s 推进，停滞门要 0.3 s 才判），不在验收的两段里
- 红灯「站定 2 s 放行」的延迟是 terrain.py 按角速度 RMS / 新周期判的，不走 moving，这次没动（B 线的）

**修复 A ②（`--stepping` 踏步模式全栈复测，端口 8833，报告 §5.2）**
- `gait_audit.py` 加 `longest_output()`（GaitEstimator + Terrain 强制 stairs_up，conf≥0.5 口径的最长连续出力），踏步代价表多一列「最长连续出力（穿戴 / 桌上）」，默认 / 踏步模式同口径对照；附录 A 按修复 A 之后的代码重跑替换
- `tests/test_gait.py`：「坐下后走动中 0.3 s 内为假 / 同相慢动作不算」在 stepping=True 下也跑一遍；+1 条「踏步模式走→坐最长连续出力 <0.5 s（关掉停滞/同相就是 4.1 s）」

**验收**：12 段回放，踏步模式 vs 默认：穿戴假周期 0 / 0，桌上 24 / 15；非走路误判走动中 **9 / 22 s**（穿戴 / 桌上；默认 3 / 13；修复 A 之前的踏步模式是 295 / 141）；走路段走动中 0.33 / 0.24；最长连续出力 **0.41 / 0.39 s，和默认完全一样**（最坏仍是 181327@1115 s 那处）。分段：175524 width=20 0.29 → 0.32 s（第 37 s 正式走路前右腿单独一步），174946 不变。`--sim --stepping` 8833：走/停/复位后变速正常。pytest 92 过；系统 Python 3.9.6 跑过新测试
**结论**：安全项没变差，踏步模式可以在展位用，不需要标「只限慢走」；但 ±6° 不规整踏步走动中只 23–35%、±4° 不规整接不住——评委抬腿太低仍要改慢走（清单第 5 项改法见给整合的留言）
**不理想**：踏步模式的收益全是模拟；sim 摆幅固定 ±20°，全栈只验了接线；「坐着两腿交替晃 ±6°」踏步模式会接受，靠死人开关

### B 地形手感（`shellos/control/terrain.py`、`scripts/afc.py`、`scripts/pulse_plot.py`）
**做了**（报告 `docs/报告/地形手感.md`，图 `docs/报告/脉冲/*.svg`）
- `terrain.py`：抽出纯函数 `pulse(kind, phase)`；参数范围收紧（`t_push/t_brake/t_step` 0–20、`width` 10–20，原来 `t_push` 能被推进摆动期）；输出自裁 ±3 Nm（上台阶 1.2×3=3.6 以前全靠 Guard）；红灯一拍多步也停在路口前、站定计时要求"没在出步"。接口 / `status()` 字段不变
- `scripts/pulse_plot.py`（纯标准库 SVG）：5 路段左右腿一周期力矩图 + 核对（梯形 / 底宽 10–20% / 30–60% 与 60–90% 无伸展 / 无方向突变 / ≤软限）+ 27 组参数边界扫描，不过退出码 1
- `scripts/strength_ladder.py`：0.5→1.5→3 Nm 峰值三档（按路段倍率换算 strength、超软限自动压），每档强制路段 N 步，选完 `/set` + `/mark`；`--dry` 对模拟器自动跑
- `scripts/afc.py`：`--api`、`--dry`（理想观察者读实发力矩作答）、`--seed`、Ctrl-C 还原、每轮和总结果写 `/mark`、补 `from __future__`
- `tests/test_terrain.py` 8 条

**验收**：5 张图全部核对通过、边界扫描 0 组不过；`--sim` 8803 红灯：走着 30+ 步不前进且被 −0.75 Nm 轻拉 → 停 → 2.0 s 放行（pos 4→7）→ 继续计步；强度阶梯干跑实测峰值 0.50/1.50/3.00；AFC 干跑 slope 6/6、strength 4/4；Guard 斜率限下强度 3 快走脉冲重心滞后 ≤2.2%（JND 2.8%）；全套 pytest 31 过；系统 Python 3.9.6 跑通脚本和 8 条测试

**没做完 / 不理想**
- 全部是模拟，手感一条没验证；HS_PHASE 仍 0.5、正=伸展仍是推断
- 强度阶梯结果没进经验卡库（`/feedback` 只能 ±0.5 一句话），只是 `/set` + `/mark`
- 回放 181327 起不来（录制里有粘行），见给 F 的留言
- 「摆动中段 30–60%」按文献约定其实是支撑后期；两种口径（30–60、60–90）都查了，都干净

**修复 B（第 ② 轮审查 2 条，端口 8831）**
- **红灯放行太慢**：放行条件从「not gait.moving 连续 2 s」改成「没有新接受的步 + 两腿角速度 RMS < 20°/s 连续 1.5 s」，不再看 `gait.moving`；另加兜底：距上一步（或进红灯）6 s 无论如何放行。9/22 录制离线复算（`scratchpad/judge/redlight2.py`，从最后一个被接受的步算起）：181327 停步 11 次 **1.8–4.2 s、中位 2.2 s**（原来 2.0–11.0、中位约 5 s）；174946 4.7 s（原 5.2）；175524 1.5 s；走动中放行后 1 s 内又出步 0 次（没有「走着被放行」）。`--sim` 8831：红灯上走 3 s pos 不动，停下 → 2.1 s 放行（pos 4→7）
- **换世界丢影子**：`Terrain.memory` 按世界 id 存 `(ghost, ghost_who, best)`；`set_preset` 先存当前世界（空的不存）再取目标世界。8831 实测：东京爬完一圈 best 34.8 s → 切泰山（best 空）→ 切回东京 best 34.8、影子还在
- `status()` 加一个字段 `wait_need`（= 1.5，放行需要静止的秒数），其余字段不变
- `tests/test_terrain.py` 8 → 11 条（moving 卡真也放行、腿在摆不放但 6 s 兜底、换世界来回记忆各自保留；旧代码上这 3 条 + 改过的红灯条都失败）；全套 pytest 81 过
- **没做 / 不理想**：阈值 20°/s、1.5 s 只在 9/22 两三段录制上调过，真正的停步样本只有 181327 的 11 次；明天真机第一件事看一次红灯。换人（`demo_reset` 新建实例）时别的世界的记忆还会丢，见给整合的留言

**修复 B（2AFC 0 力轮计分）**
- `scripts/afc.py`：发出峰值为 0 的那轮不再问答案、不计入正确率，`/mark` 记一条 `VOID`，然后重做（`for` 改成 `while len(score) < trials`）；作废累计达到 trials 轮就停下提示查 R2 / 步态（防真机上一直不按 R2 死循环）；总结果文字带「另作废 N 轮」
- 新增 `tests/test_afc.py`（假 HTTP 服务，第 1 轮一点力不发）：断言 1 条 VOID、3 条计分全对、结果 3/3；旧代码上这条失败，新代码过；全套 pytest 89 过
- **没做**：作废上限按「累计」不按「连续」算，够用

### D 仪表盘打磨（`static/index.html`、`body3d.js`）
**做了**
- 暗场：底色 #06080b，正文 17 px、状态 28 px、数值 24–30 px；次要文字 #a9b6c5（在面板底上约 8:1）
- 地形面板挪到左列最上面：世界名 + 副标题、当前路段 label + 类型色块、前方（label · 类型 · N 步后，到头显示山顶名）、海拔（world.unit）、进度 pos/total · 第几圈 · 本圈用时 / 最佳 / 上圈、影子（并排 / 领先 N 步 / 落后 N 步 · 谁 · 第几步）；红灯段提示「站定 2 s 放行 · 已站 x s」；有 force 时显示「强制 xx」
- 剖面：阶梯轮廓 + 路段色带（已走过的变暗）+ 路段名；**力矩 |sent|>0.05 Nm 时当前步整列亮黄、化身外圈脉动、面板描黄边、标签「脉冲 x.x」**，亮 0.45 s（脉冲太短，10 Hz 轮询只采到一两帧）；本圈吃到脉冲的步在路面上方留 ▼；影子是紫点
- 经验卡：只在内容变化时重建（原来每 100 ms 重建，动画没法做）；新卡滑入 + 黄光，命中数上涨或新被套用也滑入发光，并弹顶部提示「新经验卡 #n」；套用中的卡绿框 +「已套用」
- 顶栏「演示态 / 累积态」：点演示态 = POST /demo/reset + 提示（每次点都复位一次）；累积态 = 不复位只提示；选择记在本机浏览器。原来记忆面板的「演示复位」按钮并进这里
- 模拟模式（/state 里 `sim.on`）才出现「按住走路」按钮（按下 /sim walk:true，松开/移出/失焦 walk:false）+ 步频滑条 60–140 + 「一直走」锁定
- 「打开游戏屏 ↗」按钮：新窗口 /game
- 手机竖屏（≤900 px）：单列、16 px 边距，**安全面板排第一**（急停不用往下翻），地形第二；390/375 宽无横向滚动
- 去掉了眼镜面板和手柄表里的 □ 眼镜行（眼镜已砍）；手柄表补了 Options 演示复位、左右摇杆 puppet
- 卡片引语、事件、参数名都转义后再进 innerHTML（评委说的话是用户输入）
- `body3d.js` 只改一行：pixelRatio 上限 1.5，少吃点 GPU（控制循环和游戏屏同机）

**验收**（--sim 8804 + 驱动脚本自动走、红灯站 2.6 s）
- 截图 `/tmp/shellos-shots/D/dashboard-1440x900.png`、`dashboard-390x844.png`（390 宽是 iframe 拍再裁的：无头 Chrome 窗口最窄约 500 px）
- 浏览器面板里用脚本点过：按住走路 → sim.walk true、松开 → false；一直走 / 停下正常；/feedback「太陡了」→ 新卡 class=new + 提示；走 6 步检索套用后卡片 class=hit；累积态不发请求；演示态 → 事件流出现「演示复位」
- 爬台阶时 15 s 内 83/150 次采样处于「脉冲」高亮；375 宽 scrollWidth = 375；控制台 0 报错
- pytest 31 过。测试时往 `data/experiences.jsonl` 加的 #4–#6 已 `git checkout` 还原

**没做完 / 不理想**
- 两张截图里都没拍到脉冲高亮：无头 Chrome 启动要好几秒，拍到时往往已经走出台阶。高亮本身在浏览器面板里看过（黄框 + 列高亮 + 外圈）
- 截图里的曲线只有左边一小段：无头模式的虚拟时间下只轮询了十几次，真浏览器里是满的
- 「红灯站定 2 s」里的 2 是写死的（terrain.py 的 WAIT_STILL_S），那边改了这里要跟着改
- 演示态 / 累积态只是前端的操作习惯，后端没有「模式」：累积态下换人照样会复位参数（`set_wearer` 本来就这样）

**修复 D（审查：腿部 3D 轴错）**
- `body3d.js`：髋 / 膝改绕局部 Y（`setJoint(hipL, axisY, -fl)`、膝 `axisY, max(0,fl)*0.6`），注释改成 9/23 实测；骨盆 pitch 绕 Y、roll 绕 X（同一套骨骼轴）。和 game/avatar.js 一致
- 顺手修了一个同面板的问题：3D 画布只有约 90–120 px 宽，人被裁掉一半。原因是 h2 里 `float:right` 的 `#c3info` 掉到第二行，`overflow:hidden` 的 `#c3` 形成 BFC 被挤在浮动旁边。`#c3` 加 `clear:both`（index.html），body3d.js 改成 ResizeObserver 跟容器定尺寸（原来只听 window resize）
- 新增 `tests/test_body3d.py`（2 条，钉住腿骨用 axisY、c3 clear + ResizeObserver；改回 axisX 会失败）
- 验收（--sim 8830，110 步/分）：浏览器里采 10 帧，左膝相对左髋侧向 x 恒为 0.009，前后 z 在 -0.066 到 +0.118 之间摆，左右腿反相；脚尖在踝的 +z 侧，确认 +z 是前方。画布 = 容器宽，控制台 0 报错。截图 `/tmp/shellos-shots/D/fix-body3d-walk-tall.png`、`fix-body3d-crop.png`。pytest 87 过
- 注意：server.py 启动时把 index.html 读进内存，改完 HTML 要重启进程才生效
- 明天真机第 9 条「化身跟腿」：看真人抬左腿时屏上是不是左腿向前抬；方向反了只改 `CONVENTION.flexSign`

### E 提交物（`README.md`、`docs/提交/`）
**做了**
- `docs/提交/项目介绍.md`（官网提交用）：定位句 · 做了什么 · 为什么（痛点 / 机会 / 与出题方方向） · 三层新在哪 + Holotron 对比表 · 架构图 · 地形脉冲表 · 步态 · 记忆与 Agent（对评分表） · 安全表 · 验证计划与已有数据（每个数标真机 / 回放 / 模拟 / 待真机验证） · 演示截图 4 张 · 团队（只写球球） · 复现 · 参考文献
- `海报文案.md`：主海报（标题「山的记忆」+ 一句话 + 三行卖点 + 二维码位）、A5 三步自助桌卡、备用短句、不上海报的字
- `60秒演示脚本.md`：模拟版（A，带水印）/ 真机版（B）分镜 + 录制准备命令 + 口径检查
- `4分钟展位脚本.md`：对齐架构 §5 的 6 段分镜（操作 / 旁白 / 拿哪一分）、开场前 30 秒复位动作、砍的顺序、节奏估算、现场出问题预案、禁用说法、9/24 上午回填表
- `评委题库.md`：Seeed 硬件创业者 / Hypershell 工程师 / 跨赛道终审各 15 问 + 标准答，每条标出处【】，待真机验证的照标；底线口径表
- `README.md` 重写：一句话 · 截图 · 快速开始（模拟 / 回放 / 真机）· 架构 · 安全 · 数据 · 目录 · 提交物链接；去掉眼镜 / EvoMap / 手机

**验收**：5 份提交物 + README 成稿；文中每个数字对过来源（架构 v3、A/B/G 报告、调研原文、代码）。README 里的模拟 / 回放命令在 8805 上实跑过（回放用 174946，起得来）；`/feedback`「太陡了」→ 1.5→1.0、删卡回退 → 在 8805 上实测过；跑完已还原 `data/experiences.jsonl`、kill 进程。pytest 31 过（我没改代码）

**没做完 / 不理想**
- 截图用的是 G / B / D 线 9/23 夜的模拟截图，最终版要等 G 精加工后重拍；60 秒视频没录（要在不跑 shot.sh 的机器上录）
- 所有「待真机验证」的数字（力矩方向、缺省强度、2AFC x/10、穿戴用时、一圈用时）要 9/24 上午回填，位置列在 4 分钟脚本末尾
- 实测发现 3 个会影响演示叙事的代码问题（见下方 E 的留言），脚本里先写了绕法

**修复 E（第 ② 轮审查 5 条，只改文档）**
- 删卡 vs 继承：展位 1:50、60 秒 0:40 都改成「删卡回退 → 点恢复，卡片留给下一位」（仪表盘卡片本来就有「恢复」按钮，`enable_exp` 下次检索生效）；展位脚本加警示「不恢复下一位永远 0 命中」；C3 同步
- 「第一个」：项目介绍第 12 行改成「用量产消费级髋外骨骼做……」；README / 海报 / 题库里没有正面说「首个」的地方（只在「不说」清单里）
- 海报卖点「腿能感到地形」→「腿上有力」（只描述输出）
- 穿脱时间：节奏估算加一条「问答时继续穿着，脱下在下一位到场前做；上午掐表，超 30 s 按实测重排」；回填表加「脱下用时」；备用强度改成 strength 2.0（上台阶 2.4 Nm，`--cap 2.5` 下不被裁），题库 A15 同步
- C6 换成 F / 整合实测数字（10 种故障 ≤0.09 s 恢复、12 段回放 0 DISARM、真机拔线未测）；C2 加「没配大模型就主动说今天走规则兜底」；展位脚本回填表加「开场前确认 SHELLOS_LLM_* 配没配、看卡片 source」
- 没改：`worlds.html` 副标题、架构 §0「第一个」、架构 §4 清单——不是 E 的文件，已写留言给整合 / 总指挥

### F 稳定性压测（`replay.py` 故障注入、`tests/test_fault.py`、`guard.py` 仅修 bug）
**做了**（报告 `docs/报告/稳定性.md`）
- `replay.py`：故障注入 `ReplayLink(faults=...)` 或环境变量 `SHELLOS_FAULTS="gap@10:3,noten@20,reboot@30:0.5,bad@40:0.5,stall@50:0.3"`（断流 / ERR,NOT_ENABLED / 开机日志=设备复位 / 帧格式错误 / 主机卡顿）；补上和 SerialLink 同名的 `enabled / reboots / needs_recovery / recover`，加了设备侧模型（未使能时拒绝 T、固件 100 ms 清零）；**CSV 坏行跳过并计数，181327 能回放了**
- `guard.py` 修了 4 个 bug（都是收紧）：① Lock → RLock，**Ctrl-C / SIGTERM 约 1/12 概率死锁、DISABLE 发不出去**（改后 30 次 0 次卡死）② `arm()` 在 DISARMED / 急停时拒绝，只能走 `rearm()` ③ 断流立刻 T,0,0，不再走斜率限慢慢降 ④ 看门狗清零也写进录制
- `tests/test_fault.py` 13 条

**验收**
- 故障矩阵（11 种 × 3 次）：**10 种故障结束后 ≤0.09 s 就恢复**；断流 / 坏帧时 Guard 在 0.20 s 归零（固件模型 0.10 s）；恢复后力矩从 0 按斜率限爬；卡顿 1.2 s → 看门狗 DISARMED，设备复位 + 恢复逻辑都拉不回来，只能 rearm；急停同样
- ❌ **设备复位且静默超过 1 s：恢复最坏 2.02 s**（main.py 的 2 s 重试节拍），见给整合的留言，改一行后 ≤0.04 s
- 12 段录制全栈各 60 s（terrain、--force-deadman）：**0 异常退出、0 DISARM、0 Traceback**；loop_ms 中位 14，p95 15–17，最大 238（复现不出来，空循环探针最大 20 ms，判断是别的线抢 CPU）
- 游戏页开着（回放 8806）：中位 +0–2 ms ✅；**p95 +6–9 ms，超过 +5 ms 的线**；无头 swiftshader 1080p 30 s 最大 33 ms，真 GPU 浏览器面板 60 fps 最大 27–131 ms；shot.sh 连拍 3 张最大 78 ms；全程 0 DISARM
- pytest 43 过；系统 Python 3.9.6 跑 test_fault + test_guard 22 条过

**没做完 / 不理想**
- 全是模拟故障，真机没测过；每段录制只跑了开头 60 s（大多是坐着 / 站着）
- `--replay` 模式下 main.py 跳过恢复逻辑，所以全栈回放里掉使能以后一直不恢复（真机不受影响）。用 exec 打补丁验证过，去掉这个条件就能恢复
- A 的踏步模式没接进 main，没测
- ~~压测脚本在 scratchpad `F/`，没进仓库~~ → 已整理成 `scripts/stress.py`（见下「修复 F」）

**修复 F（压测脚本进仓库）**
- `scripts/stress.py`（纯标准库）：`stack` 起 `main --replay` 子进程（terrain、--force-deadman、随机空端口或 `--port`），10 Hz 读 /state，每段一行 JSON：DISARM 次数 / Traceback / 退出码 / loop_ms 中位·p95·最大 / 坏行 / 步数 / reason 分布；`--faults` 透传 SHELLOS_FAULTS；`--game` 同时开无头 Chrome 打开 /game 量 loop_ms；`matrix` 跑 11 种故障 × N 次（复用 test_fault 的 rig/drive，现在 main 的 `recover_tick` 已合并，不再需要旧的 exec 打补丁）。任何一段不过 → 退出码 1
- 明天真机后复跑：`.venv/bin/python scripts/stress.py stack --secs 60 --out /tmp/shellos-shots/F/stack.json`（缺省跑 data/recordings/ 全部，新录制自动算进去）；`scripts/stress.py matrix -n 3`；游戏屏：`scripts/stress.py stack --secs 40 --game data/recordings/0922-181327-qiuqiu.csv`
- 验收：冒烟 181327 + synthetic-walk 各 8 s 带 `gap@3:1,noten@5` → 2/2 过、0 DISARM、loop_ms p95 15；matrix -n 1 → 11/11 过（10 种 ≤0.033 s 恢复，卡顿 1.2 s 按预期 DISARMED 不自恢复）；`--game` 15 s：0 DISARM，但 **p95 148 / 最大 219 ms**（swiftshader 冷启动这 15 s 很吃 CPU，比之前 30 s 平均的数字难看，展位机别跑）；`tests/test_fault.py` 补 2 条（summarize 判 DISARM/Traceback/起不来；stack 端到端 2 s），pytest 91 过；系统 Python 3.9.6 能跑 `stress.py -h`（它没装 pytest/pygame，全栈要用 .venv）

### 整合（9/23 合并，端口 8810）
**做了**（改的 core 文件：`main.py`、`server.py`、`serial_link.py`、`recorder.py`、`agent/interpret.py`；跨线顺手修：`terrain.py`、`hud.js`、`tests/test_fault.py`、`tests/test_terrain.py`、`strength_ladder.py`、README / 展位脚本的「已知问题」）
- 🐞 **新发现并修了**：`Terrain.reset()` 把步数基准清成 0，而步态估计器还在累计（几百步）→ **走过以后从 /worlds 换世界、或从 puppet 切回 terrain，下一拍就把几百步算成好几圈**：0.0 s 登顶、best=0、一条假影子。改成复位后以步态当前计数为基准（`_strides=None`）。代价：冷启动第 1 步不计（`test_terrain` 的 force 测试起点 1→0）
- A 的留言：`--stepping` 接进入口（`App(stepping=)`，`__init__` 和 `demo_reset` 两处 GaitEstimator 都带上）；README 真机命令加一行
- E 的留言：① `demo_reset` 不再沿用 `old.params`，换人 / 演示复位强度回缺省，影子 / 最佳照旧搬运 ② `set_ctl`：切离 terrain 时暂存实例，切回沿用（影子、最佳、世界都在，回山脚）；`set_terrain` 同一个世界不再 `set_preset`（影子不清），换到别的世界照旧清影子 ③ `interpret.py`：同一参数只取第一条命中的规则、否定说法排前面、差值为 0 不存卡（`App.feedback` 也挡）；另加「轻一点 / 重一点」按要求理解（原来「轻一点」会 +0.5，「重一点」会 −0.5，展位上评委最可能说的就是这两句）
- F 的留言：① 恢复块抽成 `main.recover_tick()`，去掉 `not a.replay`，检测到复位立即重试；`guard.arm()` 返回 False 时日志写失败；`test_fault.drive()` 改成直接调它 ② `serial_link`：`_wlock` → RLock，`reboots` 只在 `enabled` 时 +1 ③ `recorder`：加锁，torque 行 `",,"*14` → `","*15`（现在 frame / torque 行都是 19 列）④ F→G：HUD 里 ACTIVE 但 `reason≠ok`（低置信 / 断流）显示黄灯「归零」，不再写「有力」
- B 的留言：`POST /memory/add {"delta":{...},"quote":...,"source":"ladder"}` → `App.add_exp()`（写卡 + 记为已套用，差值 0 不写）；`strength_ladder.py` 末尾改为写一张相对缺省 strength 的卡（换人后参数回缺省，走 6 步检索命中这张卡再套用）
- `tests/test_app.py` 5 条：换人参数回缺省且影子保留 / 切走再切回 terrain / 差值 0 不存卡 + 轻一点 / recover_tick 复位立即重试且 DISARMED 不动 / 走过后换世界不出假圈

**验收**
- pytest **48 passed**；改过的 9 个 .py 用系统 Python 3.9.6 `ast.parse(feature_version=(3,9))` 全过
- `--sim` 8810，130 步/分自动走、红灯站 2.5 s：**tokyo_night 3 圈**（每圈 21.2 s；3 次红灯都是走着 3 s pos 停在 4 不动 → 站定约 2.5 s 放行 4→7；第 2 圈起影子 ghost_pos 与 pos 同步 0→4→13→24→36）；**taishan_18pan 3 圈**（17.5 s/圈，影子 0→9→19→29→38）。loop_ms 中位 14 / p95 15–17 / 最大 21–22；全程 ACTIVE
- 修 bug 前第一次跑泰山：换世界那一拍直接 laps=3、用时 0.0——就是上面那个 bug，修后复跑正常
- 实跑 API：puppet → terrain 后 best/影子/泰山都在；「太陡了」1.5→1.0 → 换人 judgeB → 1.5；「不明显」+0.5；`/memory/add` 写卡 #6
- `/`、`/game`、`/game/engine.js`、`/worlds` 都 200；截图 `/tmp/shellos-shots/INT/game-live.png`（红灯段、走着不前进）、`game-ghost.png`（第 2 圈，影子「上一位：judgeB 并排」+ 命中经验卡 #5 滑入）、`dashboard-live.png`
- 13 个录制文件（12 段真机 + synthetic-walk）各回放 30 s（terrain、--force-deadman、4 个并行）：**全部活满 30 s、SIGTERM 退出码 0、0 Traceback、0 DISARMED**；181327 `bad=1` 正常跳过
- 故障注入回放（181327，`reboot@5:1.5,noten@12,gap@16:2`）：检测到复位立刻「重新 ENABLE 成功」，回放模式现在也会恢复
- 新录制格式核对：`--sim` 录 8 s，2206 frame + 1094 torque 行全部 19 列、0 粘行，ReplayLink 读回 bad=0（测试录制已删）
- `data/experiences.jsonl` 已还原（测试卡 #4–#6 没进提交）；8810 进程已 kill

**没做 / 不理想**
- 录制器 `close()` 后设备线程再写会抛异常（退出时一瞬间），没改：进程本来就在退出
- 截图只各拍了一张（shot.sh 吃 CPU，sim 下 loop_ms 在截图期间到 42 ms，没 DISARM）
- `interpret.py` 的规则表仍然很粗：「明显」单独出现算「太强」-0.5；大模型没配，全走规则
- D 的可选项（status 带 `wait_still_s`）没做：前端写死的 2 s 与 `WAIT_STILL_S` 一致，YAGNI

### 整合 ②（修复轮合并，端口 8810）
**做了**（跨线的留言统一在这里收口）
- 修复 B → 整合：`main.demo_reset` 加 `self.ctl.memory = old.memory`，换人后别的世界的影子 / 最佳也不丢；`tests/test_app.py` +1（去掉这行即失败，验过）
- 修复 B → G / D：`hud.js` 红灯读 `T.wait_need`（「站定 x / 1.5 s」、进度条按 1.5 s 走，文案「站稳就放行」）；`index.html` 删掉写死的 `WAIT_S=2`，改读 `t.wait_need`
- 修复 E → 整合：`worlds.html` 副标题改「坡和台阶会变成腿上的力」；架构 §0 去掉「第一个」；架构 §4 加第 11 条（开场前 `experiences.jsonl` 为空、配 `SHELLOS_LLM_*`，没配走规则表）
- 修复 core / B / G → E 的文档留言（E 这轮没接，整合代办）：README 键盘改成「全局热键默认关、`--hotkeys` 才开，R 不再上膛」，局域网设备只能看、POST 一律 403；README / 项目介绍安全表同步；展位脚本删「开场别换世界」换成「每座山各存各的影子」；红灯「两秒」全改「站稳一两秒 / 1.5 s（6 s 兜底）」；评委题库红灯数字换成 9/22 回算中位 2.2 s；60 秒脚本和题库 C3 的「力矩条变短」改成「力矩条下那行强度 1.5 → 1.0」

**验收**
- pytest **88 passed**；系统 Python 3.9.6 `ast.parse(feature_version=(3,9))` 全部改动 .py 通过，并直接 import `shellos.main / terrain / estimator / interpret / server` 成功
- `--sim` 8810，110 步/分、红灯走 2 s 再停：东京 **2 圈**（第 1 圈 21.8 s）；两次红灯都是走着 pos 停在 4，停下 **2.0 s** 放行 4→7；第 2 圈影子 ghost_pos 与 pos 同步 0→36；力矩峰值 stairs_up 1.8 / up 1.5 / stairs_down −1.5 / 红灯 −0.75，全程 ACTIVE，loop_ms 中位 15 / 最大 17
- 截图 `/tmp/shellos-shots/INT2/game-redlight-walking.png`：红灯走着 → 面板「停下！站稳就放行」、力矩条下「强度 1.5 Nm · 力矩条 = 近 1.5 s 峰值」
- 13 个录制（12 段真机 + synthetic-walk）各回放 20 s（terrain、--force-deadman、4 个并行）：**全部活满 20 s、SIGTERM 退出码 0、0 Traceback、0 DISARM**；181327 `bad=1` 照旧跳过
- `data/experiences.jsonl` 为空；8810 进程已 kill

**没做 / 不理想**
- 截图里影子标签（「上一位：anon 领先 30 步」）和红灯面板重叠——影子领先很多、站得远时标签落在画面中上方。归 G，放到游戏地图精加工阶段
- 回放前 20 s 大多是静止 / 桌面段，真正走路的片段（181327 中后段）没在这 20 s 里；A 的 30 s+ 全段回算见 A 节

### 修复 core（第 ② 轮审查 13 条，端口 8830–8831）
**做了**（改：`main.py`、`server.py`（非 /game）、`agent/interpret.py`、`memory/store.py`、`control/phase_profile.py`、`input/hotkeys.py`、`scripts/shot.sh`、`data/experiences.jsonl`；测试 `tests/test_app.py` +7、新 `tests/test_interpret.py` 23 条、`tests/test_gait.py` 两条改数）
- **phase 绕过置信度门**：主循环每拍抽成 `App.tick()`，按类判断 `isinstance(ctl, (PhaseProfile, Terrain))` 才带步态置信度。站着不动 → `low confidence`、0 Nm
- **经验卡叠加两次 / 删卡回不去**：`auto_recall` 排除已套用的卡、`applied += 新卡`。「已套用 / 检索过」改成挂在控制律实例上（`App.applied`、`App.recalled` 是属性）：开场 puppet 的检索不再占掉 terrain 的；切回缓存的 terrain 仍记得套过哪些；删卡时缓存 terrain 上的卡也能回退
- **puppet 下换人，切回 terrain 还是上一位强度**：`demo_reset` 把缓存的 terrain 参数也回缺省（影子 / 最佳 / 世界不动），applied / recalled 清零
- **步频 0 时写出 [0,999] 卡 + 多卡相加到 0**：`feedback` / `add_exp` 在步频还没估出来时不写卡、不改参（事件流提示「先走几步再说一次」）；`merged_delta` 同一参数改成**取平均**，合并结果不超过单张卡的幅度（A/B/C 三张「太陡了」→ D 命中后 1.0，不是 0）
- **phase_profile 相位约定反了**：`torque_at` 先按 `terrain.HS_PHASE`（运行时读）换算成文献相位；缺省 `t_ext 11 / t_flex 68`，和 terrain 同一口径
- **「早一点 / 晚一点」和手柄 ←→ 只动 t_push**：terrain 的时机规则和手柄 ←→ 同时挪 `t_push / t_step / t_brake`
- **规则表方向反 / 不认**：terrain 否定和「更/再/还」排最前；「太早 / 早了」= 往后挪 +5，「太晚 / 晚了」= −5，「早一点 / 晚一点」保持原方向（phase、dofc 也加了「太早 / 太晚」）。句子和期望方向都写在 `test_interpret.py`
- **局域网任何人能按死人开关**：`do_POST` 只认 127.0.0.1 / ::1，其余 403；GET 照旧对局域网开放
- **pynput 全局热键**：默认不起，`--hotkeys` 才打开；去掉全局 R = 重新上膛（游戏页 R 是回山脚，会顺手清急停）。空格 / Esc 在 `--hotkeys` 下保留
- **测试卡**：`data/experiences.jsonl` 清空（评委第一张卡是 #1）
- **shot.sh 窄屏**：W<500 时自动用 iframe 包一层、500 宽窗口拍、`sips` 从中间裁回 W；脚本头写明展位机 / 穿戴时别跑

**验收**
- pytest **78 passed**；改过的 .py 用系统 Python 3.9.6 `ast.parse(feature_version=(3,9))` 全过
- `--sim` 8830 走 HTTP：本机 POST 200；**局域网 IP（192.168.0.18）POST /hold → 403**，GET /state 200。评委-A 走 1 s（strides 0）说「太陡了」→ `card null`、强度 1.5 不变；走起来再说 → 卡 #1 区间 [100,120]、1.0；再走 6 s 仍 1.0（没叠）。`/ctl puppet` → `/wearer 评委-B` → `/ctl terrain`：强度 **1.5**、recalled False；走 1.5 s 检索命中 #1 → 1.0。切 phase、停走：30 次采样全部 `low confidence`、发出 0.00 / 0.00
- `shot.sh` 390×844：输出 390×844，排版宽度确实是 390（`/tmp/shellos-shots/core/dash-390x844.png`，右边「打开游戏屏」「重新上膛」完整）。第一次拍在进程刚起 3 s 时是全黑，重拍正常
- 测试卡已清，8830 / 8831 进程已 kill

**没做 / 不理想**
- 规则表仍是关键词匹配：「不累」会算成 −0.5、「没力了」（人累了）会算成 +0.5；大模型没配
- 删卡回退仍是「减差值」：参数被裁剪过（比如 t_step 6 −5 −5 → 0）再删卡回不到原值，今晚没改
- 每条卡只存触发时的步频区间，同一位评委自己说两次「太陡了」这次会 −1.0，下一次检索按平均只 −0.5
- README / 项目介绍里「键盘空格 = 死人开关 · Esc 急停 · R 重新上膛」「服务监听 0.0.0.0，游戏屏可放另一台设备」要改，见留言

### 修复 G（第 ② 轮审查 11 条游戏画面问题，端口 8832）
**做了**（只改 G 名下：`game.html`、`game/engine.js / hud.js / avatar.js / path.js / ghost.js / camera.js`、`themes/cyber_night.js / kit.js`；新测试 `tests/test_game_path.py`）
- **共驾开场不再是报错画面**：ctl=puppet 时游戏页照常起（默认东京），顶部「共驾：Ghost 操控 Shell · 手柄摇杆直接推两条腿 · 按住 R2 才有力」，隐藏路段/海拔/用时/影子/登顶/红灯；力矩条照常刷新，化身跟真实髋角动、原地不前进。切回 terrain 同一世界无刷新接着走（世界不同才 reload）。「切到地形」按钮和调试横幅只在其它控制律（transparent/constant/dofc/phase）出现
- **力矩条 = 近 1.5 s 峰值保持**（≥ 一个步态周期），数值也是峰值；条下多一行「强度 **1.5** Nm · 力矩条 = 近 1.5 s 峰值」（读 `/state.ctl.params.strength`；puppet 显示「摇杆推满 = 2.0 Nm」）
- **化身腿轴**：髋绕局部 Y（−flex）、膝绕局部 Y（+），屈髋是前抬不是侧劈，影子同一函数。浏览器量骨骼：flex 30 → 大腿 [0.170,−0.205,0.009]（纯前抬，和审查给的一致）
- **化身手臂**：`rest·Rx(±60)·Rz(摆)`。⚠ 审查给的 `rest·Rz(sw)·Rx(±60)` 我量过**几乎不摆**（前后分量只变 0.01），所以顺序反过来；右臂静止偏后，加 −15° 补对称。量值：站立 左 [−0.010,−0.241] 右 [−0.013,−0.241]；左腿前 40 → 左臂后 −0.071、右臂前 +0.047（对侧摆）
- **登顶 6 s 吞红灯**：登顶卡期间 pos ≥ 3 或 segment=wait 立即收起、化身回当前步；hud 红灯面板去掉 `!summit`，永远优先
- **台阶陷脚**：`heightAt` 只在台阶边缘 8%（`STAIR_EDGE`）升降，踏面上高度 = 方块顶面
- **影子落后看不到 / 贴脸大头**：右下统计面板固定一行「影子 落后 N 步 · 谁」；影子落后化身 1.5 步开始变淡、2.5 步全隐藏（按镜头距离分不开：台阶上并排的影子离镜头也才约 4），出画或隐藏时标签钉在画面下缘「↓ 上一位：谁 落后 N 步」；标签不再压到右下面板
- **东京台阶**：cyber_night 里踏面往白插 0.3 + 自发光 accent×0.08 + 每级台阶边缘一条黄色霓虹亮条（InstancedMesh，一次绘制）
- **山体穿路面**：kit.terrain 贴路一整格网格（ROAD_W/2 + size/seg）内压到路面下 0.08，再往外 0.3 才开始抬；路面材质加 polygonOffset
- **登顶卡挡化身**：卡片挪到 `top:15%`，登顶环绕镜头看点 +1.0 → +1.9，化身落在画面下半
- **起点帐篷**：东京隐藏；山地主题挪到 lateral −5、颜色取 theme.ground 提亮
- 顺手修：`ghost.onState(null)` 把 `group.visible` 设成 `null`，three.js 只认 `=== false`，puppet 画面里冒出一个 T 字影子——改成布尔；预览模式没传 `ghost=` 时不再显示影子标签

**验收**
- pytest **85 passed**（新增 `test_game_path.py`：node 跑 path.js，所有世界所有台阶步踏面上化身高度 = 方块顶面；把 STAIR_EDGE 改回 0.3 会失败，验过）
- 截图 `/tmp/shellos-shots/fixG/`：`taishan-20.png`（腿前后迈、手下垂）、`tokyo-stairs12.png`（天桥台阶每级亮边）、`tokyo-pos0.png`（无帐篷）、`tokyo-summit.png`（卡片在上、两个化身完整）、`ghost-behind1/3/6.png`、`ghost-ahead3.png`、`wutong-17-zoom.png`（平台转角无绿楔）、`fuji-14-zoom.png`、`live-puppet.png`（共驾画面）、`live-constant.png`（调试横幅保留）
- 8832 live（无头 Chrome + CDP 每 250 ms 采样，脚本在红灯站定）：puppet 5 s → 切 terrain，游戏页无刷新、puppet 画面消失；第 2 圈 t=50.1 登顶卡出现 → pos 3 时（51.9 s）收起、化身 30.4 → 3 → 52.1 s 红灯面板 block；力矩条在 stairs_up 保持 +1.8 Nm（= 1.2 × 1.5）、不再在 0 和峰值之间乱跳
- 侧视（`__camHold`）看了一步上台阶：前腿抬、膝弯，后腿伸；8832 已 kill

**没做 / 不理想**
- 富士 Z 字拐角还剩一小片暗色楔形（比原来小很多）：转角附近的地面顶点取到了相邻一段（更高）路的高度，差几厘米；要根治得让 `nearest()` 在转角取较低的那段，没做
- 手臂下垂 60° 和右臂 −15° 补偿是在 CesiumMan 上量出来的，只在这个模型成立
- 登顶提前收起用的是 pos ≥ 3：sim 110 步/分下登顶卡大约只显示 1.5–2 s（真机只接住约一半的步，会长一些）；嫌短就把 `SUMMIT_BREAK` 调大，红灯那条不受影响
- 无头截图（swiftshader）东京场景要等 15–20 s 才出第一帧，`shot.sh` 默认 6 s 会拍到「加载中」

### 收尾（9/23 夜，端口 8810）
**做了**
- 修复轮 ②（B 2AFC 0 力轮、A 踏步模式全栈复测、F `scripts/stress.py`）的文件一并提交；A ② 给整合的留言（架构 §4 第 5 项改写）已并进 §4 终版第 6 项
- 两个缺 `from __future__ import annotations` 的文件补上（`scripts/collect.py`、`shellos/device/convention.py`）
- 架构 v3：§2.1–2.6.1 把今晚验证过的写成「回放 / 模拟」并注明仍未真机验证的项；§4 改成终版 0–14 项表格，每项带命令和不过怎么办
- 新增 `docs/报告/今晚总结.md`

**验收**
- pytest **92 passed**；系统 Python 3.9.6 对全部 .py `ast.parse(feature_version=(3,9))` 0 失败
- `--sim` 8810，110 步/分、红灯走 2 s 再停：东京 2 圈（21.8 s/圈），两次红灯停步 2.0 s 放行，第 2 圈 ghost_pos 与 pos 同步 1→35；峰值 +1.8 / +1.5 / −1.5 / −0.75 Nm；全程 ACTIVE，loop_ms 中位 14 / 最大 23；4 个页面 200；0 Traceback
- `scripts/stress.py stack --secs 20`：13/13 通过（0 DISARM、0 Traceback、退出码 0；loop_ms p95 ≤18，最大 88 ms 在 181327、当时和模拟同跑）；`stress.py matrix -n 1`：11/11
- `data/experiences.jsonl` 0 字节；8810 和压测子进程都已退出

**没做 / 不理想**：见总结第 5 节。只复跑了 20 s 回放，没复跑 12 × 60 s

## 给别的线的留言

> 9/23 合并：A / B / D / E / F 写给整合的留言、F→G 的 HUD 那条都已处理，见上面「整合」节。修复轮的留言见「整合 ②」和「收尾」。**仍开着的只有一条：整合 ② → G「影子标签压红灯面板」，归地图精加工。**


- **A → 整合/总指挥（main.py）**：踏步模式还没接到入口。建议 `main.py` 加 `--stepping` 参数，`App.__init__` 和 `demo_reset` 里 `GaitEstimator(stepping=args.stepping)`（两处都 new 了 GaitEstimator）。不接的话，展位原地踏步 ±6° 左右会出现「腿上一直有脉冲、化身不前进」（报告 §5.1）
- **A → B（terrain.py）**：① HS_PHASE 证据不够，别改 0.5；明天用 `gait_audit.py --hs --rec <新录制>`。② 地形前进用的是被接受的周期，默认档下真人约 44% 的步被接受，路线步数 / 游戏节奏按「实际步数的一半」估。③ `gait.moving` 在站着晃时也会成立（非走路段 250 s）；如果想只在真走时给脉冲，可以 `GaitEstimator(recent_s=1.5)`，代价是起步多等 2 s、走路时脉冲时间减半——我不建议今晚开
- **A → F / Guard**：Guard 没动。但踏步模式把置信度参考半径 8 → 4，效果上 conf<0.5 归零门更容易过（非走路误判走动中 162 → 295 s）。压测时如果开踏步模式请一起测
- **A → G（游戏）**：模拟模式下每一步都会被接受；真机上真人的步只接住约 44%，`pos` 会一顿一顿地前进。化身插值别假设固定节奏（比如「每 0.5 s 必来一步」），停顿时原地踏步动画就好

- **G → F（压测）**：游戏开着时 loop_ms 我的数字见 G 节；另外 `scripts/shot.sh` 无头 swiftshader 截图会把主循环卡到 1.5 s（看门狗 DISARMED），压测时别把截图和 loop_ms 测量混在一起跑；展位机上别跑截图
- **G → B（terrain.py）**：游戏读 `status()` 的这些字段：`preset, world, pos, total, laps, segment, label, next{label,kind,in}, wait_still, altitude, force, segments, profile[].label, elapsed, best, last_lap, ghost_pos, ghost_who`，改名请留言
- **G → 总指挥**：`server.py` 我只改了一行（静态前缀加 `/game/`）。测经验卡滑入时我往 `data/experiences.jsonl` 加过一张测试卡 #4，已用 `git checkout` 还原——各线跑 /feedback 都会写这个文件，合并前看一眼
- **G → 下一轮地图精加工**：只改 `static/game/themes/<style>.js`；接口和可用工具见 `themes/kit.js` 文件头；看效果用 `/game?preview=<world>&pos=<n>&ghost=<n>`（`&summit=1` 看山顶）

- **B → F（replay.py）**：`data/recordings/0922-181327-qiuqiu.csv` 有 3 行两帧粘连（如 `-0.30frame,...`），`ReplayLink.__init__` 里 `float()` 抛 ValueError，`--replay` 这一段整个进程起不来——而它是唯一像样的走动段。建议解析时坏行跳过并计数（`n_bad`）
- **B → 整合（server.py / memory）**：强度阶梯想把"评委选的档"写成真正的经验卡，需要一个直接写卡的接口，例如 `POST /memory/add {"delta":{"strength":x},"quote":"强度阶梯选 1.5 Nm"}` → `store.add(wearer, ctl_key, {"cadence":…}, delta, quote, 1.0, "ladder")`。有了我把 `strength_ladder.py` 末尾换成它
- **B → G（游戏）**：`status()` 字段没变。红灯时 `wait_still` 是站定秒数（满 2.0 放行，放行时 pos 一次跳过整段红灯 3 步）；`force` 非空时是强度阶梯 / 2AFC 在强制路段，画面可以提示"训练中"。腿上能分出来的只有"台阶为主"和"坡为主"两类，泰山和梧桐手感几乎一样
- **B → A**：红灯放行现在要求 `not moving` 且这一拍没接受新周期；站着晃 `moving` 为真会一直不放行（回放 175524 里 40 s 0 步但 moving 多数为真）

- **D → 整合**：`server.py` 不用改，仪表盘要的字段 /state 里都有（sim、terrain.world/label/next/altitude/ghost_pos/ghost_who、safety.sent）。注意 `server.py` 启动时就把 index.html 读进内存，改完页面要重启进程才看得到。可选：terrain.status() 带上 `wait_still_s`（现在前端写死 2 s）
- **D → B（terrain.py）**：仪表盘也读 G 列的那些 status 字段，外加 `profile[].h / kind / label`、`force`、`wait_still`，改名请留言

- **E → 整合（main.py）⚠ 影响展位闭环叙事**：`demo_reset()` 里 `self.ctl.params = old.params` 让 terrain 换人 / 演示复位后**强度不回缺省**，而仪表盘 toast 和事件流都说「参数回缺省」。8805 实测：评委 A 说「太陡了」1.5→1.0 → 换人 → 走 6 步命中这张卡 → **0.5**（叠了两次）；删卡后回到 1.0 而不是 1.5。建议：换人时参数回缺省（B 的强度阶梯要"保留选档"，可以改成把选的档写进缺省值或写成经验卡，而不是整体沿用 params）
- **E → 整合（main.py set_ctl / set_terrain）⚠**：从 puppet（或任何非 terrain）切回 terrain 时 `CTLS[name]()` / `Terrain(preset)` 新建实例，**影子、最佳用时、世界都丢**。展位脚本 0:00 共驾开场 → 0:40 切回 terrain 正好踩到。建议每种控制律的实例缓存起来复用，或切回 terrain 时沿用 demo_reset 里那段影子搬运
- **E → 整合（agent/interpret.py 规则表）**：「不明显」同时命中 `明显`（−0.5）和 `不明显`（+0.5），差值 0，还存了一张空卡（8805 实测卡 #5 `{'strength': 0.0}`）。「再来重一点」同理。建议否定词规则先匹配、命中后跳过同参数的其它规则，并且差值为 0 不存卡
- **E → 整合**：puppet「单段 ≤10 s」代码里没有，只是操作规程；文档里已照实写成「操作规程」。要强制的话在 `Puppet.step` 里加计时

- **F → 整合（main.py 恢复块）⚠**：① 去掉 `not a.replay and`（回放现在有 `needs_recovery/recover`，不去掉的话故障注入测不到恢复）② 设备复位后立刻重新 ENABLE：条件改成 `(now - last_recover > 2.0 or link.reboots != seen_reboots) and ...`，复位且静默 >1 s 时最坏恢复时间 2.02 s → 0.04 s ③ 最好把恢复块抽成函数，`tests/test_fault.py` 的 `drive()` 现在是照抄的，抽出来以后直接调。④ `guard.arm()` 现在有返回值，DISARMED 时返回 False，不会再把 DISARMED 改回来；「重新 ENABLE 成功」的日志可以看一下这个返回值
- **F → 整合（serial_link.py）**：① `reboots` 每行开机日志都 +1（`[initImpl]` 和 `Total PSRAM` 各算一次），一次复位会数成 ≥2 次；建议只在 `self.enabled` 为真时 +1（replay 就是这么做的）② `_wlock` 改成 `threading.RLock()`：信号处理函数里的 `disable()` 可能打断正在 `send()` 的主线程，和 Guard 那个死锁是同一类
- **F → 整合（recorder.py）⚠ 录制会坏**：`frame()`（设备线程）和 `torque()`（控制线程 + 看门狗线程）写同一个文件句柄却没有加锁 → 181327 的粘行就是这么来的，明天的真机录制也会有。加一把 `threading.Lock` 包住两个 write。另外 torque 行 `",," * 14` 多了 13 列，应该是 `"," * 15`，tl/tr 现在落在错误的列（目前没有人读）
- **F → G**：低置信归零时 HUD 显示「ACTIVE · 有力 low confidence」，其实腿上是 0 Nm。可以在 `reason != ok` 的时候不写「有力」

- **修复 G → E（docs/提交/评委题库.md C3、60 秒脚本）**：力矩条现在是**近 1.5 s 峰值保持**，条下一行直接写「强度 1.5 Nm」。C3 ①② 和 60 秒脚本里「力矩条变短 / 回到原来长度」请改成「看力矩条下面那行：强度 1.5 → 1.0（峰值条跟着从 1.8 变 1.2）」；仪表盘上的参数值同理。另外开场 puppet 时游戏屏现在是「共驾：Ghost 操控 Shell」画面，脚本 0:00–0:40 可以直接让评委看大屏
- **修复 core → E（README、docs/提交/）**：① 键盘热键现在默认关（`--hotkeys` 才开），且没有 R = 重新上膛了；README 第 63 行、第 108 行，项目介绍 §5 安全表第 134、138 行里的「键盘空格 / Esc / R」要改成「手柄 R2 / 本机网页按钮；键盘默认关」② README 第 64 行：局域网设备只能**看**游戏屏（GET），POST 一律 403——在另一台设备上按 R 回山脚、选世界、`/sim` 都不生效，要在 MacBook 上点 ③ 评委题库 B4 补一句「死人开关只认手柄和本机网页，局域网来的控制请求一律拒绝」④ 展位脚本把「换人」挪到 0:40 评委穿好之后再点；操作员照评委意思敲标准说法（「太陡了」「没感觉」「早一点」），不逐字抄；评委刚穿上还没走几步就说话，事件流会提示「先走几步再说一次」⑤ 9/24 上午清单加一条：上真机前 `data/experiences.jsonl` 应为空（或放一张队友真机反馈的种子卡）
- **修复 core → A（tests/test_gait.py）**：phase_profile 改了相位约定（查表前减 HS_PHASE，缺省 t_ext 11 / t_flex 68），我改了你文件里 `test_phase_profile_peaks_where_told`、`test_gamepad_nudge_and_cycle`（t_ext 25→20 改成 11→6）、`test_memory_loop` 里的 t_ext 数字，逻辑没动
- **修复 core → G / D**：`App.applied`、`App.recalled` 变成属性（跟着当前控制律实例），/state 里 `memory.applied / recalled` 字段不变。游戏页 input.js 的 R（/demo/reset）、`/sim` 只在 MacBook 本机打开的页面有效

- **修复 B → 整合（main.py demo_reset）**：换世界现在保留每个世界的影子 / 最佳（存在 `Terrain.memory`）。但 `demo_reset` 新建实例只搬了当前世界的 ghost/best，别的世界的记忆会丢。在 `self.ctl.set_preset(old.preset)` **之前**加一行 `self.ctl.memory = old.memory` 就行（set_preset 只存非空记忆，不会用新实例的空状态盖掉）
- **修复 B → G / D（红灯文案）**：放行 = 两腿静下来 1.5 s（`status().wait_need`），从停步算起真机回放中位约 2.2 s、最长 4.7 s，兜底 6 s。hud.js 第 44 行的 `/ 2.0 s` 和 index.html 写死的 `WAIT_S` 请改读 `T.wait_need`，否则进度条走到 1.5/2.0 就跳；「站定 2 秒放行」这句对评委体感基本成立，可以留
- **修复 B → E / 总指挥（展位脚本 + 架构 §2.6.1）**：① 红灯台词改成「站定一两秒就放行」或「站稳了就放行」，真机回放最长 4.7 s，别承诺「两秒」② 换世界现在不丢影子了：展位脚本里「开场别换世界」可以删，和架构 §2.6.1「开场东京 → 评委自己挑一座山」统一；但评委挑的山如果没人爬过，那座山本来就没有影子，台词说「你是第一个爬这座山的，下一位会看到你」

- **修复 E → 整合 / 总指挥**：① `shellos/ui/static/worlds.html` 第 31 行「你在原地迈步，腿会感到坡和台阶」是评委第一屏，真机没验过手感，建议改成「你在原地迈步，坡和台阶会变成腿上的力」② 架构 v3 §0 产品一句话去掉「第一个」，改成「用量产消费级髋外骨骼做可走动的地形力反馈登山」（和项目介绍、「不说首个」口径一致）③ 架构 §4 9/24 上午清单加一条「配好 `SHELLOS_LLM_BASE / KEY / MODEL`，展位网络不稳时自动退回规则表（卡片 source=rule）」

- **修复 A → B / D / 整合（estimator 语义变化）**：① `gait.moving` 现在在「某腿相位 0.3 s 不推进」或「两腿同相」时为假（不重置保持计时），审查里「Terrain.step 里相位 0.25 s 推进 <0.03 就给 0」的兜底**不用再加**；新字段 `LegState.stalled` 可以上仪表盘调试 ② `symmetry` 改成活动度比 rom_l/rom_r（1 = 对称，跛行 ±20/±9 ≈ 2.2），index.html「左/右 对称比」标签照用；main 状态行和 `App.profile()` 拿到的也是它 ③ `cadence` 改最近周期中位数

- **修复 A ② → 整合（架构 v3 §4 第 5 项）**：踏步模式复测过了（报告 §5.2），安全项和默认一样。建议第 5 项改成「**原地踏步**：默认档让评委踏步，看 `pos` 涨不涨；腿上有脉冲、化身不走 → 重启加 `--stepping`（死人开关照旧）；还断断续续 → 请评委抬高腿（约 ±8°），再不行改慢走」

## 总指挥
- 已就绪：模拟器 `--sim` + `/sim`；地形每圈计时与影子（`terrain.status()` 的 `segments / elapsed / best / ghost_pos / ghost_who`）；`/game` 路由已占位（页面由 G 线写）
- 真机已确认 / 待验证事项见架构 v3 第 2.1、2.3、2.8 节

