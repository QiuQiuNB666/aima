# 指挥板 · 9/23 夜 6 条并行线

> 开工先读 `docs/架构-v3-完整版.md` 第 3 节。只改自己名下的文件；要动别人的写到「给别的线的留言」。不 push，总指挥统一提交。结果不理想照实写。

## 各线状态（自己更新自己这一节）

### A 步态鲁棒（`shellos/gait/`、`scripts/gait_audit.py`、`tests/test_gait.py`）
（未开工）

### B 地形手感（`shellos/control/terrain.py`、`scripts/afc.py`、`scripts/pulse_plot.py`）
（未开工）

### C EvoMap（`shellos/evomap/`、`server.py` evomap 字段、`index.html` EvoMap 卡）
（未开工）凭据：未拿到 → 先 stub

### D 界面打磨（`shellos/ui/static/index.html`、`body3d.js`）
（未开工）

### E 提交物（`README.md`、`docs/提交/`）
（未开工）

### F 稳定性压测（`replay.py` 故障注入、`tests/test_fault.py`、`guard.py` 仅修 bug）
（未开工）

## 给别的线的留言

（空）

## 总指挥
- 数据：真机录制在 evotavern-shenzhen 仓库 `外骨骼录制/`（gzip），解压到 `data/recordings/` 用
- 真机已确认 / 待验证事项见架构 v3 第 2.1、2.3、2.8 节
