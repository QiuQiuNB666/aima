# anni 的任务 · 9/23 下午 → 9/24 上午

背景先看 `docs/队友跟进.md`（5 分钟）。有问题在 `docs/指挥板.md`「留言」里写，或直接找球球。

## 一、你的作品（署名写进提交材料和游戏里）

| # | 作品 | 做什么 | 怎么做（不用写代码） | 在哪看到效果 |
|---|---|---|---|---|
| A | **你设计的一张地图** | 选一条你熟悉或想去的路线（比如你老家的一座山、上班路上的天桥、深圳某条步道），决定每一段是平地 / 上坡 / 台阶 / 下坡 / 红灯、各几步、地名叫什么、登顶说一句什么话 | 复制 `shellos/worlds/wutong_haohan.json` 改一份：`route` 里每段写 `kind`（flat/up/stairs_up/down/stairs_down/wait）、`steps`、`label`；`theme.style` 从 5 种画面风格里选一个。30–60 步最好玩。丢进 `shellos/worlds/` 就能玩，**选山页会多一张卡，署你的名字** | `/worlds` 选你的山 → 穿上外骨骼走一遍，腿上的力就是你设计的坡 |
| B | **峰哥的台词** | 峰哥在登顶 / 红灯 / 造山时会说一句话（AI 现写，断网时用兜底语录）。兜底语录现在只有 11 句，你来写 20 句更好笑、更像峰哥的 | 看 `shellos/agent/fengge.py` 里的 `CANNED`，照格式写（「这是个好事儿啊……」那种辩证反转，每句 ≤ 30 字，别碰两性和政治）。写好发给 AI，它会用峰哥的声音合成进去 | 游戏里的峰哥气泡 + 峰哥的声音 |
| C | **追兵「疾风」的人设** | 游戏里有个风系追兵（致敬捷风），你走慢了她冲上来。给她起名字（「疾风」是占位）、写 10 句追人时喊的话（每句 2–6 个字）、决定她追上你时发生什么 | 写在指挥板留言里给 J 线（捷风那个对话框） | 游戏里的追兵 |
| D | **现场主持 + 实验负责人** | 9/24 展位上你负责开场 30 秒介绍 + 带评委穿戴 + 主持下面的蒙眼实验。材料里写你是「感知实验负责人」 | 拿 `docs/提交/60秒演示脚本.md` 练两遍 | 展位 |

## 二、需要人在现场的活（AI 替不了）
| # | 活 | 为什么重要 | 大概多久 | 做完交什么 |
|---|---|---|---|---|
| 1 | ✅ **9/23 已完成（峰哥本人当面同意）**，只差补一段短视频 / 录音存档。~~当面问峰哥三件事~~：能不能在展位用他的**照片（肖像）**、**说话口吻**、**AI 复刻的声音**（给他听 `data/voice/` 里的一句）。三样分开问，拿到文字或录音确认 | 不同意 → 海报和游戏都要切到备用版（`?fengge=0&voice=0`），越早知道越好 | 15 分钟 | 在指挥板写「峰哥：肖像 ✔/✘ 口吻 ✔/✘ 声音 ✔/✘」+ 截图或录音存到私有仓库 `~/黑客松-EvoTavern/` |
| 2 | **蒙眼二选一测试（2AFC）**：你当操作员，找 1 位没玩过的人蒙眼穿外骨骼，原地踏步，每轮问「上坡还是下坡」，10 轮 | 只有 **10 轮 ≥ 9 对**，我们才能对评委说「人分得清虚拟上坡和下坡」；这是最硬的一条证据 | 20 分钟 | 命令：`.venv/bin/python scripts/afc.py --trials 10 --seconds 8`（球球或 AI 帮你在 MacBook 上开好）；把最后一行「正确率 x/10」贴到指挥板 |
| 3 | **找 3 个人各试一次**（身高 / 步幅尽量不同），每人穿上后说一句真心话评价手感，记下原话 + 穿脱用时 | 验证步态识别对不同人都灵；原话就是展位现场「评委一句话改手感」的彩排；穿脱时间要写进材料 | 30 分钟 | 表格：姓名缩写 / 身高 / 穿戴秒数 / 原话 / 有没有感觉到上坡台阶 |
| 4 | **按分镜拍 60 秒视频素材**：`docs/提交/video_storyboard.md` 里标了「补拍」的镜头（真人穿外骨骼踏步、大屏游戏画面、评委说话改参数的瞬间） | 提交物要 60 秒视频；这些镜头只能真人拍 | 40 分钟 | 原始视频放共享盘，文件名写镜头号 |
| 5 | **打印**：`docs/提交/poster.html`（A1 海报）、`docs/提交/onepager.html`（A4 一页纸，双面）。浏览器打开 → 打印 → 存 PDF → 找打印店。**第 1 项峰哥没同意前，只打印备用版**（页面右上角「切换」或地址加 `?fengge=0`） | 展位要挂海报、给评委带走一页纸 | 30 分钟 + 打印店 | 海报 1 张、一页纸 30 份 |
| 6 | **展位踩点**：量展位尺寸，看电源插座、网络（有没有 Wi-Fi、能不能连外网）、大屏接口（HDMI / USB-C） | 大屏要接 MacBook；没外网时 AI 功能自动走兜底，要提前知道 | 15 分钟 | 拍几张照片 + 写 3 行：尺寸 / 插座 / 网络 / 屏幕接口 |
| 7 | **评委题库彩排**：拿 `docs/提交/评委题库.md`，你当评委随机问球球 10 题，掐时间 | 每位评委只有约 7 分钟 | 20 分钟 | 答不顺的题号写到指挥板，AI 帮改答案 |

**安全铁律**（第 2、3 项）：穿戴者按住手柄 R2 才出力，松手就停；× 键急停。第一次穿的人从默认力度开始，别一上来就调到最大。
