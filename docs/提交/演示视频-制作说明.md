# 演示视频 · 制作说明（V3 视频线，9/24 上午）

> 方案依据：`scratchpad/demo_video_plan.md`（调研 + 分镜）、[60秒演示脚本.md](60秒演示脚本.md)（口径）、[video_storyboard.md](video_storyboard.md)。官方对视频**没有要求**，视频是给评委走到展位前看的，自定 60 s 正片 + 90 s 加长，1080p30 H.264，≤ 50 MB。
> 成片在 `docs/提交/视频素材/v{1,2,3}_60s.mp4`、`v3_90s.mp4`（进 git）和 `~/黑客松-EvoTavern/视频素材/`（同一份）。**实拍位仍是占位卡；anni 拍完 B01–B06 后按 §5 一条命令换。**
>
> **口径（9/24 球球）**：现在的 VR / 体感游戏只有视觉反馈，几乎没人做物理反馈；我们做登山走路的物理反馈——屏幕上的坡和台阶，会真的变成你腿上的阻力和助力。峰哥口吻：「VR 只骗眼睛，这玩意儿骗腿，这是个好事儿啊。」前 10 s（S01 旁白 00 + R03 旁白 02b）和结尾（T99 旁白 11b + 副题「骗腿，不骗眼睛」）都说到；真人腿部特写（实拍位）+ 腿上的力波形 HUD（R05b 特写）+ 上坡 / 台阶力脉冲镜头多给。

## 0. V3 三轮做了什么

| 轮 | 成片 | 变化 |
|---|---|---|
| 1 | v1_60s | 全部游戏画面用最新 main 重渲（无头 Chrome 连拍 `frames/lap` 一整圈珠峰：峰哥新身体 + 羽绒服、维塔跟跑、直升机降落、牦牛、帐篷、排队者、横梯 / 刀脊 / 横切、风雪云海；`frames/tokyo`、`frames/parkour`、`frames/dash`、`frames/forge`）；旁白全程峰哥克隆声；游戏内峰哥台词（导游开场 / 地标 / 登顶）按气泡出现的毫秒做同期声；CC BY 配乐，旁白处 sidechain 压 ≈ −12 dB |
| 2 | v2_60s | 分镜掐点（前 5 秒先给结果）、字幕 PNG 重做（深底 + 霓虹紫强调，不用纯白）、每段 0.3 s 淡入淡出、片头片尾（片名 / 一句话 / PRX / 微信群码） |
| 3 | v3_60s + v3_90s | 各段亮度对齐、旁白与画面事件对拍（「亡命小飞机」气泡出现时直升机正落地）、登顶后 2 s 留白落章、90 s 加长版 |

## 1. 一句话流程

```
~/黑客松-EvoTavern/视频素材/frames/<镜头>/（无头 Chrome 连拍：jpg + times.txt + events.jsonl）─┐
~/黑客松-EvoTavern/视频素材/raw/B0x.mov（实拍，可选）                                        ├─ scripts/shot_windows.py → build/shots.env
~/黑客松-EvoTavern/视频素材/vo/*.wav、vo/game/*.wav（峰哥克隆声：旁白 + 游戏内台词同期声）     ├─ V=3 scripts/cut_demo.sh ─► v3_60s.mp4 / v3_90s.mp4
~/黑客松-EvoTavern/视频素材/music/*.mp3（CC BY 配乐）                                        ─┘       └─ 片段/字幕版、片段/干净版（交剪映）
```

`scripts/cut_demo.sh` 只依赖 ffmpeg + 系统 python3（PIL 出字幕 PNG，字体 PingFang SC；这台机器的 ffmpeg 8.1 没编 drawtext，字幕 / 水印 / 角标全是透明 PNG overlay）。每个镜头的画面来源按优先级：`raw/<镜号>.*` 实拍 → `clip|<frames>|起|止[|倍速[|裁切]]` 连拍时间窗 → 截图 Ken Burns → 占位卡。连拍只有几 fps，`minterpolate`（运动补偿）补到 30 fps；`MI=blend` 快但糊。

同期声怎么对上：`scratchpad/rec2.mjs` 连拍时每帧顺手记 `#fg` 气泡文字（events.jsonl），`build/clip.py` 在时间窗里看到气泡文字变化，就向 `brain/tts.py`（127.0.0.1:8791，已缓存的直接给）要同一句的峰哥 wav，按 `(气泡毫秒 − 窗起点) / 倍速` 摆进这一段；`scripts/shot_windows.py` 再用这些事件反推每镜的起点（直升机 = 「小飞机」气泡前 4.6 s，让旁白 02b 先说完；吸氧 = 第一个 wait 路段前 3.5 s；登顶 = body.summit 前 2 s）。

配乐：`music/Our Story Begins.mp3`（Kevin MacLeod，CC BY 4.0，见 [素材授权.md](素材授权.md)），−5 dB 垫底，人声总线做 sidechain（`sidechaincompress` threshold 0.05 / ratio 3 ≈ 旁白时压 12 dB），整体 loudnorm −16 LUFS。

## 2. 素材清单

见 [视频素材/README.md](视频素材/README.md)（旁白 23 句、每镜静帧来源、抓帧 URL）。

今晚的状况：机器 load > 700（别的会话开着 20 多个无头 Chrome），`/game` 在无头 Chrome 里 `Page.enable` 都不回，1080p 逐帧抓不动；旁白（MiniMax 峰哥复刻音色，23 句全部成功、没有女声兜底）和 ffmpeg 粗剪不受影响。**画面全部是静帧 Ken Burns**，动画版等机器空了再渲（§6）。

旁白怎么补 / 改（tts 服务 `brain/tts.py` 在 127.0.0.1:8791 跑着，`brain/.env` 里有 MINIMAX_API_KEY；一句 4–10 s）：

```bash
M=~/黑客松-EvoTavern/视频素材
curl -s --noproxy '*' -D - -X POST 127.0.0.1:8791/tts -H 'Content-Type: application/json' \
  -d '{"text":"在家爬珠峰？这是个好事儿啊。"}' -o $M/vo/01.wav | grep -i no-store && echo "女声兜底，别用" && rm $M/vo/01.wav
```

辅助脚本都在这次会话的 scratchpad（`/private/tmp/claude-502/-Users-qiu----/c4d962c4-a058-4292-a7ea-7fb7af599db1/scratchpad/`）：`gen_vo.sh` + `vo_lines.txt`（批量旁白，拒收女声兜底）、`rec.mjs`（1080p 连拍）、`lap.mjs`（让 sim 自己爬一圈）、`with_chrome.sh`（全机 Chrome 信号量）。

## 3. 分镜表（粗剪实际时间线）

### 60 s（实际 61 s）

| # | 镜号 | 秒 | 画面 | 字幕 | 旁白（vo/） |
|---|---|---|---|---|---|
| 0 | T00 | 0:00–0:02 | 片头：峰哥亡命天涯 · 原地迈步，腿上爬珠峰 · PRX | — | — |
| 1 | S01 | 0:02–0:07 | 左：**B01 真人踏步（占位）**｜右：大本营经幡 | 原地迈步，腿上爬珠峰 / 消费级髋外骨骼 · 可走动 | 01 在家爬珠峰？这是个好事儿啊。 |
| 2 | R03 | 0:07–0:13 | 大本营，直升机掠过落停机坪 | 珠峰北坡 · 大本营 5200 m / ShellOS 控制 Hypershell | 02 上坡有人推，台阶腿发沉。 |
| 3 | R05 | 0:13–0:19 | 上台阶（中国梯）→ 北坳吸氧红灯 | 上台阶：支撑期阻力脉冲 · 软限 4 Nm / 北坳吸氧：站定才放行 | 03 不是振动，是按步打的力。／g_oxy 吸氧不丢人… |
| 4 | R06 | 0:19–0:21 | 脉冲插图 | 髋相位力矩脉冲 · 摆动期为 0 · 软限 4 Nm | — |
| 5 | B02 | 0:21–0:25 | **占位：评委说「太陡了」→ 摇到 MacBook** | 评委说：「太陡了」 | 05 太陡？说一句就行。 |
| 6 | R07 | 0:25–0:32 | 仪表盘 AI 决策卡 → 游戏屏决策卡 | 教练 → 记忆员 → 安全员 → 生效 / 强度 3.0 → 2.5 · 删卡 = 回退 | 06a 教练想，记忆员查，安全员管。／06b 删了这张卡，立刻回原样。 |
| 7 | R08 | 0:32–0:42 | 选山页输入 → AI 正在造山 → 剖面 → 进新山 | 一句话 → 大模型出路线 → 安全员裁剪 / 台阶不过半 · 红灯 ≤ 2 · 起步平地 | 07a 一句话，现场造一座山。／07b 大模型出路线，安全员裁。／07c AI 越不过这一行代码。 |
| 8 | R09 | 0:42–0:46.5 | 新山北山脊（成片 ×4） | ×4 · 旁边的影子是上一位 | 08 旁边那影子，是上一位。 |
| 8b | B03 | 0:46.5–0:48 | **占位：峰哥本人在展位** | 峰哥本人 · 肖像与音色已授权 | — |
| 9 | R10 | 0:48–0:54 | 登顶正面 + 结算卡 → 顶峰 | 登顶 8848.86 m · 你是下一位的影子 | g_summit 旗靠风吹开，恰恰相反，人得自己走上来。 |
| 10 | B04 | 0:54–0:57 | **占位：松开扳机 → 灯灭** | 松手 = 零力 · 死人开关 · 看门狗 | 10 松手，立刻没力。 |
| 11 | T99 | 0:57–1:01 | 片尾：片名 · AI 出主意，硬代码说了算 · PRX · 仓库 · 授权行 · 微信群码 | — | 11 AI 出主意，硬代码说了算。 |

### 90 s（实际 89.5 s）：在 60 s 上插 / 换以下段，顺序不变

| 镜号 | 秒 | 画面 | 字幕 | 旁白 |
|---|---|---|---|---|
| B05 | 6 | **占位：共驾，一人推摇杆、队友腿被推动**（S01 之后） | 共驾：摇杆 → 队友的腿 · 满杆 2 Nm | 1b1 先让人推着你走一段。／1b2 他随时能抗回来，这是共驾。 |
| B06 | 4 | **占位：anni 30 s 穿戴，加速** | 穿戴 ≈ 30 s（实测回填） | 1c 三十秒穿上，这是个好事儿啊。 |
| R04 | 4 | 牦牛让路 + 经幡（R03 之后） | 冰川上坡 · 牦牛让路 · 经幡猛风 | g_yak 牦牛给我让路？恰恰相反。 |
| R07L | 9 | 换 R07：多一段「恢复」卡 | …· 删卡 = 回退 · 恢复 | 06a／06c 删了立刻回，恢复留给下一位。 |
| R13 | 4 | 蜂群时间线（R07L 之后） | 蜂群：教练 / 记忆员 / 安全员 / 地形导演 | 06d 四个角色商量，安全员一票否决。 |
| R08L | 13 | 换 R08：每张多留 0.75 s | …· 裁完马上能爬 | 07a／07b／07c／07d 裁完，马上能爬。 |
| R09L | 5.5 | 换 R09 | 同 R09 | 08／08b 输给影子？恰恰相反，是好事儿。 |
| X01 | 4 | 跑酷起跳 + 东京攻壳彩蛋（R10 之后） | 跑酷 · 东京彩蛋 · 六座山 | — |

方案里的 9b「证据卡（2AFC ≥ 9/10）」粗剪没放；明早 anni 跑完 `scripts/afc.py` 够 9/10 再加（旁白 09b 已合成）。

声音：旁白按上表偏移摆位（`build/meta/<镜号>.vo`），垫底是 ffmpeg 合成的褐噪风声（`anoisesrc` 低通，−20 dB），整体 loudnorm −16 LUFS；**没有任何版权音乐**，配乐留给剪映曲库。

## 4. 明早实拍清单（手机横屏 1080p / 30 fps，每条拍 2 遍，先静止 3 s 再动作，文件名 = 镜号）

| 镜号 | 画面 | 谁拍 / 谁出镜 | 多长 | 用在 |
|---|---|---|---|---|
| **B01** | 真人穿外骨骼原地踏步，腰到脚特写，能看到外骨骼，背景大屏珠峰；手握手柄按住 R2 | anni 拍，队友穿 | 8 s ×2 | S01 左半屏（60/90 开头钩子） |
| **B02** | 评委 / 队友对操作员说「太陡了」（2 s）→ 摇到 MacBook 屏（2 s） | anni 拍，队友当评委 | 6 s ×2 | 60/90 |
| **B03** | 峰哥本人在展位看大屏上的自己笑一下 / 一句原声；不愿出镜就拍背影 + 大屏 | anni 拍，峰哥 | 5 s ×2 | 登顶前 1.5 s 插入 |
| **B04** | 手指松开扳机特写 → 仪表盘 / 大屏安全灯灭（再补一条 MacBook 顶栏 ACTIVE→ARMED） | anni 拍，队友 | 4 s ×2 | 结尾「关掉那一下」 |
| B05（90 s） | 共驾：一人推摇杆，另一人穿外骨骼腿被推动，手柄和腿同框 | anni 拍，两位队友 | 8 s ×2 | 90 s 开场 |
| B06（90 s） | anni 帮评委穿戴，掐表 | 队友拍 anni | 30 s ×1（脚本不加速，剪映里加速到 4 s） | 90 s「上手」段 |

铁律：横屏、别逆光、手机放腰高侧 45°；被推的只能是队友；峰哥镜头只在他当面同意后拍。拍完 AirDrop 到球球 MacBook：`~/黑客松-EvoTavern/视频素材/raw/B01.mov …`（`.mov` / `.mp4` 都认；开头分屏 S01 的左半屏自动取 `raw/B01.*`）。

## 5. 实拍替换（明早 anni 拍完 B01–B06 之后）

文件放哪：AirDrop 到球球 MacBook，放进 `~/黑客松-EvoTavern/视频素材/raw/`，**文件名 = 镜号**（`B01.mov` … `B06.mov`，`.mp4` 也认；每镜只留最好的那条，多条就 `B01_a.mov` 不会被读）。开头分屏 S01 的左半自动取 `raw/B01.*`。

跑哪条命令（tts 8791 不用开；机器空时 60 s 版约 6–8 min，90 s 版再加 5 min）：

```bash
cd ~/黑客松/aima      # 或 scratchpad 里的 aima 主工作区
ls ~/黑客松-EvoTavern/视频素材/raw/                                   # 确认 B01–B06 在
rm -f ~/黑客松-EvoTavern/视频素材/build/seg/{S01,B02,B03,B04,B05,B06}.mp4   # 只重做有实拍的 6 个镜头
V=3 scripts/cut_demo.sh all                                            # 60 s + 90 s；只要 60 s：V=3 scripts/cut_demo.sh 60
ffprobe -v error -show_entries format=duration,size -of csv=p=0 docs/提交/视频素材/v3_60s.mp4
git add docs/提交/视频素材/v3_60s.mp4 docs/提交/视频素材/v3_90s.mp4 && git commit -m "V3 视频：换入实拍 B01–B06"
```

- 实拍镜头自动不打「模拟外骨骼演示」水印，占位卡自动消失；成片同时写到 `~/黑客松-EvoTavern/视频素材/v3_*.mp4` 和 `docs/提交/视频素材/v3_*.mp4`。
- 只拍到一部分也行：没拍的镜头还是占位卡；想干脆去掉某个占位段，改 `scripts/cut_demo.sh` 末尾 `V60=(…)` / `V90=(…)` 把镜号删掉再跑。
- 拍到的实拍比镜头长：脚本从头截够秒数；想用中间一段，先 `ffmpeg -ss 3 -i raw/B02_a.mov -t 4 -c copy raw/B02.mov`。
- 改字幕 / 旁白位置：改 `build_segs` 那一张表（每行一个镜头：秒、字幕、水印、旁白偏移、来源），删对应 `build/seg/<镜号>.mp4` 重跑；全部重来 `scripts/cut_demo.sh clean && V=3 scripts/cut_demo.sh all`。
- 重新连拍游戏画面（改了建模之后）：见 §6，拍完 `python3 scripts/shot_windows.py` 重算时间窗，`FORCE=1 V=3 scripts/cut_demo.sh all`。

## 6. 重新连拍游戏画面（建模又改了 / 换机器）

```bash
# 模拟器（展位参数）+ 大脑：本线用 8965 口，大脑 8798
cd <aima>; SHELLOS_BRAIN=http://127.0.0.1:8798 SDL_VIDEODRIVER=dummy .venv/bin/python -m shellos.main --sim --ctl terrain --no-record --force-deadman --http 8965 --cap 4 --strength 3.0 --width 16 --wearer 峰哥 &
python3 brain/tts.py &                                             # 127.0.0.1:8791，峰哥克隆声（brain/.env 有 MINIMAX_API_KEY）
S=/private/tmp/claude-502/-Users-qiu----/c4d962c4-a058-4292-a7ea-7fb7af599db1/scratchpad; M=~/黑客松-EvoTavern/视频素材
# 开无头 Chrome 一律加 with_chrome.sh 前缀（全机最多 3 个），一次只开一个、各段串行
$S/with_chrome.sh node $S/rec2.mjs 'http://127.0.0.1:8965/game?fx=off&title=0' $M/frames/lap 210 base=http://127.0.0.1:8965 walkat=9000 \
    "at=70000:fetch('__base__/feedback',{method:'POST',body:JSON.stringify({text:'太陡了'})})"       # 珠峰一整圈：前 9 s 导游开场，红灯自动停，70 s 处喊「太陡了」
$S/render_rest.sh                                                  # 跑酷 / 东京 / 仪表盘决策卡 / 造山 四段
python3 scripts/shot_windows.py                                    # events.jsonl → build/shots.env（每镜在连拍里的起点）
FORCE=1 V=3 scripts/cut_demo.sh all
```

`rec2.mjs` 参数同 v0 的 `rec.mjs`（`walk=1` / `walkat=<ms>` / `stopat=<ms>` / `hud=0` / `pre=<js>` / `click=1`），另加 `at=<ms>:<js>;…`（拍到第几毫秒跑一段 JS，`__base__` = 模拟器地址）和 `js=<表达式>`（每帧多记一个值）；输出 `f%04d.jpg + times.txt + events.jsonl`。录完 `: > data/experiences.jsonl`。

## 7. 交给剪映精修（球球，09:00–10:15）

交接包（都在 `~/黑客松-EvoTavern/视频素材/`）：`v0_60s.mp4`、`v0_90s.mp4`（兜底可交）；`片段/字幕版/<镜号>.mp4`（已叠字）；`片段/干净版/<镜号>.mp4`（没叠字）；`vo/*.wav`；`build/cap/*.png`（字幕 / 角标 / 水印 / 片头片尾 PNG，透明底，可直接拖到最上层轨）；`raw/`（实拍）。

1. 新建项目 1920×1080 / 30 fps；导入 `v0_60s.mp4` 直接「分割」替换，或按 §3 顺序拖 `片段/干净版`。
2. 实拍：选中占位段 → 右键「替换片段」→ `raw/B0x.mov`；S01 用「画中画」轨把 B01 放左半（960 宽）。
3. 字幕：`build/cap/c_<镜号>.png` 拖到最上层轨（透明底，位置已对齐）；要改样式就用剪映文本，字样照 §3 的表（黑底白字 52 号，副行黄 32 号）。
4. 音频：`vo/*.wav` 位置照 §3（偏移在 `build/meta/<镜号>.vo`）；曲库搜「史诗 / 氛围 / 登山」挑 60 s，−18 dB，「音频 → 自动闪避」。
5. 静帧段可加「视频补帧 / 动态模糊」，或等 §6 的动画片段换掉。
6. 导出 1080p / 30 fps / 12 Mbps / H.264 / AAC → `final_60s.mp4`；> 50 MB 就 `ffmpeg -i final_60s.mp4 -crf 23 -maxrate 4M -bufsize 8M -c:a copy final_60s_small.mp4`。

## 8. 口径自检（沿用 60秒演示脚本.md）

- 渲染 / 静帧镜头全部有「模拟外骨骼演示」水印；实拍镜头不打。
- 峰哥音色全片常驻角标「峰哥 · AI 复刻音色 · 本人授权」；23 句 wav 全部是 MiniMax 复刻音色（脚本 `gen_vo.sh` 会拒收 `Cache-Control: no-store` 的女声兜底）。
- 旁白只说「大模型」/「AI」；蜂群时间线模型名以画面为准（现在是 MiniMax-M3）。
- 不说「首个」、不说「能分辨坡度」、不说「峰哥本人配音」、不说「峰哥的影子」。
- 静帧里波形面板写的是旧强度 1.5 Nm，所以字幕没写「3.6 Nm」，只写「软限 4 Nm」；换成 3.0 强度的渲染片段后可以把 R05 字幕改回「峰值 3.6 Nm（强度 3.0）」。
- 造山段没有跳剪（静帧版本身就是 4 张过程图）；R09 角标「×4」。
