# 展位启动清单 · 到场 30 分钟

> 9/24 评审 13:00–16:00。两台机：**展位 MacBook**（zhongrenfei，仓库 `~/aima`，跑 ShellOS + 大屏）和**开发机**（跑大脑 + 峰哥语音，SSH 反向隧道借给 MacBook）。
> 每步：命令 → 预期输出。出问题翻 [故障速查](故障速查.md)，别现场猜。

| 步 | 时间 | 做什么 | 命令 | 预期 |
|---|---|---|---|---|
| 1 | -30 | **供电** | MacBook 插电源（别靠电池，Chrome + 3D 一下午）；外骨骼充满电；手柄充电线备着 | 电源灯亮 |
| 2 | -28 | **接线** | 外骨骼先开机进入工作态，再插 Type-C 到 MacBook；手柄按 PS 键（或 USB 线）| `ls /dev/cu.usbserial*` 有一个 |
| 3 | -26 | **开发机：大脑 + 隧道** | 开发机 `cd ~/aima && brain/run.sh` | `大脑就绪 127.0.0.1:8790`、`峰哥语音就绪 …:8791`、`[tunnel] 连 zhongrenfei@…`（断了 3 s 自动重连，这个窗口别关）|
| 4 | -24 | **MacBook：一键起** | `cd ~/aima && scripts/booth_up.sh` | 五段：硬件 → `ShellOS up` → 看门狗 pid → Chrome 两个窗口 → 绿灯清单 **全绿 ✓** |
| 5 | -22 | **体检**（红了按 → 处理，再跑一次）| `scripts/booth_check.sh` | 10 项 `[OK]`，退出码 0 |
| 6 | -20 | **峰哥先爬一圈留影子** | 队友穿上，仪表盘换人写「峰哥」，按住 R2 原地踏步爬完珠峰（56 步，两处红灯站 2 s） | 登顶动画 + 峰哥解说；`/worlds` 页珠峰卡片出现「影子：峰哥」|
| 7 | -14 | **真机验力矩方向** | 仪表盘切世界 `train_slope`（训练场·长坡），按住 R2 走上坡段，问穿戴者：上坡像有人推、下坡像被拖住？反了就停：`.venv/bin/python scripts/probe.py --torque 0.5` 看正值是伸展还是屈曲，改 `convention.py` 要总指挥点头 | 穿戴者说「推 / 拖」方向对 |
| 8 | -10 | **蒙眼二选一** | `.venv/bin/python scripts/afc.py --trials 10`（穿戴者蒙眼，报上坡 / 下坡）| ≥ 8/10 才能在讲解里说「能分辨坡度」，否则不说 |
| 9 | -6 | **大屏摆位** | 大屏 Chrome（亭模式）拖到外接屏 / 电视；仪表盘窗口留在操作台；iPad 开 `http://<MacBook IP>:8765/`（`ipconfig getifaddr en0` 看 IP；iPad 只能看，按钮 403）| 大屏满屏 3D，iPad 能刷出仪表盘 |
| 10 | -4 | **换到评委档** | 仪表盘换人写「评委-01」（参数回缺省：强度 3.0 / 软限 4 Nm，影子保留）| 时间线出现「换人：评委-01」|
| 11 | -2 | **最后一遍体检** | `scripts/booth_check.sh` | 全绿 ✓ |
| 12 | 0 | **开讲** | 4 分钟脚本 `docs/提交/4分钟展位脚本.md` | — |

## 参数与地址

- 真机启动参数（`booth_up.sh` 缺省）：`--ctl terrain --stepping --cap 4 --strength 3.0 --width 16 --http 8765`；改软限 / 强度：`scripts/booth_up.sh --cap 3 --strength 2.5`
- 排练无外骨骼：`scripts/booth_up.sh --sim`（网页按钮代替 R2）
- 大屏 `http://localhost:8765/game`（亭模式退出 Cmd+Q，重开 `scripts/booth_up.sh --chrome`）· 仪表盘 `http://localhost:8765/` · 选山 `/worlds`
- 日志：ShellOS `/tmp/shellos.log`（`tr '\r' '\n' < /tmp/shellos.log | tail`）· 看门狗 `/tmp/booth-8765.watchdog.log`
- 看门狗：ShellOS 进程没了 1 s 内、5 s 不响应就重起，并从 `data/booth_8765.json` 恢复世界 + 穿戴者（**影子在内存里，重起后要再爬一圈**）
- 全部停：`scripts/booth_up.sh --down`（只停 8765 这一套）

## 开发机侧

```bash
cd ~/aima && brain/run.sh            # 大脑 8790 + 峰哥语音 8791 + 隧道（自动重连）
# 要 Claude：先 export ANTHROPIC_API_KEY=…；没 key 用 brain/.env 里的 MINIMAX_API_KEY（蜂群卡片会显示 MiniMax-M3）
python3 -m shellos.agent.guide --tts && ./deploy.sh    # 导游语音缓存缺句时：预生成 + 同步到 MacBook
```

隧道不通（MacBook 上 `curl -sI 127.0.0.1:8790/health` 拒绝连接）时先查底层：开发机 `ping 100.112.252.66`（Tailscale 在不在）→ `ssh zhongrenfei@100.112.252.66 true`（免密还在不在）→ 再看 `brain/run.sh` 窗口的 `[tunnel]` 行。

## 游戏屏 Chrome：开页面就能出峰哥声音（G 线）

Chrome 默认不让没被点过的页面出声，峰哥导游会先等人按一下键才开讲。`booth_up.sh` 起的两个窗口已经带了 `--autoplay-policy=no-user-gesture-required` 和单独的 `--user-data-dir`（`~/.booth-chrome-8765/game`、`/dash`）。要手动开一个同样能出声的游戏屏：

```bash
open -na "Google Chrome" --args --user-data-dir="$HOME/.fg-kiosk" --autoplay-policy=no-user-gesture-required --start-fullscreen "http://127.0.0.1:8765/game"
```

- `--user-data-dir` 必须单独给一个。Chrome 已经开着的话，同一个配置目录会被交给那个旧进程打开，启动参数不生效。
- 换了配置目录，游戏设置（音量、画质、捷风……）要在这个窗口里重新设一次，设置存在这个目录的 localStorage 里。或者直接写在地址栏里，比如 `?fx=low&npc=0`。
- 检查是不是生效了：页面打开后按 F12 → Console，输入 `__voiceBus.state().unlocked`，应该是 `true`。
- 没用这条命令启动也能用：开页面后按一下字母 / 空格 / 回车，或者点一下屏幕，就解锁了。**Shift / Ctrl / Esc 这类键浏览器不算。**没解锁时画面下方会出「🔈 按任意键开启峰哥声音」。

## 部署前查一下峰哥语音缓存

在开发机主工作区（`booth_check.sh` 的「导游语音」项跑的就是这条）：

```bash
python3 -m shellos.agent.guide --check
```

缺了的话先 `python3 -m shellos.agent.guide --tts` 补齐（MiniMax 按字数计费，缺几句花几分钱），再 `./deploy.sh`。ShellOS 启动时也会打印一遍缺哪几句（`guide.check(fill=True)`）。

## 出问题时看哪里

先 `scripts/booth_check.sh`（10 项红绿）或 `scripts/booth_recover.sh`（自动判症状）；一页纸在 [故障速查](故障速查.md)。游戏页 F12 → Console：

- `__guide.state()`：导游状态。armed = 等人听，playing = 在讲，done = 讲完 / 被打断 / 这一位跳过。
- `__guide.log`：导游每一步的记录。
- `__voiceBus.state()` / `__voiceBus.log`：峰哥声道现在谁在说、有没有解锁。
