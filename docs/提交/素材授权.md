# 素材授权

> 每条线用到外部素材（模型、贴图、声音、字体、代码片段）就在这里记一行：用在哪、来源、许可证、有没有改。没把握的不用。

| 素材 | 用在哪 | 来源 | 许可证 / 依据 | 备注 |
|---|---|---|---|---|
| CesiumMan.glb | 玩家化身、影子、追兵 NPC「疾风」的底模（`static/models/CesiumMan.glb`） | Khronos glTF Sample Models（CesiumMan） | CC BY 4.0（© 2017 Cesium，带商标限制：不用 Cesium 的名字和 logo） | 原有素材。J 线只是换装：分区纯色、加白发 / 衣领 / 亮边几何体，没有改 glb 文件 |
| 追兵 NPC「疾风」造型 | `static/game/npc_jifeng.js` | 原创（代码生成的几何体） | 自有 | 致敬「风系、白发、蓝色短外套、风刃、冲刺拖尾」这一类角色，**不是**任何游戏角色的模型、贴图、名字、台词或 logo。Riot 的 Legal Jibber Jabber 不允许在游戏和应用里用他们的 IP，所以没有用网上从游戏里提取的模型（Sketchfab 上的都属于这种，或者来源不明）。考虑过 Quaternius Universal Base Characters（CC0），但没用：多一个 glb，骨骼名也不同，接不上现有的 pose |
| 追兵 NPC 语音（真人） | `/voice/npc.wav` 优先播 `data/voice/npc/real/`（不进 git） | **配音演员本人现场新录**：按我们的原创台词单（`docs/提交/疾风录音台词单.md`，11 句）录，外加 30–60 秒自由说话作为复刻用的参考音频 | 本人 9/23 在展位**当面口头同意**：用于本次黑客松作品的原创角色「疾风」，包括用 MiniMax 快速复刻、生成新台词（总指挥转达，记在指挥板留言；同意短视频已请球球录下存档（**待确认录了没有**），存在手机 / 本机，不进 git、不公开） | **没用任何从游戏里提取的音频**，也没让本人念游戏原台词；参考音频是新录的自由说话，不是游戏素材。角色外形和名字仍然是原创致敬版（Riot IP 的限制不变）。复刻音色 ID 存在 `data/voice/npc_voice_id`，MiniMax 7 天没用会自动删除。只用于比赛展示，不公开发布 |
| 追兵 NPC 语音（备用：预设音色） | 真人录音缺哪句、又还没复刻时，就用这个合成；缓存在 `data/voice/npc/`（不进 git） | MiniMax 语音合成**系统预设音色**（官方系统音色列表）：第一版用 `Chinese (Mandarin)_Crisp_Girl`，候选 4 个见 `voice.NPC_CANDIDATES` | 按量付费的 API 输出，账号是我们自己的 | 没有克隆任何人。台词原创。9/23 生成两次：第一版 6 句 42 计费字符，候选试听 16 条 200 计费字符 |
| 角色名「疾风」 | `npc_jifeng.js` 里的 `角色名` | 原创代号 | 自有 | 要改名就改这一个常量，或者临时用 `?npcname=` |
