# 素材授权

> 每条线用到外部素材（模型、贴图、声音、字体、代码片段）就在这里记一行：用在哪、来源、许可证、有没有改。没把握的不用。

| 素材 | 用在哪 | 来源 | 许可证 / 依据 | 备注 |
|---|---|---|---|---|
| CesiumMan.glb | 玩家化身、影子的底模（`static/models/CesiumMan.glb`） | Khronos glTF Sample Models（CesiumMan） | CC BY 4.0（© 2017 Cesium，带商标限制：不用 Cesium 的名字和 logo） | 原有素材，glb 文件没改。P 线（峰哥建模）在运行时换头换装：原头盔顶点塌掉，换成代码生成的几何头、针织帽、头发、围脖，衣服是着色器里的纯色 + 条纹；骨骼没动 |
| 峰哥脸部照片 `fengge_face_hd.jpg`（400×540）、`fengge_face.jpg`（272×384） | 3D 峰哥头的脸（`game/fengge.js`，正投影贴图）；画中画头像（`game/fengge_hud.js`） | github.com/w466747380/talk-to-fengge-live 的 `src/talk_to_fengge_live/assets/avatar.png`（675×1200） | MIT（仓库 LICENSE，© 2026 Leaf） | 只裁剪、没修图。hd 版 = 原图 (113, 300) 起 400×540，用 `sips` 裁的。峰哥是真人、在现场：**展示前当面征得本人同意**（肖像 / 口吻 / 声音一起问，口径同评委题库） |
| 峰哥穿搭 | `game/fengge.js` 的衣服着色 | 参考同一张照片里的户外打底衫（深灰 + 胸前青色竖条纹） | 自有（配色和版型是参考，没用照片像素） | **不带任何品牌 logo / 字样**（照片衣服上有品牌标，没画） |
| 针织帽纹理、头发纹理 | `game/fengge.js` | 原创（运行时 canvas 画的） | 自有 | 没有外部贴图文件 |
| Quaternius Universal Base Characters | 没用 | quaternius.com | CC0 | P 线考虑过换底模，没换：A2 的动作（`anim.js` / `avatar.js` 的 pose）是按 CesiumMan 的骨骼名、绑定姿态和逐根量过的转轴调的，换底模这些要重量，比赛前风险大 |
