# 素材授权

> 每条线用到外部素材（模型、贴图、声音、字体、代码片段）就在这里记一行：用在哪、来源、许可证、有没有改。没把握的不用。

| 素材 | 用在哪 | 来源 | 许可证 / 依据 | 备注 |
|---|---|---|---|---|
| CesiumMan.glb | 玩家化身、影子、追兵 NPC「疾风」的底模（`static/models/CesiumMan.glb`） | Khronos glTF Sample Models（CesiumMan） | CC BY 4.0（© 2017 Cesium，带商标限制：不用 Cesium 的名字和 logo） | 原有素材，glb 文件没改。P 线（峰哥建模）运行时换头换装（原头盔顶点塌掉，换成代码生成的几何头、针织帽、头发、围脖，衣服是着色器纯色 + 条纹）；J 线给疾风换装（分区纯色、白发 / 衣领 / 亮边几何体）；骨骼都没动 |
| 峰哥脸部照片 `fengge_face_hd.jpg`（400×540）、`fengge_face.jpg`（272×384） | 3D 峰哥头的脸（`game/fengge.js`，正投影贴图）；画中画头像（`game/fengge_hud.js`） | github.com/w466747380/talk-to-fengge-live 的 `src/talk_to_fengge_live/assets/avatar.png`（675×1200） | MIT（仓库 LICENSE，© 2026 Leaf） | 只裁剪、没修图。hd 版 = 原图 (113, 300) 起 400×540，用 `sips` 裁的。峰哥本人 9/23 当面口头同意本次黑客松展示使用肖像 / 口吻 / AI 复刻声音（记录见 `docs/指挥板.md` 留言）；发到黑客松以外的平台另问 |
| 峰哥穿搭 | `game/fengge.js` 的衣服着色 | 参考同一张照片里的户外打底衫（深灰 + 胸前青色竖条纹） | 自有（配色和版型是参考，没用照片像素） | **不带任何品牌 logo / 字样**（照片衣服上有品牌标，没画） |
| 针织帽纹理、头发纹理 | `game/fengge.js` | 原创（运行时 canvas 画的） | 自有 | 没有外部贴图文件 |
| Quaternius Universal Base Characters | 没用 | quaternius.com | CC0 | P 线考虑过换底模，没换：A2 的动作（`anim.js` / `avatar.js` 的 pose）是按 CesiumMan 的骨骼名、绑定姿态和逐根量过的转轴调的，换底模这些要重量，比赛前风险大 |
| 峰哥口吻（解说人设） | 大脑 `/fengge` 的提示词（`brain/claude_brain.py`）、兜底语录（`shellos/agent/fengge.py` 的 `CANNED`） | github.com/YixiaJack/feng-ge-skill 的 `SKILL.md`（只节选口吻：辩证反转、短句、口头禅） | MIT | 没搬原文，只提炼风格；两性 / 政治话题在提示词里禁掉。峰哥本人已同意（同上） |
| 峰哥参考音频 `fengge_ref.wav`（45 s）→ `fengge_ref.m4a` | MiniMax 快速复刻峰哥音色（`brain/tts.py --clone`），生成解说语音 | github.com/YeJe-cpu/talk-to-fengge 的 `assets/voice_samples/fengge_ref.wav`（剪自峰哥公开直播） | Apache-2.0；峰哥本人同意（同上） | 转 32 kbps AAC 后**上传给了 MiniMax**做复刻（音色 Fengge0923123754 存在 MiniMax）；合成音频只存本机 `data/voice/`，不进 git、不公开发布 |
| 追兵 NPC「捷风」模型 | `static/models/jett/`（scene.gltf + 贴图，**.gitignore 里，不进 git、不上 GitHub**，只由 deploy.sh 的 rsync 同步到展位机）；`static/game/npc_jifeng.js` 加载 | Sketchfab 用户 **BojanV06** 上传的「Jett (Fighting Stance)」（https://sketchfab.com/3d-models/jett-fighting-stance-69f7b293673a4e418d22133fd50a0857），glTF 自动转换版，2.15 万三角面，带格斗站姿动画 | 上传者标的是 CC-BY。**但原始 IP 属于 Riot Games**，这个模型和另外两个上传的面数几乎一样，**很可能是从游戏里提取的**；Riot 的 Legal Jibber Jabber 不允许在游戏和应用里用他们的 IP | 9/23 球球（方案 A）**知情后决定**在非商业的黑客松展示里使用。模型由球球自己登录 Sketchfab 下载（我们没替他登录），不进 git、不公开分发。按 CC-BY 署名：模型作者 BojanV06。文件不在时游戏自动退回原创造型（CesiumMan 换装，下一行） |
| 原创造型（备用） | `npc_jifeng.js` 的 `dress()`：没有捷风模型时用 | CesiumMan 换装 + 代码生成的白发 / 衣领 | 自有（底模 CC BY 4.0，见第一行） | 最早的原创致敬版，现在只做退路 |
| 追兵 NPC 语音（真人） | `/voice/npc.wav` 优先播 `data/voice/npc/real/`（不进 git） | **配音演员本人现场新录**：按我们的原创台词单（`docs/提交/疾风录音台词单.md`，11 句）录，外加 30–60 秒自由说话作为复刻用的参考音频 | 本人 9/23 在展位**当面口头同意**：用于本次黑客松作品里的追兵角色（当时叫「疾风」，后来改名「捷风」），包括用 MiniMax 快速复刻、生成新台词（总指挥转达，记在指挥板留言；同意短视频已请球球录下存档（**待确认录了没有**），存在手机 / 本机，不进 git、不公开） | **没用任何从游戏里提取的音频**，也没让本人念游戏原台词；参考音频是新录的自由说话，不是游戏素材。（外形和名字后来改成了 Sketchfab 模型和「捷风」，见上面两行；声音这一行的来源不变。）复刻音色 ID 存在 `data/voice/npc_voice_id`，MiniMax 7 天没用会自动删除。只用于比赛展示，不公开发布 |
| 追兵 NPC 语音（备用：预设音色） | 真人录音缺哪句、又还没复刻时，就用这个合成；缓存在 `data/voice/npc/`（不进 git） | MiniMax 语音合成**系统预设音色**（官方系统音色列表）：第一版用 `Chinese (Mandarin)_Crisp_Girl`，候选 4 个见 `voice.NPC_CANDIDATES` | 按量付费的 API 输出，账号是我们自己的 | 没有克隆任何人。台词原创。9/23 生成两次：第一版 6 句 42 计费字符，候选试听 16 条 200 计费字符 |
| 角色名「捷风」 | `npc_jifeng.js` 里的 `角色名` | Riot Games《无畏契约》角色 Jett 的官方中文名 | 属于 Riot 的 IP | 9/23 球球决定从原创代号「疾风」改成「捷风」（和模型一起，知情后的选择）。临时换名用 `?npcname=` |
